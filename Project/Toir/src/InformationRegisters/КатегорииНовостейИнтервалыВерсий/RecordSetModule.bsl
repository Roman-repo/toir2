///////////////////////////////////////////////////////////////////////////////////////////////////////
// Copyright (c) 2018, ООО 1С-Софт
// Все права защищены. Эта программа и сопроводительные материалы предоставляются 
// в соответствии с условиями лицензии Attribution 4.0 International (CC BY 4.0)
// Текст лицензии доступен по ссылке:
// https://creativecommons.org/licenses/by/4.0/legalcode
///////////////////////////////////////////////////////////////////////////////////////////////////////

#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

Процедура ОбработкаЗаполнения(ДанныеЗаполнения, СтандартнаяОбработка)

	Для каждого ТекущаяЗапись Из ЭтотОбъект Цикл
		Если ВРег(ТекущаяЗапись.КатегорияНовостей.Код) = ВРег("ProductVersion")
				ИЛИ ВРег(ТекущаяЗапись.КатегорияНовостей.Код) = ВРег("PlatformVersion") Тогда
			// Все нормально.
		Иначе
			// Нестандартная категория.
			// Ошибку не выдавать - оставить как есть.
		КонецЕсли;
	КонецЦикла;

КонецПроцедуры

Процедура ПередЗаписью(Отказ, Замещение)

	// Проверка должна находиться в самом начале процедуры.
	// Это необходимо для того, чтобы никакая бизнес-логика объекта не выполнялась при записи объекта через механизм обмена данными,
	//  поскольку она уже была выполнена для объекта в том узле, где он был создан.
	// В этом случае все данные загружаются в ИБ "как есть", без искажений (изменений),
	//  проверок или каких-либо других дополнительных действий, препятствующих загрузке данных.
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;

	Для каждого ТекущаяЗапись Из ЭтотОбъект Цикл
		Если ВРег(ТекущаяЗапись.КатегорияНовостей.Код) = ВРег("ProductVersion")
			ИЛИ ВРег(ТекущаяЗапись.КатегорияНовостей.Код) = ВРег("PlatformVersion") Тогда
			// Все нормально.
		Иначе
			// Нестандартная категория.
			// Ошибку не выдавать - оставить как есть.
		КонецЕсли;
	КонецЦикла;

	// Вызывается автоматически только из формы, при программной записи необходимо вызывать явно.
	ОбработкаПроверкиЗаполнения(Отказ, Новый Массив);

КонецПроцедуры

Процедура ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты)

	Для каждого ТекущаяЗапись Из ЭтотОбъект Цикл
		// 1. Для категории "ProductVersion" проверять формат: 99.99.999.9999.
		// 2. Для категории "PlatformVersion" проверять формат: 99.99.999.9999.
		Если ВРег(ТекущаяЗапись.КатегорияНовостей.Код) = ВРег("ProductVersion")
				ИЛИ ВРег(ТекущаяЗапись.КатегорияНовостей.Код) = ВРег("PlatformVersion") Тогда
			// Проверка формата версий: только цифры, формат 99.99.999.9999.
			// Версия ОТ.
			ТекстСообщения = НСтр("es='El formato del significado de la versión DE (%ВерсияОТ del %) debe ser 99.99.999.9999 %ПодробныйТекстОшибки del %.';ru='Формат значения версии ОТ (%ВерсияОТ%) должен быть 99.99.999.9999 %ПодробныйТекстОшибки%.';en='Format of the version value FROM (%ВерсияДО%) must be 99.99.999.9999 %ПодробныйТекстОшибки%.'");
			ТекстСообщения = СтрЗаменить(ТекстСообщения, "%ВерсияОТ%", ТекущаяЗапись.ВерсияОТ);
			ПроверитьВерсию(ТекущаяЗапись.ВерсияОТ, ТекстСообщения, Отказ);
			// Версия ДО.
			ТекстСообщения = НСтр("es='El formato del significado de la versión HASTA (%ВерсияДО del %) debe ser 99.99.999.9999 %ПодробныйТекстОшибки del %.';ru='Формат значения версии ДО (%ВерсияДО%) должен быть 99.99.999.9999 %ПодробныйТекстОшибки%.';en='Format of the version value BEFORE (%ВерсияДО%) must be 99.99.999.9999 %ПодробныйТекстОшибки%.'");
			ТекстСообщения = СтрЗаменить(ТекстСообщения, "%ВерсияДО%", ТекущаяЗапись.ВерсияДО);
			ПроверитьВерсию(ТекущаяЗапись.ВерсияДО, ТекстСообщения, Отказ);
		КонецЕсли;
	КонецЦикла;

КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Процедура проверяет строку версии на формат 99.99.999.9999 и в случае несоответствия выдает ошибку.
//
// Параметры:
//  ВерсияСтрокой  - Строка;
//  ТекстСообщения - Строка;
//  Отказ          - Булево.
//
Процедура ПроверитьВерсию(ВерсияСтрокой, ТекстСообщения, Отказ)

	ВсеЦифры = ИнтернетПоддержкаПользователей.ВсеЦифры();

	МассивСловВерсии = СтроковыеФункцииКлиентСервер.РазложитьСтрокуВМассивСлов(ВерсияСтрокой, ".");
	Если МассивСловВерсии.Количество() = 4 Тогда // 99.99.999.9999
		Для СловоВерсии=0 По МассивСловВерсии.Количество()-1 Цикл
			Если СловоВерсии=0 Тогда // 2 цифры
				Если СтрДлина(МассивСловВерсии[СловоВерсии]) <> 2 Тогда
					Сообщение = Новый СообщениеПользователю;
					Сообщение.Текст = СтрЗаменить(
						ТекстСообщения,
						"%ПодробныйТекстОшибки%",
						СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
							НСтр("es='(El primer de la versión %1 debe ser la longitud de 2 símbolos)';ru='(первое число версии %1 должно быть длиной 2 символа)';en='(the first version number %1 must be 2 characters long)'"),
							ВерсияСтрокой));
					Сообщение.УстановитьДанные(ЭтотОбъект);
					Сообщение.Сообщить();
					Отказ = Истина;
					ВызватьИсключение Сообщение.Текст;
				Иначе // Только цифры?
					Для С=1 По СтрДлина(МассивСловВерсии[СловоВерсии]) Цикл
						ТекущийСимвол = Сред(МассивСловВерсии[СловоВерсии], С, 1);
						Если СтрНайти(ВсеЦифры, ТекущийСимвол) = 0 Тогда
							Сообщение = Новый СообщениеПользователю;
							Сообщение.Текст = СтрЗаменить(
								ТекстСообщения,
								"%ПодробныйТекстОшибки%",
								СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
									НСтр("es='(El primer de la versión %1 debe contener solamente las cifras, y es encontrado el símbolo %2)';ru='(первое число версии %1 должно содержать только цифры, а найден символ %2)';en='(the first version number %1 should contain only digits, but %2 symbol is found)'"),
									ВерсияСтрокой,
									ТекущийСимвол));
							Сообщение.УстановитьДанные(ЭтотОбъект);
							Сообщение.Сообщить();
							Отказ = Истина;
							ВызватьИсключение Сообщение.Текст;
						КонецЕсли;
					КонецЦикла;
				КонецЕсли;
			ИначеЕсли СловоВерсии=1 Тогда // 2 цифры
				Если СтрДлина(МассивСловВерсии[СловоВерсии]) <> 2 Тогда
					Сообщение = Новый СообщениеПользователю;
					Сообщение.Текст = СтрЗаменить(
						ТекстСообщения,
						"%ПодробныйТекстОшибки%",
						СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
							НСтр("es='(El segundo de la versión %1 debe ser la longitud de 2 símbolos)';ru='(второе число версии %1 должно быть длиной 2 символа)';en='(second version number %1 must be 2 characters long)'"),
							ВерсияСтрокой));
					Сообщение.УстановитьДанные(ЭтотОбъект);
					Сообщение.Сообщить();
					Отказ = Истина;
					ВызватьИсключение Сообщение.Текст;
				Иначе // Только цифры?
					Для С=1 По СтрДлина(МассивСловВерсии[СловоВерсии]) Цикл
						ТекущийСимвол = Сред(МассивСловВерсии[СловоВерсии], С, 1);
						Если СтрНайти(ВсеЦифры, ТекущийСимвол) = 0 Тогда
							Сообщение = Новый СообщениеПользователю;
							Сообщение.Текст = СтрЗаменить(
								ТекстСообщения,
								"%ПодробныйТекстОшибки%",
								СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
									НСтр("es='(El segundo de la versión %1 debe contener solamente las cifras, y es encontrado el símbolo %2)';ru='(второе число версии %1 должно содержать только цифры, а найден символ %2)';en='(the second version number%1 should contain only digits, but the character %2 was found)'"),
									ВерсияСтрокой,
									ТекущийСимвол));
							Сообщение.УстановитьДанные(ЭтотОбъект);
							Сообщение.Сообщить();
							Отказ = Истина;
							ВызватьИсключение Сообщение.Текст;
						КонецЕсли;
					КонецЦикла;
				КонецЕсли;
			ИначеЕсли СловоВерсии=2 Тогда // 3 цифры
				Если СтрДлина(МассивСловВерсии[СловоВерсии]) <> 3 Тогда
					Сообщение = Новый СообщениеПользователю;
					Сообщение.Текст = СтрЗаменить(
						ТекстСообщения,
						"%ПодробныйТекстОшибки%",
						СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
							НСтр("es='(El tercer de la versión %1 debe ser la longitud de 3 símbolos)';ru='(третье число версии %1 должно быть длиной 3 символа)';en='(third version number %1 must be 3 characters long)'"),
							ВерсияСтрокой));
					Сообщение.УстановитьДанные(ЭтотОбъект);
					Сообщение.Сообщить();
					Отказ = Истина;
					ВызватьИсключение Сообщение.Текст;
				Иначе // Только цифры?
					Для С=1 По СтрДлина(МассивСловВерсии[СловоВерсии]) Цикл
						ТекущийСимвол = Сред(МассивСловВерсии[СловоВерсии], С, 1);
						Если СтрНайти(ВсеЦифры, ТекущийСимвол) = 0 Тогда
							Сообщение = Новый СообщениеПользователю;
							Сообщение.Текст = СтрЗаменить(
								ТекстСообщения,
								"%ПодробныйТекстОшибки%",
								СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
									НСтр("es='(El tercer de la versión %1 debe contener solamente las cifras, y es encontrado el símbolo %2)';ru='(третье число версии %1 должно содержать только цифры, а найден символ %2)';en='(the third version number %1 should contain only digits, but %2 is found)'"),
									ВерсияСтрокой,
									ТекущийСимвол));
							Сообщение.УстановитьДанные(ЭтотОбъект);
							Сообщение.Сообщить();
							Отказ = Истина;
							ВызватьИсключение Сообщение.Текст;
						КонецЕсли;
					КонецЦикла;
				КонецЕсли;
			ИначеЕсли СловоВерсии=3 Тогда // 4 цифры
				Если СтрДлина(МассивСловВерсии[СловоВерсии]) <> 4 Тогда
					Сообщение = Новый СообщениеПользователю;
					Сообщение.Текст = СтрЗаменить(
						ТекстСообщения,
						"%ПодробныйТекстОшибки%",
						СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
							НСтр("es='(El cuarto de la versión %1 debe ser la longitud de 4 símbolos)';ru='(четвертое число версии %1 должно быть длиной 4 символа)';en='(the fourth version number%1 must be 4 characters long)'"),
							ВерсияСтрокой));
					Сообщение.УстановитьДанные(ЭтотОбъект);
					Сообщение.Сообщить();
					Отказ = Истина;
					ВызватьИсключение Сообщение.Текст;
				Иначе // Только цифры?
					Для С=1 По СтрДлина(МассивСловВерсии[СловоВерсии]) Цикл
						ТекущийСимвол = Сред(МассивСловВерсии[СловоВерсии], С, 1);
						Если СтрНайти(ВсеЦифры, ТекущийСимвол) = 0 Тогда
							Сообщение = Новый СообщениеПользователю;
							Сообщение.Текст = СтрЗаменить(
								ТекстСообщения,
								"%ПодробныйТекстОшибки%",
								СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
									НСтр("es='(El cuarto de la versión %1 debe contener solamente las cifras, y es encontrado el símbolo %2)';ru='(четвертое число версии %1 должно содержать только цифры, а найден символ %2)';en='(the fourth version number%1 should contain only numbers, but%2 is found)'"),
									ВерсияСтрокой,
									ТекущийСимвол));
							Сообщение.УстановитьДанные(ЭтотОбъект);
							Сообщение.Сообщить();
							Отказ = Истина;
							ВызватьИсключение Сообщение.Текст;
						КонецЕсли;
					КонецЦикла;
				КонецЕсли;
			КонецЕсли;
		КонецЦикла;
	Иначе
		Сообщение = Новый СообщениеПользователю;
		Сообщение.Текст = СтрЗаменить(
			ТекстСообщения,
			"%ПодробныйТекстОшибки%",
			СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
				НСтр("es='(Deben ser 4 números de la versión, y no %1 como en %2)';ru='(должно быть 4 числа версии, а не %1 как в %2)';en='(there must be 4 version numbers, not %1 as in %2)'"),
				МассивСловВерсии.Количество(),
				ВерсияСтрокой));
		Сообщение.УстановитьДанные(ЭтотОбъект);
		Сообщение.Сообщить();
		Отказ = Истина;
		ВызватьИсключение Сообщение.Текст;
	КонецЕсли;

КонецПроцедуры

#КонецОбласти

#КонецЕсли