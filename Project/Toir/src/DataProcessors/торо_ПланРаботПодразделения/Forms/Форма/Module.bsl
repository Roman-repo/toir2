#Область ОбработчикиСобытийФормы
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Элементы.ГруппаЛегенда.Видимость = Элементы.ОтображениеЛегенды.Пометка;
	
	ПоСметамПоДефектам = Истина;
	ПоСметамПоВнешнимОснованиям = Истина;
	ПоГрафикамРегламентныхМероприятий = Истина;
	ПоПланГрафикамППР = Истина;
	
	ДатаНач = НачалоГода(ТекущаяДата());
	ДатаКон = КонецГода(ТекущаяДата());
	
	ФОИспользоватьВыгрузкуЗагрузкуProject = Константы.торо_ИспользоватьВыгрузкуЗагрузкуMSProject.Получить();
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	ВыводитьПо = "Видам работ";	
	УстановитьВидимостьЭлементов();
	Если ЗначениеЗаполнено(ДатаНач) И ЗначениеЗаполнено(ДатаКон) Тогда
		ПостроитьДиаграммуГанта(ЭтаФорма.Команды.ПостроитьДиаграммуГанта);	
	КонецЕсли;
	
	Элементы.ФормаВыгрузитьВProject.Видимость 	= ФОИспользоватьВыгрузкуЗагрузкуProject;
	Элементы.ФормаЗагрузитьИзProject.Видимость 	= ФОИспользоватьВыгрузкуЗагрузкуProject;
	
	СформироватьЗаголовокСвернутойГруппы();
КонецПроцедуры

&НаКлиенте
Процедура ОбработкаОповещения(ИмяСобытия, Параметр, Источник)
	
	Если ИмяСобытия = "ПАРАМЕТРЫ_ПЕЧАТИ_ПланРабот" Тогда
		
		ПараметрКоманды = Новый Массив; 		

		ПараметрКоманды.Добавить(ПодразделениеПустое);		
		
		СтруктураПараметровКоманды = Новый Структура("ДокППР, Параметры", Объект, Параметр);
		
		торо_Печать.НапечататьДокумент("Обработка.торо_ПланРаботПодразделения",
		"МакетПланаРемонтовНаПериод",
		ПараметрКоманды, СтруктураПараметровКоманды);
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы
&НаКлиенте
Процедура ПолосаРегулированияДиаграммыГантаПриИзменении(Элемент)
	
	Если ПолосаРегулированияДиаграммыГанта < 1 Тогда
		ДиаграммаГантаПодразделения.ПоддержкаМасштаба 				= ПоддержкаМасштабаДиаграммыГанта.ВсеДанные;
	Иначе		
		ДиаграммаГантаПодразделения.ПоддержкаМасштаба 				= ПоддержкаМасштабаДиаграммыГанта.Период;
		ДиаграммаГантаПодразделения.ЕдиницаПериодическогоВарианта 	= ТипЕдиницыШкалыВремени["День"];
		ДиаграммаГантаПодразделения.КратностьПериодическогоВарианта 	= ПолосаРегулированияДиаграммыГанта;		
	КонецЕсли;
		
КонецПроцедуры

&НаКлиенте
Процедура ДиаграммаГантаПодразделенияОбработкаРасшифровки(Элемент, Расшифровки, СтандартнаяОбработка, Дата)
	СтандартнаяОбработка = Ложь;
	Если ТипЗнч(Расшифровки) = Тип("Массив") И Расшифровки.ВГраница()>0 Тогда 		
		СписокПараметров = Новый СписокЗначений();
		МассивРасшифровки0 = Расшифровки[0];
		МассивРасшифровки1 = Расшифровки[1];
		Расшифровки.Очистить();
		Для каждого РасшифровкаЭлемент ИЗ МассивРасшифровки0 Цикл
			СписокПараметров.Добавить(РасшифровкаЭлемент.Значение);	
		КонецЦикла; 
		Для каждого РасшифровкаЭлемент ИЗ МассивРасшифровки1 Цикл
			Если РасшифровкаЭлемент.Ключ <>"ID" Тогда
				СписокПараметров.Добавить(РасшифровкаЭлемент.Значение);	
			КонецЕсли;
		КонецЦикла;
	
		ВыбранноеЗначение = Неопределено;

	
		СписокПараметров.ПоказатьВыборЭлемента(Новый ОписаниеОповещения("ДиаграммаГантаПодразделенияОбработкаРасшифровкиЗавершение", ЭтотОбъект, Новый Структура("МассивРасшифровки1", МассивРасшифровки1)), "Расшифровка "+ МассивРасшифровки0.ВидРемонта+ ", "+МассивРасшифровки1.Объект,);
	ИначеЕсли ТипЗнч(Расшифровки) = Тип("Структура") Тогда
		ПоказатьЗначение(Неопределено, Расшифровки.ВидРемонта);	
	Иначе
		ПоказатьЗначение(Неопределено, Расшифровки); 
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ДиаграммаГантаПодразделенияОбработкаРасшифровкиЗавершение(ВыбранныйЭлемент, ДополнительныеПараметры) Экспорт
    
    МассивРасшифровки1 = ДополнительныеПараметры.МассивРасшифровки1;
    
    
    ВыбранноеЗначение = ВыбранныйЭлемент;
    Если ВыбранноеЗначение<>Неопределено Тогда
        Если ТипЗнч(ВыбранноеЗначение.Значение) = Тип("ДокументСсылка.торо_ЗаявкаНаРемонт") Тогда
            ПараметрыФормы = Новый Структура;
            ПараметрыФормы.Вставить("Ключ",ВыбранноеЗначение.Значение);
            ПараметрыФормы.Вставить("АктивироватьСтроку",Истина);
            ПараметрыФормы.Вставить("ТекСтрокаИзОбработкиID",МассивРасшифровки1.ID);
            
            ОткрытьФорму("Документ.торо_ЗаявкаНаРемонт.ФормаОбъекта",ПараметрыФормы);
            
            
        ИначеЕсли ТипЗнч(ВыбранноеЗначение.Значение) = Тип("ДокументСсылка.торо_ПланГрафикРемонта") Тогда
            ПараметрыФормы = Новый Структура;
            ПараметрыФормы.Вставить("Ключ",ВыбранноеЗначение.Значение);
            ПараметрыФормы.Вставить("АктивироватьСтроку",Истина);
            ПараметрыФормы.Вставить("ТекСтрокаИзОбработкиID",МассивРасшифровки1.ID);
            
            ОткрытьФорму("Документ.торо_ПланГрафикРемонта.ФормаОбъекта",ПараметрыФормы);  	   
            
        ИначеЕсли ТипЗнч(ВыбранноеЗначение.Значение) = Тип("ДокументСсылка.торо_ГрафикРегламентныхМероприятийТОиР") Тогда
            ПараметрыФормы = Новый Структура;
            ПараметрыФормы.Вставить("Ключ",ВыбранноеЗначение.Значение);
            ПараметрыФормы.Вставить("АктивироватьСтроку",Истина);
            ПараметрыФормы.Вставить("ТекСтрокаИзОбработкиID",МассивРасшифровки1.ID);
            
            ОткрытьФорму("Документ.торо_ГрафикРегламентныхМероприятийТОиР.ФормаОбъекта",ПараметрыФормы);				
            
        Иначе
            ПоказатьЗначение(Неопределено, ВыбранноеЗначение.Значение);
        КонецЕсли;
    КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ПодразделениеИсполнительНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	ОткрытьФормуПодбораПодразделений(Элемент, ПодразделениеИсполнитель);
	
КонецПроцедуры

&НаКлиенте
Процедура ПодразделениеВладелецНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	ОткрытьФормуПодбораПодразделений(Элемент, ПодразделениеВладелец);
	
КонецПроцедуры

&НаКлиенте
Процедура ПоСметамПоДефектамПриИзменении(Элемент)
	СформироватьЗаголовокСвернутойГруппы();
КонецПроцедуры

&НаКлиенте
Процедура ПоСметамПоВнешнимОснованиямПриИзменении(Элемент)
	СформироватьЗаголовокСвернутойГруппы();
КонецПроцедуры

&НаКлиенте
Процедура ПоПланГрафикамППРПриИзменении(Элемент)
	СформироватьЗаголовокСвернутойГруппы();
КонецПроцедуры

&НаКлиенте
Процедура ПоГрафикамРегламентныхМероприятийПриИзменении(Элемент)
	СформироватьЗаголовокСвернутойГруппы();
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовТаблицыФормыДеревоРемонтов
&НаКлиенте
Процедура ДеревоРемонтовВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	
	Если ЗначениеЗаполнено(Элемент.ТекущиеДанные.ДокументРегистратор) и Поле.Имя = "ТаблицаРемонтовДокументРегистратор" Тогда 	
		ПараметрыФормы = Новый Структура;
		ПараметрыФормы.Вставить("Ключ",Элемент.ТекущиеДанные.ДокументРегистратор);
		ПараметрыФормы.Вставить("АктивироватьСтроку",Истина);
		ПараметрыФормы.Вставить("ТекСтрокаИзОбработкиID",Элемент.ТекущиеДанные.ID);	
		Если ТипЗнч(Элемент.ТекущиеДанные.ДокументРегистратор) = Тип("ДокументСсылка.торо_ЗаявкаНаРемонт") Тогда
			ОткрытьФорму("Документ.торо_ЗаявкаНаРемонт.ФормаОбъекта", ПараметрыФормы);
		ИначеЕсли ТипЗнч(Элемент.ТекущиеДанные.ДокументРегистратор) = Тип("ДокументСсылка.торо_ПланГрафикРемонта") Тогда
			ОткрытьФорму("Документ.торо_ПланГрафикРемонта.ФормаОбъекта", ПараметрыФормы);	
		ИначеЕсли ТипЗнч(Элемент.ТекущиеДанные.ДокументРегистратор) = Тип("ДокументСсылка.торо_ГрафикРегламентныхМероприятийТОиР") Тогда
			ОткрытьФорму("Документ.торо_ГрафикРегламентныхМероприятийТОиР.ФормаОбъекта", ПараметрыФормы);	
		ИначеЕсли ТипЗнч(Элемент.ТекущиеДанные.ДокументРегистратор) = Тип("ДокументСсылка.торо_ОстановочныеРемонты") Тогда
			ОткрытьФорму("Документ.торо_ОстановочныеРемонты.ФормаОбъекта", ПараметрыФормы);	
		ИначеЕсли ТипЗнч(Элемент.ТекущиеДанные.ДокументРегистратор) = Тип("ДокументСсылка.торо_ЗакрытиеЗаявокИРемонтов") Тогда
			ОткрытьФорму("Документ.торо_ЗакрытиеЗаявокИРемонтов.ФормаОбъекта", ПараметрыФормы);
		ИначеЕсли ТипЗнч(Элемент.ТекущиеДанные.ДокументРегистратор) = Тип("ДокументСсылка.торо_ВнешнееОснованиеДляРабот") Тогда
			ОткрытьФорму("Документ.торо_ВнешнееОснованиеДляРабот.ФормаОбъекта", ПараметрыФормы);
		ИначеЕсли ТипЗнч(Элемент.ТекущиеДанные.ДокументРегистратор) = Тип("ДокументСсылка.торо_ВыявленныеДефекты") Тогда
			ОткрытьФорму("Документ.торо_ВыявленныеДефекты.ФормаОбъекта", ПараметрыФормы);
		КонецЕсли;
	ИначеЕсли ЗначениеЗаполнено(Элемент.ТекущиеДанные.ОбъектРемонта) и Поле.Имя = "ТаблицаРемонтовОбъектРемонта" Тогда 
		ПараметрыФормы = Новый Структура;
		ПараметрыФормы.Вставить("Ключ", Элемент.ТекущиеДанные.ОбъектРемонта);
		Если ТипЗнч(Элемент.ТекущиеДанные.ОбъектРемонта) = Тип("СправочникСсылка.торо_СписокОбъектовРегламентногоМероприятия") Тогда
			ОткрытьФорму("Справочник.торо_СписокОбъектовРегламентногоМероприятия.ФормаОбъекта", ПараметрыФормы);
		Иначе
			ОткрытьФорму("Справочник.торо_ОбъектыРемонта.ФормаОбъекта", ПараметрыФормы);
		КонецЕсли; 
	ИначеЕсли ЗначениеЗаполнено(Элемент.ТекущиеДанные.ОбъектРемонта) и Поле.Имя = "ДеревоРемонтовПодразделение" И ВыводитьПо = "Объектам ремонта" Тогда 
		ПараметрыФормы = Новый Структура;
		ПараметрыФормы.Вставить("Ключ", Элемент.ТекущиеДанные.ОбъектРемонта);
		Если ТипЗнч(Элемент.ТекущиеДанные.ОбъектРемонта) = Тип("СправочникСсылка.торо_СписокОбъектовРегламентногоМероприятия") Тогда
			ОткрытьФорму("Справочник.торо_СписокОбъектовРегламентногоМероприятия.ФормаОбъекта", ПараметрыФормы);
		Иначе
			ОткрытьФорму("Справочник.торо_ОбъектыРемонта.ФормаОбъекта", ПараметрыФормы);
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы
&НаКлиенте
Процедура ПечатьПланаРабот(Команда)
	
	ФормаВыбораРемонтаОборудования = ПолучитьФорму("Обработка.торо_ПланРаботПодразделения.Форма.ФормаВводаПериода", 
	Новый Структура("ДокППР, КлючНазначенияИспользования, ДатаНачала, ДатаОкончания,ПодразделениеВладелец,ПодразделениеИсполнитель, ВыводитьПо",, "ПАРАМЕТРЫ_ПЕЧАТИ_ПланРабот",ДатаНач,ДатаКон,ПодразделениеВладелец,ПодразделениеИсполнитель,ВыводитьПо)
	, ,);
	ФормаВыбораРемонтаОборудования.Открыть();
	
КонецПроцедуры

&НаКлиенте
Процедура ПостроитьДиаграммуГанта(Команда)
	
	Если НЕ ЗначениеЗаполнено(ДатаНач) И НЕ ЗначениеЗаполнено(ДатаКон) Тогда
		ТекстСообщения = НСтр("es='Es imposible construir el diagrama - no es rellenado el período de la representación.';ru='Невозможно построить диаграмму - не заполнен период отображения.';en='Unable to build chart - display period is not filled.'");
		ОбщегоНазначенияКлиент.СообщитьПользователю(ТекстСообщения);
	ИначеЕсли НЕ ЗначениеЗаполнено(ДатаНач) Тогда
		ТекстСообщения = НСтр("es='Es imposible construir el diagrama - no es rellenada la fecha del comienzo del período.';ru='Невозможно построить диаграмму - не заполнена дата начала периода.';en='Unable to build a chart - the period start date is not filled.'");
		ОбщегоНазначенияКлиент.СообщитьПользователю(ТекстСообщения);
	ИначеЕсли НЕ ЗначениеЗаполнено(ДатаКон) Тогда
		ТекстСообщения = НСтр("es='Es imposible construir el diagrama - no es rellenada la fecha del fin del período.';ru='Невозможно построить диаграмму - не заполнена дата конца периода.';en='Unabble to build a chart - the end date of the period is not filled.'");
		ОбщегоНазначенияКлиент.СообщитьПользователю(ТекстСообщения);
	Иначе
		Состояние("Построение диаграммы ремонтов по подразделениям...");
		ПостроитьДиаграммуГантаНаСервере();
	КонецЕсли;
		
КонецПроцедуры

&НаКлиенте
Процедура ВыгрузитьВProject(Команда)
	
	СписокРемонтов = Новый Массив;
	Для каждого СтрокаПодразделения из ДеревоРемонтов.ПолучитьЭлементы() Цикл
		Для каждого СтрокаРемонта из СтрокаПодразделения.ПолучитьЭлементы() Цикл
			СписокРемонтов.Добавить(Новый Структура("ДокументРегистратор, ID", СтрокаРемонта.ДокументРегистратор, СтрокаРемонта.ID));
		КонецЦикла;
	КонецЦикла;
	
	Торо_ИнтеграцияСmsProject.ОтправитьВProject(СписокРемонтов,Неопределено);
	
КонецПроцедуры

&НаКлиенте
Процедура ЗагрузитьИзProject(Команда)
	
	Оп = Новый ОписаниеОповещения("ВыполнитьПослеЗакрытияФормы", ЭтотОбъект);
	Форма = ОткрытьФорму("ОбщаяФорма.торо_ФормаВыбораФайлаProjectКЗагрузке",,,,,, Оп, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
КонецПроцедуры

&НаКлиенте
Процедура ВыполнитьПослеЗакрытияФормы(РезультатЗакрытия, ДополнительныеПараметры) Экспорт
	
	Если РезультатЗакрытия = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ОбновленныеДокументы = Новый Массив;
	
	Если ТипЗнч(РезультатЗакрытия) = Тип("Строка") И РезультатЗакрытия <> "" Тогда
		Если НРег(Прав(РезультатЗакрытия, 3)) = "mpp" Тогда
			ОбновленныеДокументы = Торо_ИнтеграцияСmsProject.ЗагрузитьЗаявки(РезультатЗакрытия, "ПланРабот");
			
		ИначеЕсли НРег(Прав(РезультатЗакрытия, 3)) = "xml" Тогда
			ОбновленныеДокументы = Торо_ИнтеграцияСmsProject.ЗагрузитьЗаявкиИзXML(РезультатЗакрытия, "ПланРабот");
			
		КонецЕсли;
	КонецЕсли;
	
	Для каждого ОбновленныйДокумент из ОбновленныеДокументы Цикл
		Оповестить("ЗагрузкаИзПроджект", ОбновленныйДокумент);
	КонецЦикла;

КонецПроцедуры

&НаКлиенте
Процедура СортироватьПоОР(Команда)
	
	СортироватьПоОР = НЕ СортироватьПоОР;
	Элементы.СортироватьПоОР.Пометка = СортироватьПоОР;

КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Процедура ПостроитьДиаграммуГантаНаСервере()
	    
    мДатаОкончанияПланирования = ДатаКон;
	
	ОтображатьЗамещенные = Истина;
	
	Запрос = Новый Запрос;
	Запрос.МенеджерВременныхТаблиц = Новый МенеджерВременныхТаблиц;
	// Отбор по датам должен накладываться на окончательные результаты, поэтому отбор по ним так поздно происходит.
	Запрос.Текст =
	"ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	торо_ПлановыеРемонтныеРаботыСрезПервых.Регистратор КАК Регистратор,
	|	торо_ПлановыеРемонтныеРаботыСрезПервых.ID КАК ID,
	|	торо_ПлановыеРемонтныеРаботыСрезПервых.ОбъектРемонтныхРабот КАК ОбъектРемонтныхРабот,
	|	ВЫБОР
	|		КОГДА ТИПЗНАЧЕНИЯ(торо_ПлановыеРемонтныеРаботыСрезПервых.ОбъектРемонтныхРабот.Подразделение) = ТИП(Справочник.СтруктураПредприятия)
	|			ТОГДА торо_ПлановыеРемонтныеРаботыСрезПервых.ОбъектРемонтныхРабот.Подразделение
	|		ИНАЧЕ ЗНАЧЕНИЕ(Справочник.СтруктураПредприятия.ПустаяСсылка)
	|	КОНЕЦ КАК Владелец,
	|	торо_ПлановыеРемонтныеРаботыСрезПервых.ВидРемонтныхРабот КАК ВидРемонтныхРабот,
	|	торо_ПлановыеРемонтныеРаботыСрезПервых.ДатаНачалаРемонтныхРабот КАК ДатаНачалаРемонтныхРабот,
	|	торо_ПлановыеРемонтныеРаботыСрезПервых.ДатаОкончанияРемонтныхРабот КАК ДатаОкончанияРемонтныхРабот,
	|	торо_ПлановыеРемонтныеРаботыСрезПервых.Исполнитель КАК Исполнитель,
	|	""ППР"" КАК ИсточникДанных,
	|	торо_ПлановыеРемонтныеРаботыСрезПервых.Замещен КАК Замещен
	|ПОМЕСТИТЬ ВТ_ППРСрезПервых
	|ИЗ
	|	РегистрСведений.торо_ПлановыеРемонтныеРаботы.СрезПервых(, ) КАК торо_ПлановыеРемонтныеРаботыСрезПервых
	|ГДЕ
	|	&ПоПланГрафикамППР
	|	И НЕ торо_ПлановыеРемонтныеРаботыСрезПервых.Отменен
	|	И ВЫБОР
	|			КОГДА &ЕстьОтборПодразделниеВладелец
	|				ТОГДА торо_ПлановыеРемонтныеРаботыСрезПервых.ОбъектРемонтныхРабот.Подразделение В (&СписокПодразделенияВладелец)
	|			ИНАЧЕ ИСТИНА
	|		КОНЕЦ
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	ID
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	торо_ПлановыеРемонтныеРаботыСрезПоследних.Регистратор КАК Регистратор,
	|	торо_ПлановыеРемонтныеРаботыСрезПоследних.ID КАК ID,
	|	торо_ПлановыеРемонтныеРаботыСрезПоследних.ОбъектРемонтныхРабот КАК ОбъектРемонтныхРабот,
	|	торо_ПлановыеРемонтныеРаботыСрезПоследних.ВидРемонтныхРабот КАК ВидРемонтныхРабот,
	|	торо_ПлановыеРемонтныеРаботыСрезПоследних.ДатаНачалаРемонтныхРабот КАК ДатаНачалаРемонтныхРабот,
	|	торо_ПлановыеРемонтныеРаботыСрезПоследних.ДатаОкончанияРемонтныхРабот КАК ДатаОкончанияРемонтныхРабот,
	|	торо_ПлановыеРемонтныеРаботыСрезПоследних.Отменен КАК Отменен,
	|	торо_ПлановыеРемонтныеРаботыСрезПоследних.Исполнитель КАК Исполнитель,
	|	торо_ПлановыеРемонтныеРаботыСрезПоследних.Замещен КАК Замещен,
	|	торо_ПлановыеРемонтныеРаботыСрезПоследних.СпособВыполнения КАК СпособВыполнения
	|ПОМЕСТИТЬ ВТ_ППРСрезПоследних
	|ИЗ
	|	РегистрСведений.торо_ПлановыеРемонтныеРаботы.СрезПоследних(
	|			,
	|			ID В
	|				(ВЫБРАТЬ
	|					ВТ_ППРСрезПервых.ID
	|				ИЗ
	|					ВТ_ППРСрезПервых КАК ВТ_ППРСрезПервых)) КАК торо_ПлановыеРемонтныеРаботыСрезПоследних
	|ГДЕ
	|	ВЫБОР
	|			КОГДА &ЕстьОтборПодразделниеИсполнитель
	|				ТОГДА торо_ПлановыеРемонтныеРаботыСрезПоследних.Исполнитель В (&СписокПодразделенияИсполнитель)
	|			ИНАЧЕ ИСТИНА
	|		КОНЕЦ
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	ID
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_ППРСрезПервых.Регистратор КАК Регистратор,
	|	ВТ_ППРСрезПервых.ID КАК ID,
	|	ВТ_ППРСрезПервых.ОбъектРемонтныхРабот КАК ОбъектРемонтныхРабот,
	|	ВТ_ППРСрезПервых.Владелец КАК Владелец,
	|	ВТ_ППРСрезПервых.ВидРемонтныхРабот КАК ВидРемонтныхРабот,
	|	ВТ_ППРСрезПервых.ИсточникДанных КАК ИсточникДанных,
	|	ВТ_ППРСрезПоследних.ДатаНачалаРемонтныхРабот КАК ДатаНачалаРемонтныхРабот,
	|	ВТ_ППРСрезПоследних.ДатаОкончанияРемонтныхРабот КАК ДатаОкончанияРемонтныхРабот,
	|	ВТ_ППРСрезПоследних.Исполнитель КАК Исполнитель,
	|	ВТ_ППРСрезПоследних.Отменен КАК Отменен,
	|	ВТ_ППРСрезПоследних.СпособВыполнения КАК СпособВыполнения
	|ПОМЕСТИТЬ ВТ_ППР
	|ИЗ
	|	ВТ_ППРСрезПервых КАК ВТ_ППРСрезПервых
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_ППРСрезПоследних КАК ВТ_ППРСрезПоследних
	|		ПО ВТ_ППРСрезПервых.ID = ВТ_ППРСрезПоследних.ID
	|ГДЕ
	|	НЕ ВТ_ППРСрезПоследних.Отменен
	|	И НЕ ВТ_ППРСрезПоследних.Замещен
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	торо_ПлановыеМероприятияСрезПоследних.Регистратор КАК Регистратор,
	|	торо_ПлановыеМероприятияСрезПоследних.СписокОбъектов КАК СписокОбъектов,
	|	торо_ПлановыеМероприятияСрезПоследних.ВидМероприятия КАК ВидМероприятия,
	|	торо_ПлановыеМероприятияСрезПоследних.ID КАК ID,
	|	торо_ПлановыеМероприятияСрезПоследних.ДатаНачала КАК ДатаНачала,
	|	торо_ПлановыеМероприятияСрезПоследних.ДатаОкончания КАК ДатаОкончания,
	|	торо_ПлановыеМероприятияСрезПоследних.Исполнитель КАК Исполнитель,
	|	ЗНАЧЕНИЕ(Справочник.СтруктураПредприятия.ПустаяСсылка) КАК Владелец,
	|	""РМ"" КАК ИсточникДанных,
	|	торо_ПлановыеМероприятияСрезПоследних.Отменен КАК Отменен,
	|	торо_ПлановыеМероприятияСрезПоследних.СпособВыполнения КАК СпособВыполнения
	|ПОМЕСТИТЬ ВТ_ГрафикиРМ
	|ИЗ
	|	РегистрСведений.торо_ПлановыеМероприятия.СрезПоследних КАК торо_ПлановыеМероприятияСрезПоследних
	|ГДЕ
	|	&ПоГрафикамРегламентныхМероприятий
	|	И НЕ торо_ПлановыеМероприятияСрезПоследних.Отменен
	|	И ВЫБОР
	|			КОГДА &ЕстьОтборПодразделниеВладелец
	|				ТОГДА ЗНАЧЕНИЕ(Справочник.СтруктураПредприятия.ПустаяСсылка) В (&СписокПодразделенияВладелец)
	|			ИНАЧЕ ИСТИНА
	|		КОНЕЦ
	|	И ВЫБОР
	|			КОГДА &ЕстьОтборПодразделниеИсполнитель
	|				ТОГДА торо_ПлановыеМероприятияСрезПоследних.Исполнитель В (&СписокПодразделенияИсполнитель)
	|			ИНАЧЕ ИСТИНА
	|		КОНЕЦ
	|	И ВЫБОР
	|			КОГДА &ЕстьОтборПоБригадам
	|				ТОГДА ЗНАЧЕНИЕ(Справочник.торо_РемонтныеБригады.ПустаяСсылка) В (&СписокБригад)
	|			ИНАЧЕ ИСТИНА
	|		КОНЕЦ
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	торо_ЗаявкиПоРемонтамСрезПоследних.Регистратор КАК Регистратор,
	|	торо_ЗаявкиПоРемонтамСрезПоследних.IDРемонта КАК IDРемонта,
	|	торо_ЗаявкиПоРемонтамСрезПоследних.Исполнитель КАК Исполнитель,
	|	торо_ЗаявкиПоРемонтамСрезПоследних.УточнениеИсполнителя КАК Бригада,
	|	торо_ЗаявкиПоРемонтамСрезПоследних.ДатаНачалаРемонтныхРабот КАК ДатаНачалаРемонтныхРабот,
	|	торо_ЗаявкиПоРемонтамСрезПоследних.ДатаОкончанияРемонтныхРабот КАК ДатаОкончанияРемонтныхРабот,
	|	торо_ЗаявкиПоРемонтамСрезПоследних.Отменен КАК Отменен,
	|	торо_ЗаявкиПоРемонтамСрезПоследних.Период КАК Период
	|ПОМЕСТИТЬ ВТ_Заявки
	|ИЗ
	|	РегистрСведений.торо_ЗаявкиПоРемонтам.СрезПоследних(, ) КАК торо_ЗаявкиПоРемонтамСрезПоследних
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.торо_ОтмененныеЗаявки КАК торо_ОтмененныеЗаявки
	|		ПО торо_ЗаявкиПоРемонтамСрезПоследних.IDРемонта = торо_ОтмененныеЗаявки.ИДРемонта
	|			И торо_ЗаявкиПоРемонтамСрезПоследних.Регистратор = торо_ОтмененныеЗаявки.Заявка
	|ГДЕ
	|	НЕ торо_ЗаявкиПоРемонтамСрезПоследних.Отменен
	|	И торо_ОтмененныеЗаявки.НомерСтроки ЕСТЬ NULL
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	IDРемонта
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	торо_ОстановочныеРемонтыСрезПоследних.Регистратор КАК Регистратор,
	|	торо_ОстановочныеРемонтыСрезПоследних.IDОсновного КАК IDОсновного,
	|	торо_ОстановочныеРемонтыСрезПоследних.IDЗависимого КАК IDЗависимого,
	|	торо_ОстановочныеРемонтыСрезПоследних.Отвязан КАК Отвязан,
	|	торо_ОстановочныеРемонтыСрезПоследних.ДатаНачалаРемонта КАК ДатаНачалаРемонта,
	|	торо_ОстановочныеРемонтыСрезПоследних.ДатаОкончанияРемонта КАК ДатаОкончанияРемонта
	|ПОМЕСТИТЬ ВТ_Остановочные
	|ИЗ
	|	РегистрСведений.торо_ОстановочныеРемонты.СрезПоследних КАК торо_ОстановочныеРемонтыСрезПоследних
	|ГДЕ
	|	НЕ торо_ОстановочныеРемонтыСрезПоследних.Отвязан
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	IDЗависимого
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	торо_РемонтыСрезПервых.Регистратор КАК Регистратор,
	|	торо_РемонтыСрезПервых.ID КАК ID,
	|	торо_РемонтыСрезПервых.ОбъектРемонта КАК ОбъектРемонта,
	|	торо_РемонтыСрезПервых.ВидРемонта КАК ВидРемонта,
	|	ВЫБОР
	|		КОГДА ТИПЗНАЧЕНИЯ(торо_РемонтыСрезПервых.ОбъектРемонта.Подразделение) = ТИП(Справочник.СтруктураПредприятия)
	|			ТОГДА торо_РемонтыСрезПервых.ОбъектРемонта.Подразделение
	|		ИНАЧЕ ЗНАЧЕНИЕ(Справочник.СтруктураПредприятия.ПустаяСсылка)
	|	КОНЕЦ КАК Владелец,
	|	торо_РемонтыСрезПервых.ДатаНачалаПлан КАК ДатаНачалаПлан,
	|	торо_РемонтыСрезПервых.ДатаОкончанияПлан КАК ДатаОкончанияПлан
	|ПОМЕСТИТЬ ВТ_Ремонты
	|ИЗ
	|	РегистрСведений.торо_Ремонты.СрезПервых(
	|			,
	|			ID В
	|				(ВЫБРАТЬ
	|					ВТ_Заявки.IDРемонта
	|				ИЗ
	|					ВТ_Заявки КАК ВТ_Заявки)) КАК торо_РемонтыСрезПервых
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	ID
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	торо_РемонтыСрезПоследних.Регистратор КАК Регистратор,
	|	торо_РемонтыСрезПоследних.ID КАК ID,
	|	торо_РемонтыСрезПоследних.ОбъектРемонта КАК ОбъектРемонта,
	|	торо_РемонтыСрезПоследних.ВидРемонта КАК ВидРемонта,
	|	торо_РемонтыСрезПоследних.ДатаНачалаПлан КАК ДатаНачалаПлан,
	|	торо_РемонтыСрезПоследних.ДатаОкончанияПлан КАК ДатаОкончанияПлан
	|ПОМЕСТИТЬ ВТ_РемонтыСрезПоследних
	|ИЗ
	|	РегистрСведений.торо_Ремонты.СрезПоследних(
	|			,
	|			ID В
	|				(ВЫБРАТЬ
	|					ВТ_Заявки.IDРемонта
	|				ИЗ
	|					ВТ_Заявки КАК ВТ_Заявки)) КАК торо_РемонтыСрезПоследних
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	ID
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	торо_ОтмененныеРемонты.ID КАК ID
	|ПОМЕСТИТЬ ВТ_Отмененые
	|ИЗ
	|	РегистрСведений.торо_ОтмененныеРемонты КАК торо_ОтмененныеРемонты
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	ID
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_Ремонты.Регистратор КАК Регистратор,
	|	ВТ_Заявки.IDРемонта КАК IDРемонта,
	|	ВТ_Заявки.Исполнитель КАК Исполнитель,
	|	ВТ_Заявки.Бригада КАК Бригада,
	|	ВТ_Заявки.ДатаНачалаРемонтныхРабот КАК ДатаНачалаРемонтныхРабот,
	|	ВТ_Заявки.ДатаОкончанияРемонтныхРабот КАК ДатаОкончанияРемонтныхРабот,
	|	""СПД"" КАК ИсточникДанных,
	|	ВТ_Ремонты.ОбъектРемонта КАК ОбъектРемонта,
	|	ВТ_Ремонты.ВидРемонта КАК ВидРемонта,
	|	ВТ_Ремонты.Владелец КАК Владелец,
	|	ВТ_Заявки.Отменен КАК Отменен
	|ПОМЕСТИТЬ ВТ_ЗаявкиПоДефектам
	|ИЗ
	|	ВТ_Заявки КАК ВТ_Заявки
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_Ремонты КАК ВТ_Ремонты
	|		ПО ВТ_Заявки.IDРемонта = ВТ_Ремонты.ID
	|			И (ТИПЗНАЧЕНИЯ(ВТ_Ремонты.Регистратор) = ТИП(Документ.торо_ВыявленныеДефекты))
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_Отмененые КАК ВТ_Отмененые
	|		ПО ВТ_Заявки.IDРемонта = ВТ_Отмененые.ID
	|ГДЕ
	|	&ПоСметамПоДефектам
	|	И ВЫБОР
	|			КОГДА &ЕстьОтборПодразделниеВладелец
	|				ТОГДА ВТ_Ремонты.Владелец В (&СписокПодразделенияВладелец)
	|			ИНАЧЕ ИСТИНА
	|		КОНЕЦ
	|	И ВЫБОР
	|			КОГДА &ЕстьОтборПодразделниеИсполнитель
	|				ТОГДА ВТ_Заявки.Исполнитель В (&СписокПодразделенияИсполнитель)
	|			ИНАЧЕ ИСТИНА
	|		КОНЕЦ
	|	И ВЫБОР
	|			КОГДА &ЕстьОтборПоБригадам
	|				ТОГДА ВТ_Заявки.Бригада В (&СписокБригад)
	|			ИНАЧЕ ИСТИНА
	|		КОНЕЦ
	|	И ВТ_Отмененые.ID ЕСТЬ NULL
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_Ремонты.Регистратор КАК Регистратор,
	|	ВТ_Заявки.IDРемонта КАК IDРемонта,
	|	ВТ_Заявки.Исполнитель КАК Исполнитель,
	|	ВТ_Заявки.Бригада КАК Бригада,
	|	ВТ_Заявки.ДатаНачалаРемонтныхРабот КАК ДатаНачалаРемонтныхРабот,
	|	ВТ_Заявки.ДатаОкончанияРемонтныхРабот КАК ДатаОкончанияРемонтныхРабот,
	|	""СПВН"" КАК ИсточникДанных,
	|	ВТ_Ремонты.ОбъектРемонта КАК ОбъектРемонта,
	|	ВТ_Ремонты.ВидРемонта КАК ВидРемонта,
	|	ВТ_Ремонты.Владелец КАК Владелец,
	|	ВТ_Заявки.Отменен КАК Отменен
	|ПОМЕСТИТЬ ВТ_ЗаявкиПоВнешнимОснованиям
	|ИЗ
	|	ВТ_Заявки КАК ВТ_Заявки
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ВТ_Ремонты КАК ВТ_Ремонты
	|		ПО ВТ_Заявки.IDРемонта = ВТ_Ремонты.ID
	|			И (ТИПЗНАЧЕНИЯ(ВТ_Ремонты.Регистратор) = ТИП(Документ.торо_ВнешнееОснованиеДляРабот))
	|ГДЕ
	|	&ПоСметамПоВнешнимОснованиям
	|	И ВЫБОР
	|			КОГДА &ЕстьОтборПодразделниеВладелец
	|				ТОГДА ВТ_Ремонты.Владелец В (&СписокПодразделенияВладелец)
	|			ИНАЧЕ ИСТИНА
	|		КОНЕЦ
	|	И ВЫБОР
	|			КОГДА &ЕстьОтборПодразделниеИсполнитель
	|				ТОГДА ВТ_Заявки.Исполнитель В (&СписокПодразделенияИсполнитель)
	|			ИНАЧЕ ИСТИНА
	|		КОНЕЦ
	|	И ВЫБОР
	|			КОГДА &ЕстьОтборПоБригадам
	|				ТОГДА ВТ_Заявки.Бригада В (&СписокБригад)
	|			ИНАЧЕ ИСТИНА
	|		КОНЕЦ
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_ППР.Регистратор КАК Регистратор,
	|	ВТ_ППР.ID КАК IDРемонта,
	|	ВТ_ППР.ОбъектРемонтныхРабот КАК ОбъектРемонта,
	|	ВТ_ППР.Владелец КАК Владелец,
	|	ВТ_ППР.ВидРемонтныхРабот КАК ВидРемонта,
	|	ЕСТЬNULL(ВТ_Заявки.ДатаНачалаРемонтныхРабот, ВТ_ППР.ДатаНачалаРемонтныхРабот) КАК ДатаНачала,
	|	ЕСТЬNULL(ВТ_Заявки.ДатаОкончанияРемонтныхРабот, ВТ_ППР.ДатаОкончанияРемонтныхРабот) КАК ДатаОкончания,
	|	ЕСТЬNULL(ВТ_Заявки.Исполнитель, ВТ_ППР.Исполнитель) КАК Исполнитель,
	|	ВТ_ППР.ИсточникДанных КАК ИсточникДанных,
	|	ЕСТЬNULL(ВТ_Заявки.Бригада, ЗНАЧЕНИЕ(Справочник.торо_РемонтныеБригады.ПустаяСсылка)) КАК Бригада,
	|	ЕСТЬNULL(ВТ_Заявки.Отменен, ЛОЖЬ) КАК Отменен,
	|	ВЫБОР
	|		КОГДА ВТ_Заявки.Исполнитель ЕСТЬ NULL
	|			ТОГДА ВТ_ППР.СпособВыполнения
	|		ИНАЧЕ ВЫБОР
	|				КОГДА ТИПЗНАЧЕНИЯ(ВТ_Заявки.Исполнитель) = ТИП(Справочник.Контрагенты)
	|					ТОГДА ЗНАЧЕНИЕ(Перечисление.СпособыСтроительства.Подрядный)
	|				ИНАЧЕ ЗНАЧЕНИЕ(Перечисление.СпособыСтроительства.Хозспособ)
	|			КОНЕЦ
	|	КОНЕЦ КАК СпособВыполнения
	|ПОМЕСТИТЬ ВТ_Данные
	|ИЗ
	|	ВТ_ППР КАК ВТ_ППР
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_Заявки КАК ВТ_Заявки
	|		ПО ВТ_ППР.ID = ВТ_Заявки.IDРемонта
	|ГДЕ
	|	ВЫБОР
	|			КОГДА &ЕстьОтборПоБригадам
	|				ТОГДА ЕСТЬNULL(ВТ_Заявки.Бригада, ЗНАЧЕНИЕ(Справочник.торо_РемонтныеБригады.ПустаяСсылка)) В (&СписокБригад)
	|			ИНАЧЕ ИСТИНА
	|		КОНЕЦ
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	ВТ_ЗаявкиПоДефектам.Регистратор,
	|	ВТ_ЗаявкиПоДефектам.IDРемонта,
	|	ВТ_ЗаявкиПоДефектам.ОбъектРемонта,
	|	ВТ_ЗаявкиПоДефектам.Владелец,
	|	ВЫБОР
	|		КОГДА ВТ_ЗаявкиПоДефектам.ВидРемонта = ЗНАЧЕНИЕ(Справочник.торо_ВидыРемонтов.ПустаяСсылка)
	|			ТОГДА ВЫБОР
	|					КОГДА ВТ_РемонтыСрезПоследних.ВидРемонта = ЗНАЧЕНИЕ(Справочник.торо_ВидыРемонтов.ПустаяСсылка)
	|						ТОГДА торо_ВидРемонтаПриВводеНаОснованииВыявленныхДефектов.Значение
	|					ИНАЧЕ ВТ_РемонтыСрезПоследних.ВидРемонта
	|				КОНЕЦ
	|		ИНАЧЕ ВТ_ЗаявкиПоДефектам.ВидРемонта
	|	КОНЕЦ,
	|	ВТ_ЗаявкиПоДефектам.ДатаНачалаРемонтныхРабот,
	|	ВТ_ЗаявкиПоДефектам.ДатаОкончанияРемонтныхРабот,
	|	ВТ_ЗаявкиПоДефектам.Исполнитель,
	|	ВТ_ЗаявкиПоДефектам.ИсточникДанных,
	|	ВТ_ЗаявкиПоДефектам.Бригада,
	|	ВТ_ЗаявкиПоДефектам.Отменен,
	|	ВЫБОР
	|		КОГДА ВТ_ЗаявкиПоДефектам.Исполнитель ЕСТЬ NULL
	|			ТОГДА ЗНАЧЕНИЕ(Перечисление.СпособыСтроительства.Подрядный)
	|		ИНАЧЕ ВЫБОР
	|				КОГДА ТИПЗНАЧЕНИЯ(ВТ_ЗаявкиПоДефектам.Исполнитель) = ТИП(Справочник.Контрагенты)
	|					ТОГДА ЗНАЧЕНИЕ(Перечисление.СпособыСтроительства.Подрядный)
	|				ИНАЧЕ ЗНАЧЕНИЕ(Перечисление.СпособыСтроительства.Хозспособ)
	|			КОНЕЦ
	|	КОНЕЦ
	|ИЗ
	|	ВТ_ЗаявкиПоДефектам КАК ВТ_ЗаявкиПоДефектам
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_РемонтыСрезПоследних КАК ВТ_РемонтыСрезПоследних
	|		ПО ВТ_ЗаявкиПоДефектам.IDРемонта = ВТ_РемонтыСрезПоследних.ID,
	|	Константа.торо_ВидРемонтаПриВводеНаОснованииВыявленныхДефектов КАК торо_ВидРемонтаПриВводеНаОснованииВыявленныхДефектов
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	ВТ_ЗаявкиПоВнешнимОснованиям.Регистратор,
	|	ВТ_ЗаявкиПоВнешнимОснованиям.IDРемонта,
	|	ВТ_ЗаявкиПоВнешнимОснованиям.ОбъектРемонта,
	|	ВТ_ЗаявкиПоВнешнимОснованиям.Владелец,
	|	ВЫБОР
	|		КОГДА ВТ_ЗаявкиПоВнешнимОснованиям.ВидРемонта = ЗНАЧЕНИЕ(Справочник.торо_ВидыРемонтов.ПустаяСсылка)
	|			ТОГДА ВЫБОР
	|					КОГДА ВТ_РемонтыСрезПоследних.ВидРемонта = ЗНАЧЕНИЕ(Справочник.торо_ВидыРемонтов.ПустаяСсылка)
	|						ТОГДА торо_ВидРемонтаПриВводеНаОснованииВнешнихОснований.Значение
	|					ИНАЧЕ ВТ_РемонтыСрезПоследних.ВидРемонта
	|				КОНЕЦ
	|		ИНАЧЕ ВТ_ЗаявкиПоВнешнимОснованиям.ВидРемонта
	|	КОНЕЦ,
	|	ВТ_ЗаявкиПоВнешнимОснованиям.ДатаНачалаРемонтныхРабот,
	|	ВТ_ЗаявкиПоВнешнимОснованиям.ДатаОкончанияРемонтныхРабот,
	|	ВТ_ЗаявкиПоВнешнимОснованиям.Исполнитель,
	|	ВТ_ЗаявкиПоВнешнимОснованиям.ИсточникДанных,
	|	ВТ_ЗаявкиПоВнешнимОснованиям.Бригада,
	|	ВТ_ЗаявкиПоВнешнимОснованиям.Отменен,
	|	ВЫБОР
	|		КОГДА ВТ_ЗаявкиПоВнешнимОснованиям.Исполнитель ЕСТЬ NULL
	|			ТОГДА ЗНАЧЕНИЕ(Перечисление.СпособыСтроительства.Подрядный)
	|		ИНАЧЕ ВЫБОР
	|				КОГДА ТИПЗНАЧЕНИЯ(ВТ_ЗаявкиПоВнешнимОснованиям.Исполнитель) = ТИП(Справочник.Контрагенты)
	|					ТОГДА ЗНАЧЕНИЕ(Перечисление.СпособыСтроительства.Подрядный)
	|				ИНАЧЕ ЗНАЧЕНИЕ(Перечисление.СпособыСтроительства.Хозспособ)
	|			КОНЕЦ
	|	КОНЕЦ
	|ИЗ
	|	ВТ_ЗаявкиПоВнешнимОснованиям КАК ВТ_ЗаявкиПоВнешнимОснованиям
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_РемонтыСрезПоследних КАК ВТ_РемонтыСрезПоследних
	|		ПО ВТ_ЗаявкиПоВнешнимОснованиям.IDРемонта = ВТ_РемонтыСрезПоследних.ID,
	|	Константа.торо_ВидРемонтаПриВводеНаОснованииВнешнихОснований КАК торо_ВидРемонтаПриВводеНаОснованииВнешнихОснований
	|
	|ОБЪЕДИНИТЬ ВСЕ
	|
	|ВЫБРАТЬ
	|	ВТ_ГрафикиРМ.Регистратор,
	|	ВТ_ГрафикиРМ.ID,
	|	ВТ_ГрафикиРМ.СписокОбъектов,
	|	ВТ_ГрафикиРМ.Владелец,
	|	ВТ_ГрафикиРМ.ВидМероприятия,
	|	ВТ_ГрафикиРМ.ДатаНачала,
	|	ВТ_ГрафикиРМ.ДатаОкончания,
	|	ВТ_ГрафикиРМ.Исполнитель,
	|	ВТ_ГрафикиРМ.ИсточникДанных,
	|	ЗНАЧЕНИЕ(Справочник.торо_РемонтныеБригады.ПустаяСсылка),
	|	ВТ_ГрафикиРМ.Отменен,
	|	ВТ_ГрафикиРМ.СпособВыполнения
	|ИЗ
	|	ВТ_ГрафикиРМ КАК ВТ_ГрафикиРМ
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	IDРемонта
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	ВТ_Данные.Регистратор КАК Регистратор,
	|	ВТ_Данные.IDРемонта КАК IDРемонта,
	|	ВТ_Данные.ОбъектРемонта КАК ОбъектРемонта,
	|	ВЫБОР
	|		КОГДА ТИПЗНАЧЕНИЯ(ВТ_Данные.Владелец) = ТИП(Справочник.СтруктураПредприятия)
	|			ТОГДА ВТ_Данные.Владелец
	|		ИНАЧЕ ЗНАЧЕНИЕ(Справочник.СтруктураПредприятия.ПустаяСсылка)
	|	КОНЕЦ КАК Владелец,
	|	ВТ_Данные.ВидРемонта КАК ВидРемонта,
	|	ЕСТЬNULL(ВТ_Остановочные.ДатаНачалаРемонта, ВТ_Данные.ДатаНачала) КАК ДатаНачала,
	|	ЕСТЬNULL(ВТ_Остановочные.ДатаОкончанияРемонта, ВТ_Данные.ДатаОкончания) КАК ДатаОкончания,
	|	ВЫБОР
	|		КОГДА ТИПЗНАЧЕНИЯ(ВТ_Данные.Исполнитель) = ТИП(Справочник.СтруктураПредприятия)
	|			ТОГДА ВТ_Данные.Исполнитель
	|		ИНАЧЕ ЗНАЧЕНИЕ(Справочник.СтруктураПредприятия.ПустаяСсылка)
	|	КОНЕЦ КАК Исполнитель,
	|	ВТ_Данные.ИсточникДанных КАК ИсточникДанных,
	|	ВЫБОР
	|		КОГДА ТИПЗНАЧЕНИЯ(ВТ_Данные.Бригада) = ТИП(Справочник.торо_РемонтныеБригады)
	|			ТОГДА ВТ_Данные.Бригада
	|		ИНАЧЕ ЗНАЧЕНИЕ(Справочник.торо_РемонтныеБригады.ПустаяСсылка)
	|	КОНЕЦ КАК Бригада,
	|	ВТ_Данные.Отменен КАК Отменен,
	|	ВТ_Данные.СпособВыполнения КАК СпособВыполнения
	|ПОМЕСТИТЬ ВТ_ДанныеСОстановочными
	|ИЗ
	|	ВТ_Данные КАК ВТ_Данные
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_Остановочные КАК ВТ_Остановочные
	|		ПО ВТ_Данные.IDРемонта = ВТ_Остановочные.IDЗависимого
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	IDРемонта
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	торо_Ремонты.Регистратор КАК Регистратор,
	|	торо_Ремонты.ID КАК ID,
	|	торо_Ремонты.ДатаНачалаПлан КАК ДатаНачалаПлан,
	|	торо_Ремонты.ДатаОкончанияПлан КАК ДатаОкончанияПлан
	|ПОМЕСТИТЬ ВТ_РемонтыРегистраторы
	|ИЗ
	|	РегистрСведений.торо_Ремонты КАК торо_Ремонты
	|ГДЕ
	|	торо_Ремонты.ID В
	|			(ВЫБРАТЬ
	|				ВТ_ДанныеСОстановочными.IDРемонта КАК IDРемонта
	|			ИЗ
	|				ВТ_ДанныеСОстановочными КАК ВТ_ДанныеСОстановочными)
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ВТ_РемонтыРегистраторы.ID КАК ID,
	|	ВТ_РемонтыРегистраторы.Регистратор КАК Регистратор,
	|	ВТ_РемонтыРегистраторы.ДатаНачалаПлан КАК ДатаНачалаПлан,
	|	ВТ_РемонтыРегистраторы.ДатаОкончанияПлан КАК ДатаОкончанияПлан
	|ПОМЕСТИТЬ ВТ_ЗаявкиПоРемонтам
	|ИЗ
	|	ВТ_РемонтыРегистраторы КАК ВТ_РемонтыРегистраторы
	|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.торо_ОтмененныеЗаявки КАК торо_ОтмененныеЗаявки
	|		ПО ВТ_РемонтыРегистраторы.ID = торо_ОтмененныеЗаявки.ИДРемонта
	|			И ВТ_РемонтыРегистраторы.Регистратор = торо_ОтмененныеЗаявки.Заявка
	|ГДЕ
	|	ТИПЗНАЧЕНИЯ(ВТ_РемонтыРегистраторы.Регистратор) = ТИП(Документ.торо_ЗаявкаНаРемонт)
	|	И торо_ОтмененныеЗаявки.НомерСтроки ЕСТЬ NULL
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	ID
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ВТ_РемонтыРегистраторы.ID КАК ID,
	|	ВТ_РемонтыРегистраторы.Регистратор КАК Регистратор
	|ПОМЕСТИТЬ ВТ_НарядыПоРемонтам
	|ИЗ
	|	ВТ_РемонтыРегистраторы КАК ВТ_РемонтыРегистраторы
	|ГДЕ
	|	(ТИПЗНАЧЕНИЯ(ВТ_РемонтыРегистраторы.Регистратор) = ТИП(Документ.торо_НарядНаВыполнениеРемонтныхРабот)
	|			ИЛИ ТИПЗНАЧЕНИЯ(ВТ_РемонтыРегистраторы.Регистратор) = ТИП(Документ.торо_НарядНаРегламентноеМероприятие))
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	ID
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗРЕШЕННЫЕ
	|	ВТ_РемонтыРегистраторы.ID КАК ID,
	|	ВТ_РемонтыРегистраторы.Регистратор КАК Регистратор
	|ПОМЕСТИТЬ ВТ_АктыПоРемонтам
	|ИЗ
	|	ВТ_РемонтыРегистраторы КАК ВТ_РемонтыРегистраторы
	|ГДЕ
	|	(ТИПЗНАЧЕНИЯ(ВТ_РемонтыРегистраторы.Регистратор) = ТИП(Документ.торо_АктОВыполненииРегламентногоМероприятия)
	|			ИЛИ ТИПЗНАЧЕНИЯ(ВТ_РемонтыРегистраторы.Регистратор) = ТИП(Документ.торо_АктОВыполненииЭтапаРабот))
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	ID
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_ДанныеСОстановочными.Регистратор КАК Регистратор,
	|	ВТ_ДанныеСОстановочными.IDРемонта КАК IDРемонта,
	|	ВТ_ДанныеСОстановочными.ОбъектРемонта КАК ОбъектРемонта,
	|	ВТ_ДанныеСОстановочными.Владелец КАК Владелец,
	|	ВТ_ДанныеСОстановочными.ВидРемонта КАК ВидРемонта,
	|	ВТ_ДанныеСОстановочными.Исполнитель КАК Исполнитель,
	|	ВТ_ДанныеСОстановочными.ИсточникДанных КАК ИсточникДанных,
	|	ВТ_ДанныеСОстановочными.Бригада КАК Бригада,
	|	ВТ_ДанныеСОстановочными.ДатаНачала КАК ДатаНачала,
	|	ВТ_ДанныеСОстановочными.ДатаОкончания КАК ДатаОкончания,
	|	ВТ_ДанныеСОстановочными.Отменен КАК Отменен
	|ПОМЕСТИТЬ ВТ_Итоговая
	|ИЗ
	|	ВТ_ДанныеСОстановочными КАК ВТ_ДанныеСОстановочными
	|ГДЕ
	|	ВТ_ДанныеСОстановочными.ДатаНачала >= &ДатаНачала
	|	И ВТ_ДанныеСОстановочными.ДатаОкончания <= &ДатаОкончания
	|
	|ИНДЕКСИРОВАТЬ ПО
	|	IDРемонта
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ВТ_Итоговая.Регистратор КАК ДокументППР,
	|	ВТ_Итоговая.IDРемонта КАК ID,
	|	ВТ_Итоговая.ОбъектРемонта КАК ОР,
	|	ВТ_Итоговая.Владелец КАК ПодразделениеВладелец,
	|	ВТ_Итоговая.ВидРемонта КАК ВидРемонтныхРабот,
	|	ВТ_Итоговая.Исполнитель КАК ПодразделениеИсполнитель,
	|	ВТ_Итоговая.ИсточникДанных КАК ИсточникДанных,
	|	ВТ_Итоговая.Бригада КАК Бригада,
	|	ВТ_Итоговая.ДатаНачала КАК ДатаНач,
	|	ВТ_Итоговая.ДатаОкончания КАК ДатаКон,
	|	0 КАК НомерОР,
	|	ВТ_Итоговая.Отменен КАК Отменен,
	|	ИСТИНА КАК ДоступенДляРедактирования,
	|	ЛОЖЬ КАК ЕстьДвиженияПоВЗ,
	|	ВТ_Итоговая.ОбъектРемонта.Наименование КАК НаименованиеОР
	|ИЗ
	|	ВТ_Итоговая КАК ВТ_Итоговая
	|
	|УПОРЯДОЧИТЬ ПО
	|	ВЫБОР
	|		КОГДА &ПоОр
	|			ТОГДА ВТ_Итоговая.Владелец.Наименование
	|		ИНАЧЕ ВТ_Итоговая.ДатаНачала
	|	КОНЕЦ,
	|	ВЫБОР
	|		КОГДА &ПоОр
	|			ТОГДА ВТ_Итоговая.ДатаНачала
	|		ИНАЧЕ ВТ_Итоговая.Владелец.Наименование
	|	КОНЕЦ
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ РАЗЛИЧНЫЕ
	|	ВТ_Итоговая.Регистратор КАК Регистратор,
	|	ВТ_Итоговая.IDРемонта КАК ID,
	|	ВТ_Итоговая.ОбъектРемонта КАК ОбъектРемонта,
	|	ВТ_Итоговая.Владелец КАК ПодразделениеВладелец,
	|	ВТ_Итоговая.ВидРемонта КАК ВидРемонта,
	|	ВТ_Итоговая.Исполнитель КАК ПодразделениеИсполнитель,
	|	ВТ_Итоговая.ИсточникДанных КАК ИсточникДанных,
	|	ВТ_Итоговая.Бригада КАК Бригада,
	|	ВТ_Итоговая.ДатаНачала КАК ДатаНачала,
	|	ВТ_Итоговая.ДатаОкончания КАК ДатаОкончания,
	|	ВЫБОР
	|		КОГДА &ПоВидам
	|			ТОГДА ВТ_Итоговая.ВидРемонта
	|		ИНАЧЕ ВТ_Итоговая.ОбъектРемонта
	|	КОНЕЦ КАК ДляВыводаВПервуюКолонку,
	|	ВЫБОР
	|		КОГДА &ПоВидам
	|			ТОГДА ВТ_Итоговая.ВидРемонта.Наименование
	|		ИНАЧЕ ВТ_Итоговая.ОбъектРемонта.Наименование
	|	КОНЕЦ КАК Наименование,
	|	ВЫБОР
	|		КОГДА &ГруппировкаПоВладельцам
	|			ТОГДА ВТ_Итоговая.Владелец
	|		ИНАЧЕ ВТ_Итоговая.Исполнитель
	|	КОНЕЦ КАК Подразделение,
	|	ВЫБОР
	|		КОГДА НЕ ВТ_ЗаявкиПоРемонтам.Регистратор ЕСТЬ NULL
	|			ТОГДА ИСТИНА
	|		ИНАЧЕ ЛОЖЬ
	|	КОНЕЦ КАК Смета,
	|	ВЫБОР
	|		КОГДА НЕ ВТ_НарядыПоРемонтам.Регистратор ЕСТЬ NULL
	|			ТОГДА ИСТИНА
	|		ИНАЧЕ ЛОЖЬ
	|	КОНЕЦ КАК Наряд,
	|	ВЫБОР
	|		КОГДА НЕ ВТ_АктыПоРемонтам.Регистратор ЕСТЬ NULL
	|			ТОГДА ИСТИНА
	|		ИНАЧЕ ЛОЖЬ
	|	КОНЕЦ КАК Акт
	|ИЗ
	|	ВТ_Итоговая КАК ВТ_Итоговая
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_ЗаявкиПоРемонтам КАК ВТ_ЗаявкиПоРемонтам
	|		ПО ВТ_Итоговая.IDРемонта = ВТ_ЗаявкиПоРемонтам.ID
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_НарядыПоРемонтам КАК ВТ_НарядыПоРемонтам
	|		ПО ВТ_Итоговая.IDРемонта = ВТ_НарядыПоРемонтам.ID
	|		ЛЕВОЕ СОЕДИНЕНИЕ ВТ_АктыПоРемонтам КАК ВТ_АктыПоРемонтам
	|		ПО ВТ_Итоговая.IDРемонта = ВТ_АктыПоРемонтам.ID
	|
	|УПОРЯДОЧИТЬ ПО
	|	Подразделение,
	|	ВЫБОР
	|		КОГДА &ПоВидам
	|			ТОГДА ВТ_Итоговая.ВидРемонта.Наименование
	|		ИНАЧЕ ВТ_Итоговая.ОбъектРемонта.Наименование
	|	КОНЕЦ,
	|	ВТ_Итоговая.ДатаНачала
	|ИТОГИ ПО
	|	Подразделение,
	|	Бригада";
	
	МассивПодразделенийВладелец = ПодразделениеВладелец.ВыгрузитьЗначения();
	Запрос.УстановитьПараметр("СписокПодразделенияВладелец", МассивПодразделенийВладелец);
	
	МассивПодразделенийИсполнитель = ПодразделениеИсполнитель.ВыгрузитьЗначения();
	Запрос.УстановитьПараметр("СписокПодразделенияИсполнитель", МассивПодразделенийИсполнитель);
	
	Запрос.УстановитьПараметр("ЕстьОтборПодразделниеВладелец", МассивПодразделенийВладелец.Количество() > 0);
	Запрос.УстановитьПараметр("ЕстьОтборПодразделниеИсполнитель", МассивПодразделенийИсполнитель.Количество() > 0);
	
	МассивБригад = Бригады.ВыгрузитьЗначения();
	Запрос.УстановитьПараметр("СписокБригад", МассивБригад);
	Запрос.УстановитьПараметр("ЕстьОтборПоБригадам", МассивБригад.Количество() > 0);
	
	
	Если ВыводитьПо = "Видам работ" Тогда
		Запрос.УстановитьПараметр("ПоВидам", Истина);
	Иначе
		Запрос.УстановитьПараметр("ПоВидам", Ложь);
	КонецЕсли;
	Если ПоИсполнителям Тогда
		Запрос.УстановитьПараметр("ГруппировкаПоВладельцам", Ложь);
	Иначе	
	    Запрос.УстановитьПараметр("ГруппировкаПоВладельцам", Истина);
	КонецЕсли;
	Если СортироватьПоОР Тогда
		Запрос.УстановитьПараметр("ПоОР", Истина);
	Иначе
		Запрос.УстановитьПараметр("ПоОР", Ложь);
	КонецЕсли;
	
	Запрос.УстановитьПараметр("ДатаНачала",ДатаНач);
	Запрос.УстановитьПараметр("ДатаОкончания",ДатаКон);
	
	Запрос.УстановитьПараметр("ПоПланГрафикамППР",ПоПланГрафикамППР);
	Запрос.УстановитьПараметр("ПоСметамПоДефектам",ПоСметамПоДефектам);
	Запрос.УстановитьПараметр("ПоСметамПоВнешнимОснованиям",ПоСметамПоВнешнимОснованиям);
	Запрос.УстановитьПараметр("ПоГрафикамРегламентныхМероприятий",ПоГрафикамРегламентныхМероприятий);
		
	МассивРезультатов = Запрос.ВыполнитьПакет();
	ТаблицаРемонтов = МассивРезультатов[18].Выгрузить();
	ДеревоРемонтовИзЗапроса = МассивРезультатов[19].Выгрузить(ОбходРезультатаЗапроса.ПоГруппировкамСИерархией);
	СформироватьДеревоНаФорме(ДеревоРемонтовИзЗапроса);	
	
	ПостроитьДиаграммуГантаДляППР(ДиаграммаГантаПодразделения, ТаблицаРемонтов, , ,,, , , ,Истина,СортироватьПоОР);
	
	Если НЕ ЗначениеЗаполнено(мДатаОкончанияПланирования) Тогда
		мДатаОкончанияПланирования = '29991231235959';
		ДатаКон = мДатаОкончанияПланирования;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ДатаНач) Тогда
		ДатаНач = '10000101000000';
	КонецЕсли;	
	
	ДиаграммаГантаПодразделения.УстановитьПолныйИнтервал(ДатаНач, мДатаОкончанияПланирования);
	ДиаграммаГантаПодразделения.ОтображатьЛегенду = Ложь; 
	
КонецПроцедуры

&НаСервере
Процедура СформироватьДеревоНаФорме(ТаблицаРемонтов)
	
	ДеревоСФормы = РеквизитФормыВЗначение("ДеревоРемонтов");	
	ДеревоСФормы.Строки.Очистить();	
	
	// >> [Деснол] [Ушаков А.Д.] [24.10.2019] 
	Если ПоИсполнителям Тогда
		ЗаголовокКолонки = НСтр("es='Unidad ejecutiva / Equipo / ';ru='Подразделение-исполнитель / Бригада / ';en='Executive unit / Team /'");
	Иначе  
		ЗаголовокКолонки = НСтр("es='Unidad de propietario / Equipo / ';ru='Подразделение-владелец / Бригада / ';en='Unit-owner / Crew /'");
	КонецЕсли; 
	Если ВыводитьПо = НСтр("es='Tipos de trabajos';ru='Видам работ';en='Types of work'") Тогда
		Элементы.ДеревоРемонтовПодразделение.Заголовок = ЗаголовокКолонки + НСтр("es='Tipo de reparación';ru='Вид ремонта';en='Type of repair'");
		Элементы.ТаблицаРемонтовОбъектРемонта.Видимость = Истина;
		Элементы.ДеревоРемонтовВидРемонта.Видимость = Ложь;
	Иначе 
		Элементы.ДеревоРемонтовПодразделение.Заголовок = ЗаголовокКолонки + НСтр("es='El objeto de la reparación';ru='Объект ремонта';en='Object of repair'");
		Элементы.ТаблицаРемонтовОбъектРемонта.Видимость = Ложь;
		Элементы.ДеревоРемонтовВидРемонта.Видимость = Истина;
	КонецЕсли; 
	// << [Деснол] [Ушаков А.Д.] [24.10.2019] 
	
	ТекущееПодразделение = "";
	Для Каждого ПервыйУровень Из ТаблицаРемонтов.Строки Цикл
		ТекущаяБригада = "";
		Для каждого ВторойУровень из ПервыйУровень.Строки Цикл
			Для каждого ТретийУровень Из ВторойУровень.Строки Цикл
				Если ТретийУровень.Подразделение <> ТекущееПодразделение Тогда
					СтрокаПодразделения = ДеревоСФормы.Строки.Добавить();
					СтрокаПодразделения.Подразделение = ТретийУровень.Подразделение;
					ТекущееПодразделение = ТретийУровень.Подразделение;
				КонецЕсли;
				Если ПоИсполнителям Тогда
					Если ТретийУровень.Бригада <> ТекущаяБригада Тогда
						СтрокаБригады = СтрокаПодразделения.Строки.Добавить();
						СтрокаБригады.Подразделение = ТретийУровень.Бригада;
						ТекущаяБригада = ТретийУровень.Бригада;
					КонецЕсли;
				Иначе
					СтрокаБригады = СтрокаПодразделения;
				КонецЕсли;
				НоваяСтрока = СтрокаБригады.Строки.Добавить();
				НоваяСтрока.Подразделение = ТретийУровень.ДляВыводаВПервуюКолонку;
				НоваяСтрока.ВидРемонта = ТретийУровень.ВидРемонта;
				НоваяСтрока.ID = ТретийУровень.ID; 
				НоваяСтрока.ДатаНачала  =  ТретийУровень.ДатаНачала;
				НоваяСтрока.ДатаОкончания = ТретийУровень.ДатаОкончания;
				НоваяСтрока.ОбъектРемонта = ТретийУровень.ОбъектРемонта;
				НоваяСтрока.ДокументРегистратор = ТретийУровень.Регистратор;
				НоваяСтрока.Смета = ТретийУровень.Смета;
				НоваяСтрока.Наряд = ТретийУровень.Наряд;
				НоваяСтрока.Акт = ТретийУровень.Акт;
			КонецЦикла; 
		 КонецЦикла;
	КонецЦикла;
	
	ЗначениеВРеквизитФормы(ДеревоСФормы, "ДеревоРемонтов");	
	
	
КонецПроцедуры

// Процедура выполняет построение диагрммы Ганта для графика ППР
//
// Параметры:
//  Диаграмма - диаграмма Ганта.
//  ИсхДанные - таблица значений исходных данных.
//
Процедура ПостроитьДиаграммуГантаДляППР(Диаграмма, ТаблицаДанныхППР, СтруктураИерархии = Неопределено, ОтображатьИерархию = Ложь, ИспользоватьИерархию = Ложь, РезультатЗапроса = Неопределено, ЭтоКорректировка = Ложь, Редактировать = Ложь, ИспользоватьРасшифровку = Истина, ДиаграммаДляОтчета = Ложь, СортироватьПоНаименованию = Истина) 
	
	//////////////////////////////////////////////////////
	ТекДата = ТекущаяДата();
	
	МассивТипов = Новый Массив;
	МассивТипов.Добавить(Тип("Булево"));
	
	ОписаниеТ = новый ОписаниеТипов(МассивТипов);
	
	ЕстьЦепочки = ТаблицаДанныхППР.Колонки.Найти("Цепочка") <> Неопределено;
	
	Если ТаблицаДанныхППР.Колонки.Найти("ЕстьВЗ") = Неопределено Тогда
		ТаблицаДанныхППР.Колонки.Добавить("ЕстьВЗ",ОписаниеТ);
		ТаблицаДанныхППР.ЗаполнитьЗначения(Ложь,"ЕстьВЗ");
	КонецЕсли;
	
	Если ТаблицаДанныхППР.Колонки.Найти("ЕстьДвиженияПоВЗ") = Неопределено Тогда
		ТаблицаДанныхППР.Колонки.Добавить("ЕстьДвиженияПоВЗ",ОписаниеТ);
		ТаблицаДанныхППР.ЗаполнитьЗначения(Ложь,"ЕстьДвиженияПоВЗ");
	КонецЕсли;
	
	Диаграмма.Очистить();
	Диаграмма.АвтоОпределениеПолногоИнтервала = Ложь;
		
	СерияРемонт = Диаграмма.Серии.Добавить();
	СерияРемонт.Текст = "График ремонта";
	
	
	НаименованиеПоляПодразделения = ?(ПоИсполнителям, "ПодразделениеИсполнитель", "ПодразделениеВладелец");
	
	Запрос = Новый Запрос;
	Запрос.Текст = "ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|   ТаблицаДанныхППР.ID,
		|	ТаблицаДанныхППР." + НаименованиеПоляПодразделения + " КАК ОбъектРемонтныхРабот,
		|	ТаблицаДанныхППР.ВидРемонтныхРабот,
		|	ТаблицаДанныхППР.ДокументППР,
		|	ТаблицаДанныхППР.ОР,
		|	ТаблицаДанныхППР.Бригада,
		|	ТаблицаДанныхППР.ДатаНач,
		|	ТаблицаДанныхППР.ДатаКон,
		|	ТаблицаДанныхППР.ЕстьВЗ,
		|	ТаблицаДанныхППР.ЕстьДвиженияПоВЗ,
		|	ТаблицаДанныхППР.ДоступенДляРедактирования,
		|	ТаблицаДанныхППР.ИсточникДанных";
		Если НЕ СортироватьПоНаименованию Тогда
			Запрос.Текст = Запрос.Текст + ", ТаблицаДанныхППР.НомерОР";
		КонецЕсли;
		Если ЕстьЦепочки Тогда
			Запрос.Текст = Запрос.Текст + ", ТаблицаДанныхППР.Цепочка, ТаблицаДанныхППР.НомерСтрокиВРЦепочки";
		КонецЕсли;
		Запрос.Текст = Запрос.Текст + "
		|ПОМЕСТИТЬ ТаблицаДанныхППР
		|ИЗ
		|	&ТаблицаДанныхППР КАК ТаблицаДанныхППР
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|   ТаблицаДанныхППР.ID,
		|	ТаблицаДанныхППР.ОбъектРемонтныхРабот КАК ОбъектРемонтныхРабот,
		|	ТаблицаДанныхППР.ВидРемонтныхРабот КАК ВидРемонтныхРабот,
		|	ТаблицаДанныхППР.ДокументППР,
		|	ТаблицаДанныхППР.ДатаНач КАК ДатаНач,
		|	ТаблицаДанныхППР.ДатаКон КАК ДатаКон,
		|	ТаблицаДанныхППР.ОР,
		|	ПРЕДСТАВЛЕНИЕ(ТаблицаДанныхППР.ОбъектРемонтныхРабот),
		|	ПРЕДСТАВЛЕНИЕ(ТаблицаДанныхППР.ВидРемонтныхРабот),
		|	ТаблицаДанныхППР.Бригада,
		|	ПРЕДСТАВЛЕНИЕ(ТаблицаДанныхППР.Бригада) КАК БригадаПредставление,
		|	ТаблицаДанныхППР.ЕстьВЗ,
		|	ТаблицаДанныхППР.ЕстьДвиженияПоВЗ,
		|	ТаблицаДанныхППР.ДоступенДляРедактирования,
		|	ТаблицаДанныхППР.ИсточникДанных";
		Если НЕ СортироватьПоНаименованию Тогда
			Запрос.Текст = Запрос.Текст + ", ТаблицаДанныхППР.НомерОР";
		КонецЕсли;
		Если ЕстьЦепочки Тогда
			Запрос.Текст = Запрос.Текст + ", ТаблицаДанныхППР.Цепочка, ТаблицаДанныхППР.НомерСтрокиВРЦепочки";
		КонецЕсли;

		Запрос.Текст = Запрос.Текст + "
		|ИЗ
		|	ТаблицаДанныхППР КАК ТаблицаДанныхППР
		|ГДЕ ДатаНач > ДАТАВРЕМЯ(1,1,1,0,0,0) ";
		Если СортироватьПоНаименованию Тогда
			Если ЕстьЦепочки Тогда
             Запрос.Текст = Запрос.Текст + "
				|УПОРЯДОЧИТЬ ПО
				|	ОбъектРемонтныхРабот.Наименование,
				|	Цепочка,
			    |	НомерСтрокиВРЦепочки,
				|	ВидРемонтныхРабот,
				|	ДатаНач,
				|	ДатаКон ";
			Иначе
				Если ВыводитьПо = "Видам работ" Тогда
					Запрос.Текст = Запрос.Текст + "
					|УПОРЯДОЧИТЬ ПО
					|	ОбъектРемонтныхРабот.Наименование,
					|	ВидРемонтныхРабот.Наименование,
					|	ДатаНач,
					|	ДатаКон ";
				Иначе
					Запрос.Текст = Запрос.Текст + "
					|УПОРЯДОЧИТЬ ПО
					|	ОбъектРемонтныхРабот.Наименование,
					|	ОР.Наименование,
					|	ДатаНач,
					|	ДатаКон ";
				КонецЕсли;
			КонецЕсли;
		Иначе
			Если ЕстьЦепочки Тогда
				Запрос.Текст = Запрос.Текст + "
				|УПОРЯДОЧИТЬ ПО
				|	НомерОР,
				|	Цепочка,
			    |	НомерСтрокиВРЦепочки";
			Иначе
				Запрос.Текст = Запрос.Текст + "
				|УПОРЯДОЧИТЬ ПО
				|	НомерОР ";
			КонецЕсли;
		КонецЕсли;
		Запрос.Текст = Запрос.Текст + "
		|ИТОГИ ПО
		|	ОбъектРемонтныхРабот";
		
		Если ПоИсполнителям Тогда
			Запрос.Текст = Запрос.Текст + ", Бригада";
		КонецЕсли;
		
		
		Если ВыводитьПо = "Объектам ремонта" Тогда
			Запрос.Текст = Запрос.Текст + ", ОР";
		Иначе
			Запрос.Текст = Запрос.Текст + ", ВидРемонтныхРабот";
		КонецЕсли;
		
		Запрос.УстановитьПараметр("ТаблицаДанныхППР", ТаблицаДанныхППР);
		
		Результат = Запрос.Выполнить();
		
		ВыборкаПоОР = Результат.Выбрать(ОбходРезультатаЗапроса.ПоГруппировкам);
		Пока ВыборкаПоОР.Следующий() Цикл
			
			ТочкаОРР = Диаграмма.Точки.Добавить();
			ТочкаОРР.Текст = ВыборкаПоОР.ОбъектРемонтныхРаботПредставление;
			Если ИспользоватьРасшифровку Тогда
				ТочкаОРР.Расшифровка = ВыборкаПоОР.ОбъектРемонтныхРабот;
			КонецЕсли;
			
			Если ПоИсполнителям Тогда
				ВыборкаПоБригадам = ВыборкаПоОР.Выбрать(ОбходРезультатаЗапроса.ПоГруппировкам);
				Пока ВыборкаПоБригадам.Следующий() Цикл
					ТочкаБригады = ТочкаОРР.Точки.Добавить();
					ТочкаБригады.Текст = ВыборкаПоБригадам.БригадаПредставление;					
					Если ИспользоватьРасшифровку Тогда
						ТочкаБригады.Расшифровка = ВыборкаПоБригадам.Бригада;
					КонецЕсли; 
					
					ВыборкаПоВРИлиЦепочкам = ВыборкаПоБригадам.Выбрать(ОбходРезультатаЗапроса.ПоГруппировкам);
					Пока ВыборкаПоВРИлиЦепочкам.Следующий() Цикл
						
						Если ЕстьЦепочки Тогда
							ВыборкаПоВР = ВыборкаПоВРИлиЦепочкам.Выбрать(ОбходРезультатаЗапроса.ПоГруппировкам);
							ВыборкаПоВР.Следующий();
						Иначе
							ВыборкаПоВР = ВыборкаПоВРИлиЦепочкам;
						КонецЕсли;
						ТочкаВРР = ТочкаБригады.Точки.Добавить();
						
						Если ВыводитьПо = "Объектам ремонта" Тогда
							ТочкаВРР.Текст = ВыборкаПоВР.ОР;					
							Если ИспользоватьРасшифровку Тогда
								ТочкаВРР.Расшифровка = ВыборкаПоВР.ОР;
							КонецЕсли; 
						Иначе
							ТочкаВРР.Текст = ВыборкаПоВР.ВидРемонтныхРаботПредставление;					
							Если ИспользоватьРасшифровку Тогда
								ТочкаВРР.Расшифровка = ВыборкаПоВР.ВидРемонтныхРабот;
							КонецЕсли; 
						КонецЕсли;
						
						
						Выборка = ВыборкаПоВР.Выбрать();
						Пока Выборка.Следующий() Цикл
							
							ВставитьИнтервал(Диаграмма, Выборка, ВыборкаПоБригадам, ТочкаОРР, ТочкаВРР, СерияРемонт, ТекДата, Редактировать, ИспользоватьРасшифровку);
							
						КонецЦикла;
						
					КонецЦикла;
				КонецЦикла;
			Иначе
			
				ВыборкаПоВРИлиЦепочкам = ВыборкаПоОР.Выбрать(ОбходРезультатаЗапроса.ПоГруппировкам);
				Пока ВыборкаПоВРИлиЦепочкам.Следующий() Цикл
					
					Если ЕстьЦепочки Тогда
						ВыборкаПоВР = ВыборкаПоВРИлиЦепочкам.Выбрать(ОбходРезультатаЗапроса.ПоГруппировкам);
						ВыборкаПоВР.Следующий();
					Иначе
						ВыборкаПоВР = ВыборкаПоВРИлиЦепочкам;
					КонецЕсли;
					ТочкаВРР = ТочкаОРР.Точки.Добавить();
					
					Если ВыводитьПо = "Объектам ремонта" Тогда
						ТочкаВРР.Текст = ВыборкаПоВР.ОР;					
						Если ИспользоватьРасшифровку Тогда
							ТочкаВРР.Расшифровка = ВыборкаПоВР.ОР;
						КонецЕсли; 
					Иначе
						ТочкаВРР.Текст = ВыборкаПоВР.ВидРемонтныхРаботПредставление;					
						Если ИспользоватьРасшифровку Тогда
							ТочкаВРР.Расшифровка = ВыборкаПоВР.ВидРемонтныхРабот;
						КонецЕсли; 
					КонецЕсли;
					
					
					Выборка = ВыборкаПоВР.Выбрать();
					Пока Выборка.Следующий() Цикл
						
						ВставитьИнтервал(Диаграмма, Выборка, ВыборкаПоОР, ТочкаОРР, ТочкаВРР, СерияРемонт, ТекДата, Редактировать, ИспользоватьРасшифровку);
												
					КонецЦикла;
					
				КонецЦикла;				
			КонецЕсли; 
		КонецЦикла;
КонецПроцедуры // ПостроитьДиаграммуГантаДляППР()

&НаСервере
Процедура ВставитьИнтервал(Диаграмма, Выборка, ВыборкаПоОР, ТочкаОРР, ТочкаВРР, СерияРемонт, ТекДата, Редактировать, ИспользоватьРасшифровку)
	
	ЦветВыполненогоППР 						= WebЦвета.Синий;
	ЦветЗапланированногоППР 				= WebЦвета.Васильковый;	
	
	ЦветВыполненогоМероприятия 				= WebЦвета.ТемноЗеленый;
	ЦветЗапланированногоМероприятия			= WebЦвета.ЖелтоЗеленый;	
	
	ЦветВыполненогоСметыПоДефектам 			= WebЦвета.Красный;
	ЦветЗапланированногоСметыПоДефектам 	= WebЦвета.Кирпичный;
	
	ЦветВыполненогоСметыПоПредписаниям		= WebЦвета.Желтый;
	ЦветЗапланированногоСметыПоПредписаниям	= WebЦвета.Оранжевый; 	
	
	ЦветВыполненого				= WebЦвета.Синий;	
	ЦветИзмененного 			= WebЦвета.Томатный;
	ЦветОтмененного 			= WebЦвета.Красный;
	ЦветЗапланированного 		= WebЦвета.Васильковый;
	ЦветЗамещенногоИсходного 	= WebЦвета.СеребристоСерый;
	ЦветИсходного 				= WebЦвета.ГолубойСоСтальнымОттенком;
	ЦветЗамещенного 			= WebЦвета.СветлоСерый;

	
	Значение = Диаграмма.ПолучитьЗначение(ТочкаОРР, СерияРемонт);
	Интервал = Значение.Добавить();
	Интервал.Начало = Выборка.ДатаНач;
	Интервал.Конец = ?(Выборка.ДатаКон > Выборка.ДатаНач, Выборка.ДатаКон, Выборка.ДатаНач + 3600);
	Если Выборка.ИсточникДанных <> "СПВН" И Выборка.ИсточникДанных <> "СПД" Тогда
		Интервал.Конец = КонецДня(Интервал.Конец);
	КонецЕсли;
	
	Если Выборка.ИсточникДанных = "ППР" Тогда
		Интервал.Цвет   = ?(КонецДня(Выборка.ДатаКон) < ТекДата, ЦветВыполненогоППР,  ЦветЗапланированногоППР);	
	ИначеЕсли Выборка.ИсточникДанных = "РМ" Тогда
		Интервал.Цвет   = ?(КонецДня(Выборка.ДатаКон) < ТекДата, ЦветВыполненогоМероприятия,  ЦветЗапланированногоМероприятия);	
	ИначеЕсли Выборка.ИсточникДанных = "СПД" Тогда
		Интервал.Цвет   = ?(Выборка.ДатаКон < ТекДата, ЦветВыполненогоСметыПоДефектам,  ЦветЗапланированногоСметыПоДефектам);
	ИначеЕсли Выборка.ИсточникДанных = "СПВН" Тогда
		Интервал.Цвет   = ?(Выборка.ДатаКон < ТекДата, ЦветВыполненогоСметыПоПредписаниям,  ЦветЗапланированногоСметыПоПредписаниям);
	КонецЕсли;
	Интервал.Текст  = Строка(Интервал.Начало) + " - " + Строка(Интервал.Конец); 					
	
	Значение = Диаграмма.ПолучитьЗначение(ТочкаВРР, СерияРемонт);
	Значение.Редактирование = Редактировать;
	Если ИспользоватьРасшифровку Тогда
		Значение.Расшифровка = Новый Структура;
		Значение.Расшифровка.Вставить("ВидРемонта",Выборка.ВидРемонтныхРабот);											
	КонецЕсли;
	
	Интервал = Значение.Добавить();
	Интервал.Начало = Выборка.ДатаНач;
	Интервал.Конец  = ?(Выборка.ДатаКон > Выборка.ДатаНач, Выборка.ДатаКон, Выборка.ДатаНач + 3600);
	Если Выборка.ИсточникДанных <> "СПВН" И Выборка.ИсточникДанных <> "СПД" Тогда
		Интервал.Конец = КонецДня(Интервал.Конец);
	КонецЕсли;
	
	Если Выборка.ИсточникДанных = "ППР" Тогда
		Интервал.Цвет   = ?(КонецДня(Выборка.ДатаКон) < ТекДата, ЦветВыполненогоППР,  ЦветЗапланированногоППР);	
	ИначеЕсли Выборка.ИсточникДанных = "РМ" Тогда
		Интервал.Цвет   = ?(КонецДня(Выборка.ДатаКон) < ТекДата, ЦветВыполненогоМероприятия,  ЦветЗапланированногоМероприятия);	
	ИначеЕсли Выборка.ИсточникДанных = "СПД" Тогда
		Интервал.Цвет   = ?(Выборка.ДатаКон < ТекДата, ЦветВыполненогоСметыПоДефектам,  ЦветЗапланированногоСметыПоДефектам);
	ИначеЕсли Выборка.ИсточникДанных = "СПВН" Тогда
		Интервал.Цвет   = ?(Выборка.ДатаКон < ТекДата, ЦветВыполненогоСметыПоПредписаниям,  ЦветЗапланированногоСметыПоПредписаниям);
	КонецЕсли;			
	
	Интервал.Текст  = "Объект работ: " + Выборка.ОР + "
	|Вид работ: "+Выборка.ВидРемонтныхРабот +"
	|Дата работ: "+Строка(Интервал.Начало) + " - " + Строка(Интервал.Конец); 			
	
	Если ИспользоватьРасшифровку Тогда
		Интервал.Расшифровка = Новый Структура;
		Если НЕ ВыборкаПоОР.ОбъектРемонтныхРабот = Справочники.СтруктураПредприятия.ПустаяССылка() Тогда
			Интервал.Расшифровка.Вставить("Подразделение",ВыборкаПоОР.ОбъектРемонтныхРабот);
		КонецЕсли;	
		Интервал.Расшифровка.Вставить("Регистратор",Выборка.ДокументППР);
		Интервал.Расшифровка.Вставить("Объект",Выборка.ОР);
		Интервал.Расшифровка.Вставить("ID",Выборка.ID);
	КонецЕсли;
	
	Если Выборка.ЕстьВЗ И Выборка.ЕстьДвиженияПоВЗ Тогда
		Интервал.Текст = Интервал.Текст + Символы.ПС + "Начата работа по внутренним заказам.";
	ИначеЕсли Выборка.ЕстьВЗ И Не Выборка.ЕстьДвиженияПоВЗ Тогда
		Интервал.Текст = Интервал.Текст + Символы.ПС + "Введены внутренние заказы.";
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура УстановитьВидимостьЭлементов()
	
	Если НЕ ПолучитьЗначениеФО("торо_ИспользоватьРегламентныеМероприятия") Тогда
		ПоГрафикамРегламентныхМероприятий = Ложь;
		Элементы.ПоГрафикамРегламентныхМероприятий.Видимость = Ложь;		
	КонецЕсли;
	Если НЕ ПолучитьЗначениеФО("торо_ИспользоватьППР") Тогда
		ПоПланГрафикамППР = Ложь;
		Элементы.ПоПланГрафикамППР.Видимость = Ложь;		
	КонецЕсли;			
	Если НЕ ПолучитьЗначениеФО("торо_ИспользоватьСметыРемонта") Тогда
		ПоСметамПоВнешнимОснованиям = Ложь;
		Элементы.ПоСметамПоВнешнимОснованиям.Видимость = Ложь;
		ПоСметамПоДефектам = Ложь;
		Элементы.ПоСметамПоДефектам.Видимость = Ложь;	
	КонецЕсли;				
	Если НЕ ПолучитьЗначениеФО("торо_ИспользоватьВнешниеОснованияДляРабот") Тогда
		ПоСметамПоВнешнимОснованиям = Ложь;
		Элементы.ПоСметамПоВнешнимОснованиям.Видимость = Ложь;		
	КонецЕсли;					
	Если НЕ ПолучитьЗначениеФО("торо_УчетВыявленныхДефектовОборудования") Тогда
		ПоСметамПоДефектам = Ложь;
		Элементы.ПоСметамПоДефектам.Видимость = Ложь;		
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Функция ПолучитьЗначениеФО(ТекущаяОпция)
	
	Возврат ПолучитьФункциональнуюОпцию(ТекущаяОпция);
	
КонецФункции

&НаКлиенте
Процедура ОтображениеЛегенды(Команда)
	
	Элементы.ОтображениеЛегенды.Пометка = НЕ Элементы.ОтображениеЛегенды.Пометка;
	Элементы.ГруппаЛегенда.Видимость = Элементы.ОтображениеЛегенды.Пометка;

КонецПроцедуры

&НаКлиенте
Процедура СформироватьЗаголовокСвернутойГруппы()
	
	// >> [Деснол] [Ушаков А.Д.] [24.10.2019] 
	ЗаголовокСвернутойГруппы = НСтр("es='Mostrar la información de ';ru='Выводить информацию по ';en='Display information on '");
	
	ПерваяЗапись = Истина;
	Если ПоСметамПоДефектам Тогда
		ЗаголовокСвернутойГруппы = ЗаголовокСвернутойГруппы + НСтр("es='las solicitudes de defectos';ru='заявкам по дефектам';en='defect claims'");
		ПерваяЗапись = Ложь;
	КонецЕсли;
	
	Если ПоСметамПоВнешнимОснованиям Тогда
		Если не ПерваяЗапись тогда
			ЗаголовокСвернутойГруппы = ЗаголовокСвернутойГруппы + ", ";
		КонецЕсли;
		
		ЗаголовокСвернутойГруппы = ЗаголовокСвернутойГруппы + НСтр("es='las solicitudes por motivos externos';ru='заявкам по внешним основаниям';en='applications on external grounds'");
		ПерваяЗапись = Ложь;
	КонецЕсли;
	
	Если ПоПланГрафикамППР Тогда
		Если не ПерваяЗапись тогда
			ЗаголовокСвернутойГруппы = ЗаголовокСвернутойГруппы + ", ";
		КонецЕсли;
		ЗаголовокСвернутойГруппы = ЗаголовокСвернутойГруппы + НСтр("es='el plan-gráficos de la parada';ru='план-графикам ППР';en='plan-schedule PPM'");
		ПерваяЗапись = Ложь;
	КонецЕсли;
	
	Если ПоГрафикамРегламентныхМероприятий Тогда
		Если не ПерваяЗапись тогда
			ЗаголовокСвернутойГруппы = ЗаголовокСвернутойГруппы + ", ";
		КонецЕсли;
		ЗаголовокСвернутойГруппы = ЗаголовокСвернутойГруппы + НСтр("es='los calendarios de actividades de mantenimiento';ru='графикам регламентных мероприятий';en='schedule of routine activities'");
	КонецЕсли;
	// << [Деснол] [Ушаков А.Д.] [24.10.2019] 
	
	//ЗаголовокСвернутойГруппы = ЛЕВ(ЗаголовокСвернутойГруппы, СтрДлина(ЗаголовокСвернутойГруппы) - 2);
	Элементы.ВыводитьИнформациюПо.ЗаголовокСвернутогоОтображения = ЗаголовокСвернутойГруппы;
	
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФормуПодбораПодразделений(Элемент, СписокЗначений)
	
	ПараметрыФормыСписка = Новый Структура("Представление, ОграничиватьВыборУказаннымиЗначениями, ОписаниеТипов, ЗначенияДляВыбора, ОтображатьПометки", "Подразделения", Ложь, Новый ОписаниеТипов("СправочникСсылка.СтруктураПредприятия"), СписокЗначений, Ложь);
	ФормаСпискаЗначений = ОткрытьФорму("ОбщаяФорма.торо_ВводЗначенийСпискомСФлажками", ПараметрыФормыСписка, Элемент, УникальныйИдентификатор, ВариантОткрытияОкна.ОтдельноеОкно,,,РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
	Если СписокЗначений.Количество() = 0 Тогда
		ОткрытьФорму("Справочник.СтруктураПредприятия.Форма.ФормаВыбора", Новый Структура("ЗакрыватьПриВыборе, РежимОткрытияОкна", Ложь, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца) ,ФормаСпискаЗначений.Элементы.Список, ФормаСпискаЗначений.УникальныйИдентификатор, ВариантОткрытияОкна.ОтдельноеОкно);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ДатаКонПриИзменении(Элемент)
	Если ДатаНач > ДатаКон Тогда
		ТекстСообщения = НСтр("es='Se prohíbe introducir la fecha de la terminación menos de fechas del comienzo.';ru='Запрещено вводить дату окончания меньше даты начала.';en='It is prohibited to enter an end date less than the start date.'");
		ОбщегоНазначенияКлиент.СообщитьПользователю(ТекстСообщения);
	КонецЕсли; 
КонецПроцедуры

&НаКлиенте
Процедура ДатаНачПриИзменении(Элемент)
	Если ДатаНач > ДатаКон Тогда
		ТекстСообщения = НСтр("es='Se prohíbe introducir la fecha ha comenzado más de fechas de la terminación.';ru='Запрещено вводить дату начала больше даты окончания.';en='It is prohibited to enter a start date greater than the end date.'");
		ОбщегоНазначенияКлиент.СообщитьПользователю(ТекстСообщения);
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура СвернутьВсе(Команда)
	Для каждого ТочкаДиаграммыГанта Из ДиаграммаГантаПодразделения.Точки Цикл
		ДиаграммаГантаПодразделения.СвернутьТочку(ТочкаДиаграммыГанта, Истина);
	КонецЦикла;
КонецПроцедуры

&НаКлиенте
Процедура РазвернутьВсе(Команда)
	Для каждого ТочкаДиаграммыГанта Из ДиаграммаГантаПодразделения.Точки Цикл
		ДиаграммаГантаПодразделения.РазвернутьТочку(ТочкаДиаграммыГанта, Истина);
	КонецЦикла;
КонецПроцедуры

&НаКлиенте
Процедура СвернутьВсеТаблица(Команда)
	ЭлементыДереваПодразделения = ДеревоРемонтов.ПолучитьЭлементы();
	Для Каждого СтрокаПодразделения Из ЭлементыДереваПодразделения Цикл
		Элементы.ДеревоРемонтов.Свернуть(СтрокаПодразделения.ПолучитьИдентификатор());
	КонецЦикла;
КонецПроцедуры

&НаКлиенте
Процедура РазвернутьВсеТаблица(Команда)
	ЭлементыДереваПодразделения = ДеревоРемонтов.ПолучитьЭлементы();
	Для Каждого СтрокаПодразделения Из ЭлементыДереваПодразделения Цикл
		Элементы.ДеревоРемонтов.Развернуть(СтрокаПодразделения.ПолучитьИдентификатор(), Истина);
	КонецЦикла;
КонецПроцедуры

&НаКлиенте
Процедура Группа2ПриСменеСтраницы(Элемент, ТекущаяСтраница)
	Если ТекущаяСтраница.Имя = "Группа4" Тогда   // это таблица ремонтов
		Элементы.СвернутьВсеТаблица.Видимость = Истина;
	    Элементы.РазвернутьВсеТаблица.Видимость = Истина;
		Элементы.СвернутьВсе.Видимость = Ложь;
		Элементы.РазвернутьВсе.Видимость = Ложь;
		Элементы.ОтображениеЛегенды.Видимость = Ложь;
		Элементы.ПолосаРегулированияДиаграммыГанта.Видимость = Ложь;	
		Элементы.СортироватьПоОР.Видимость = Ложь;	
	Иначе
		Элементы.СвернутьВсеТаблица.Видимость = Ложь;
	    Элементы.РазвернутьВсеТаблица.Видимость = Ложь;
		Элементы.СвернутьВсе.Видимость = Истина;
		Элементы.РазвернутьВсе.Видимость = Истина;
		Элементы.ОтображениеЛегенды.Видимость = Истина;
		Элементы.ПолосаРегулированияДиаграммыГанта.Видимость = Истина;
		Элементы.СортироватьПоОР.Видимость = Истина;	
	КонецЕсли; 
КонецПроцедуры

&НаКлиенте
Процедура БригадыНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	СтандартнаяОбработка = Ложь;
	ОткрытьФормуПодбораБригад(Элемент, Бригады);
КонецПроцедуры

&НаКлиенте
Процедура ОткрытьФормуПодбораБригад(Элемент, СписокЗначений)
	
	ПараметрыФормыСписка = Новый Структура("Представление, ОграничиватьВыборУказаннымиЗначениями, ОписаниеТипов, ЗначенияДляВыбора, ОтображатьПометки", 
		"Бригады", Ложь, Новый ОписаниеТипов("СправочникСсылка.торо_РемонтныеБригады"), СписокЗначений, Ложь);
	
	Если ПодразделениеИсполнитель.Количество() > 0 Тогда
		ПараметрыФормыСписка.Вставить("ОтборВладелец", ПодразделениеИсполнитель);
	КонецЕсли; 
	
	ФормаСпискаЗначений = ОткрытьФорму("ОбщаяФорма.торо_ВводЗначенийСпискомСФлажками", ПараметрыФормыСписка, Элемент, УникальныйИдентификатор, ВариантОткрытияОкна.ОтдельноеОкно, , , РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
	
	Если СписокЗначений.Количество() = 0 Тогда		
		Если ПодразделениеИсполнитель.Количество() > 0 Тогда
			СтруктураОтбора = Новый Структура("Подразделение", ПодразделениеИсполнитель.ВыгрузитьЗначения());
			СтруктураПараметров = Новый Структура("ЗакрыватьПриВыборе, Отбор, РежимОткрытияОкна", Ложь, СтруктураОтбора, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
		Иначе
			СтруктураПараметров = Новый Структура("ЗакрыватьПриВыборе, РежимОткрытияОкна", Ложь, РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);
		КонецЕсли; 
		ОткрытьФорму("Справочник.торо_РемонтныеБригады.Форма.ФормаВыбора", СтруктураПараметров ,ФормаСпискаЗначений.Элементы.Список, ФормаСпискаЗначений.УникальныйИдентификатор, ВариантОткрытияОкна.ОтдельноеОкно);
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ПодразделениеИсполнительОбработкаВыбора(Элемент, ВыбранноеЗначение, СтандартнаяОбработка)
	Если Бригады.Количество() Тогда
		Бригады.Очистить();
	КонецЕсли; 
КонецПроцедуры


#КонецОбласти