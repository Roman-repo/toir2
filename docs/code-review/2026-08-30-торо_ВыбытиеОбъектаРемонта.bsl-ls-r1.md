# Отчёт код-ревью — BSL LS (`bsl_ls_analyze.py`)

- Дата: 2026-08-30 20:02
- Слой: bsl-language-server (AST), файлов разобрано: 2627
- Вход: /Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыбытиеОбъектаРемонта/ObjectModule.bsl
- Замечания: **13** — 🔴 1, 🟡 4, 🟢 8
- **Вердикт: 🔴 есть — возврат на этап «Разработка», правка, повторный прогон (следующий отчёт r2).**

## 🔴 Блокирующие (исправить до повторного прогона)

### 1. `DataExchangeLoad` — Наличие обязательной конструкции "Если ОбменДанными.Загрузка Тогда ..."

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыбытиеОбъектаРемонта/ObjectModule.bsl:36` · диагностика BSL LS: `DataExchangeLoading` · [карточка](https://docs.checkbsl.org/checks/overall/DataExchangeLoad/)

```bsl
  34 | КонецПроцедуры
  35 | 
  36 | Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)  ← замечание
  37 | 
  38 | 	Если РежимЗаписи = РежимЗаписиДокумента.Проведение Тогда
```

**Что не так:** Без проверки ОбменДанными.Загрузка обработчики записи при обмене/миграции повторно запускают бизнес-логику: проверки, побочные записи, пересчёты итогов (№459).

**Как правильно:**

```bsl
Процедура ПередЗаписью(Отказ)
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	// ... бизнес-логика
КонецПроцедуры
```


## 🟡 Важные (править или обосновать в 05a)

### 2. `ExportVariable` — Использование экспортной переменной

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыбытиеОбъектаРемонта/ObjectModule.bsl:6` · диагностика BSL LS: `ExportVariables` · [карточка](https://docs.checkbsl.org/checks/overall/ExportVariable/)

```bsl
   4 | // СЛУЖЕБНЫЕ ПЕРЕМЕННЫЕ
   5 | 
   6 | Перем БезусловнаяЗапись Экспорт; // Отключает проверки при записи документа  ← замечание
   7 | Перем СтруктураДанных Экспорт;  // Структура, хранящая данные для работы с уведомлениями.
   8 | 
```

**Что не так:** Экспортная переменная — скрытое глобальное состояние: модули сцепляются через неё и ломаются при параллельных вызовах и в фоновых заданиях; замените методами.

**Как правильно:**

```bsl
// вместо «Перем КэшЦен Экспорт» — повторное использование:
Функция КэшЦен() Экспорт
	Возврат ОбщегоНазначения.ЗначениеИзКэшаСеанса("КэшЦен");
КонецФункции
```

### 3. `ExportVariable` — Использование экспортной переменной

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыбытиеОбъектаРемонта/ObjectModule.bsl:7` · диагностика BSL LS: `ExportVariables` · [карточка](https://docs.checkbsl.org/checks/overall/ExportVariable/)

```bsl
   5 | 
   6 | Перем БезусловнаяЗапись Экспорт; // Отключает проверки при записи документа
   7 | Перем СтруктураДанных Экспорт;  // Структура, хранящая данные для работы с уведомлениями.  ← замечание
   8 | 
   9 | #Область ОбработчикиСобытий
```

**Что не так:** Экспортная переменная — скрытое глобальное состояние: модули сцепляются через неё и ломаются при параллельных вызовах и в фоновых заданиях; замените методами.

**Как правильно:**

```bsl
// вместо «Перем КэшЦен Экспорт» — повторное использование:
Функция КэшЦен() Экспорт
	Возврат ОбщегоНазначения.ЗначениеИзКэшаСеанса("КэшЦен");
КонецФункции
```

### 4. `SubqueryJoin` — Запросы, выполняющие соединение с вложенными запросами или виртуальными таблицами

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыбытиеОбъектаРемонта/ObjectModule.bsl:57` · диагностика BSL LS: `JoinWithVirtualTable` · [карточка](https://docs.checkbsl.org/checks/query/SubqueryJoin/)

```bsl
  55 | 		|ИЗ
  56 | 		|	ВТ_ОР КАК ВТ_ОР
  57 | 		|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.торо_СтатусыОбъектовРемонтаВУчете.СрезПоследних(, Регистратор <> &Ссылка  ← замечание
  58 | 		|		ПО ВТ_ОР.ОбъектРемонта = торо_СтатусыОбъектовРемонтаВУчетеСрезПоследних.ОбъектРемонта
  59 | 		|ГДЕ
```

**Что не так:** Соединение напрямую с вложенными запросами и виртуальными таблицами лишает СУБД статистики и порождает медленные планы (№655): сначала материализуйте подзапрос во временную таблицу.

**Как правильно:**

```bsl
Запрос.Текст =
"ВЫБРАТЬ Регистратор, Сумма ПОМЕСТИТЬ ВТДвижения
|ИЗ РегистрНакопления.Продажи
|ГДЕ Организация = &Организация;
|
|ВЫБРАТЬ Р.Ссылка, Д.Сумма ИЗ Документ.Реализация КАК Р
|ЛЕВОЕ СОЕДИНЕНИЕ ВТДвижения КАК Д
|	ПО Д.Регистратор = Р.Ссылка";
```

### 5. `UsingLogicalOrInWhere` — Использование логического "ИЛИ" в секции "ГДЕ" запроса (№658)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыбытиеОбъектаРемонта/ObjectModule.bsl:62` · диагностика BSL LS: `LogicalOrInTheWhereSectionOfQuery` · [карточка](https://docs.checkbsl.org/checks/query/UsingLogicalOrInWhere/)

```bsl
  60 | 		|	(торо_СтатусыОбъектовРемонтаВУчетеСрезПоследних.СтатусОР = ЗНАЧЕНИЕ(Перечисление.торо_СтатусыОРВУчете.Прин
  61 | 		|				И НЕ ВТ_ОР.СнятьСУчета
  62 | 		|			ИЛИ ЕСТЬNULL(торо_СтатусыОбъектовРемонтаВУчетеСрезПоследних.СтатусОР, 0) <> ЗНАЧЕНИЕ(Перечисление.торо_С  ← замечание
  63 | 		|				И ВТ_ОР.СнятьСУчета)";
  64 | 
```

**Что не так:** ИЛИ в секции ГДЕ по разыменованным полям лишает СУБД индексного доступа (№658): переформулируйте через В().

**Как правильно:**

```bsl
ГДЕ Документ.Организация В (&МассивОрганизаций)
// вместо: ГДЕ Документ.Организация = &Организация1
//          ИЛИ Документ.Организация = &Организация2
```


## 🟢 Рекомендации (на усмотрение автора)

### 6. `CodeOutsideRegion` — Код должен быть расположен внутри области (№455,680)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыбытиеОбъектаРемонта/ObjectModule.bsl:6` · диагностика BSL LS: `CodeOutOfRegion` · [карточка](https://docs.checkbsl.org/checks/overall/CodeOutsideRegion/)

```bsl
   4 | // СЛУЖЕБНЫЕ ПЕРЕМЕННЫЕ
   5 | 
   6 | Перем БезусловнаяЗапись Экспорт; // Отключает проверки при записи документа  ← замечание
   7 | Перем СтруктураДанных Экспорт;  // Структура, хранящая данные для работы с уведомлениями.
   8 | 
```

**Что не так:** Код вне областей ломает структуру модуля и навигацию по нему; размещайте по стандартным областям (№455/№680).

**Как правильно:**

```bsl
#Область СлужебныеПроцедурыИФункции

Процедура ЗаполнитьСтрокуЗакрытия(Строка)
	// ...
КонецПроцедуры

#КонецОбласти
```

### 7. `CodeOutsideRegion` — Код должен быть расположен внутри области (№455,680)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыбытиеОбъектаРемонта/ObjectModule.bsl:7` · диагностика BSL LS: `CodeOutOfRegion` · [карточка](https://docs.checkbsl.org/checks/overall/CodeOutsideRegion/)

```bsl
   5 | 
   6 | Перем БезусловнаяЗапись Экспорт; // Отключает проверки при записи документа
   7 | Перем СтруктураДанных Экспорт;  // Структура, хранящая данные для работы с уведомлениями.  ← замечание
   8 | 
   9 | #Область ОбработчикиСобытий
```

**Что не так:** Код вне областей ломает структуру модуля и навигацию по нему; размещайте по стандартным областям (№455/№680).

**Как правильно:**

```bsl
#Область СлужебныеПроцедурыИФункции

Процедура ЗаполнитьСтрокуЗакрытия(Строка)
	// ...
КонецПроцедуры

#КонецОбласти
```

### 8. `LineLength` — Ограничение на длину строки (№444)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыбытиеОбъектаРемонта/ObjectModule.bsl:74` · диагностика BSL LS: `LineLength` · [карточка](https://docs.checkbsl.org/checks/overall/LineLength/)

```bsl
  72 | 			Если СтрПодчиненныйОР.СнятьСУчета Тогда
  73 | 				СтрПодчиненныйОР.СнятьСУчета = Ложь;
  74 | 				ШаблонСообщения = НСтр("es='El objeto subordinado de la reparación %1 no es aceptado al recuento (¡es dado  ← замечание
  75 | 				ТекстСообщения = СтрШаблон(ШаблонСообщения, СтрДляКорректировки.ОбъектРемонта);
  76 | 				ОбщегоНазначения.СообщитьПользователю(ТекстСообщения);
```

**Что не так:** Строка длиннее 120 символов (№444): горизонтальная прокрутка прячет конец выражения — самое важное.

**Как правильно:**

```bsl
ТекстСообщения = НСтр("ru = 'Ни для одной из выбранных строк'
	+ ' не указан новый период отправки'");
```

### 9. `LineLength` — Ограничение на длину строки (№444)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыбытиеОбъектаРемонта/ObjectModule.bsl:78` · диагностика BSL LS: `LineLength` · [карточка](https://docs.checkbsl.org/checks/overall/LineLength/)

```bsl
  76 | 				ОбщегоНазначения.СообщитьПользователю(ТекстСообщения);
  77 | 			Иначе
  78 | 				ШаблонСообщения = НСтр("es='¡El objeto subordinado de la reparación %1 es aceptado al recuento! Para la fo  ← замечание
  79 | 				ТекстСообщения = СтрШаблон(ШаблонСообщения, СтрДляКорректировки.ОбъектРемонта, ОбъектРемонта);
  80 | 				ОбщегоНазначения.СообщитьПользователю(ТекстСообщения,,,,Отказ);
```

**Что не так:** Строка длиннее 120 символов (№444): горизонтальная прокрутка прячет конец выражения — самое важное.

**Как правильно:**

```bsl
ТекстСообщения = НСтр("ru = 'Ни для одной из выбранных строк'
	+ ' не указан новый период отправки'");
```

### 10. `WriteLogEventWrongUsage` — Нерекомендуемое использование метода "ЗаписьЖурналаРегистрации"

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыбытиеОбъектаРемонта/ObjectModule.bsl:146` · диагностика BSL LS: `UsageWriteLogEvent` · [карточка](https://docs.checkbsl.org/checks/overall/WriteLogEventWrongUsage/)

```bsl
 144 | 
 145 | 	Исключение
 146 | 		ЗаписьЖурналаРегистрации("Исключение",УровеньЖурналаРегистрации.Ошибка,,Ссылка,ОписаниеОшибки());  ← замечание
 147 | 		ТекстСообщения = НСтр("es='La anotación no es cumplida.';ru='Запись не выполнена.';en='Writing failed.'");
 148 | 		ОбщегоНазначения.СообщитьПользователю(ТекстСообщения,,,,Отказ);
```

**Что не так:** В тексте комментария нет вызова "ПодробноеПредставлениеОшибки(ИнформацияОбОшибке())"

**Как правильно:** пример — по карточке правила (https://docs.checkbsl.org/checks/overall/WriteLogEventWrongUsage/).

### 11. `WriteLogEventWrongUsage` — Нерекомендуемое использование метода "ЗаписьЖурналаРегистрации"

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыбытиеОбъектаРемонта/ObjectModule.bsl:179` · диагностика BSL LS: `UsageWriteLogEvent` · [карточка](https://docs.checkbsl.org/checks/overall/WriteLogEventWrongUsage/)

```bsl
 177 | 
 178 | 	Исключение
 179 | 		ЗаписьЖурналаРегистрации("Исключение",УровеньЖурналаРегистрации.Ошибка,,Ссылка,ОписаниеОшибки());  ← замечание
 180 | 		ТекстСообщения = НСтр("es='La anotación no es cumplida.';ru='Запись не выполнена.';en='Writing failed.'");
 181 | 		ОбщегоНазначения.СообщитьПользователю(ТекстСообщения,,,,Отказ);
```

**Что не так:** В тексте комментария нет вызова "ПодробноеПредставлениеОшибки(ИнформацияОбОшибке())"

**Как правильно:** пример — по карточке правила (https://docs.checkbsl.org/checks/overall/WriteLogEventWrongUsage/).

### 12. `LineLength` — Ограничение на длину строки (№444)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыбытиеОбъектаРемонта/ObjectModule.bsl:213` · диагностика BSL LS: `LineLength` · [карточка](https://docs.checkbsl.org/checks/overall/LineLength/)

```bsl
 211 | 	Если Не Результат.Пустой() Тогда
 212 | 		// Проверка на наличие документов после указанный даты ввода в эксплуатацию.
 213 | 		ШаблонСообщения = НСтр("es='Para el objeto de la reparación <%1> hay unos documentos a la fecha misma o más   ← замечание
 214 | 		ТекстСообщения = СтрШаблон(ШаблонСообщения,	ОбъектРемонта);
 215 | 		ОбщегоНазначения.СообщитьПользователю(ТекстСообщения,,,,Отказ);
```

**Что не так:** Строка длиннее 120 символов (№444): горизонтальная прокрутка прячет конец выражения — самое важное.

**Как правильно:**

```bsl
ТекстСообщения = НСтр("ru = 'Ни для одной из выбранных строк'
	+ ' не указан новый период отправки'");
```

### 13. `LineLength` — Ограничение на длину строки (№444)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыбытиеОбъектаРемонта/ObjectModule.bsl:242` · диагностика BSL LS: `LineLength` · [карточка](https://docs.checkbsl.org/checks/overall/LineLength/)

```bsl
 240 | 
 241 | 	Если НЕ ПринятКУчету Тогда
 242 | 		ШаблонСообщения = НСтр("es='Del objeto de la reparación <%1> no es aceptado al recuento a la fecha de %2';ru  ← замечание
 243 | 		ТекстСообщения = СтрШаблон(ШаблонСообщения, ОбъектРемонта, ДатаСписания);
 244 | 		ОбщегоНазначения.СообщитьПользователю(ТекстСообщения,,,,Отказ);
```

**Что не так:** Строка длиннее 120 символов (№444): горизонтальная прокрутка прячет конец выражения — самое важное.

**Как правильно:**

```bsl
ТекстСообщения = НСтр("ru = 'Ни для одной из выбранных строк'
	+ ' не указан новый период отправки'");
```

---

Карточки: docs.checkbsl.org/checks/<section>/<Ключ>/; ключи без каталога — 1c-syntax.github.io/bsl-language-server/diagnostics/<Код>/.
Срабатывания — кандидаты в findings 05a, решение за Ревьюером.
