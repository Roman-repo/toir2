# Отчёт код-ревью — BSL LS (`bsl_ls_analyze.py`)

- Дата: 2026-08-31 10:46
- Слой: bsl-language-server (AST), файлов разобрано: 2627 + слой 1 (`checkbsl_scan.py`, слито с дедупликацией)
- Покрытие: slim — отключено 53 диагностик вне каталога∪ALIAS (--slim-config)
- Подавлено: 3 находок (--suppress, решения Ревьюера «не баг» — причины в конце отчёта)
- Вход: /Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl
- Замечания: **168** — 🔴 1, 🟡 137, 🟢 30
- **Вердикт: 🔴 есть — возврат на этап «Разработка», правка, повторный прогон (следующий отчёт r3).**

## 🔴 Блокирующие (исправить до повторного прогона)

### 1. `DataExchangeLoad` — Наличие обязательной конструкции "Если ОбменДанными.Загрузка Тогда ..."

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:17` · диагностика BSL LS: `DataExchangeLoading` · [карточка](https://docs.checkbsl.org/checks/overall/DataExchangeLoad/)

```bsl
  15 | КонецПроцедуры
  16 | 
  17 | Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)  ← замечание
  18 | 
  19 | 	Если РежимЗаписи = РежимЗаписиДокумента.Проведение И Не ЭтоНовый() Тогда
```

**Что не так:** Без проверки ОбменДанными.Загрузка обработчики записи при обмене/миграции повторно запускают бизнес-логику: проверки, побочные записи, пересчёты итогов (№459).

**Как правильно:**

```bsl
Процедура ПередЗаписью(Отказ)
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	// ... бизнес-логика
КонецПроцедуры
```


## 🟡 Важные (править или обосновать в 05a)

### 2. `ExportVariable` — Использование экспортной переменной

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:6` · диагностика BSL LS: `ExportVariables` · [карточка](https://docs.checkbsl.org/checks/overall/ExportVariable/)

```bsl
   4 | // СЛУЖЕБНЫЕ ПЕРЕМЕННЫЕ
   5 | 
   6 | перем СтруктураДанных Экспорт; // Содержит структуру данных о статусе документа (Начальный, СогласованиеЗаверш  ← замечание
   7 | 										// из регистра сведений торо_МатрицаПереходаСтатусовДокументов.
   8 | Перем БезусловнаяЗапись Экспорт; // Отключает проверки при записи документа
```

**Что не так:** Экспортная переменная — скрытое глобальное состояние: модули сцепляются через неё и ломаются при параллельных вызовах и в фоновых заданиях; замените методами.

**Как правильно:**

```bsl
// вместо «Перем КэшЦен Экспорт» — повторное использование:
Функция КэшЦен() Экспорт
	Возврат ОбщегоНазначения.ЗначениеИзКэшаСеанса("КэшЦен");
КонецФункции
```

### 3. `ExportVariable` — Использование экспортной переменной

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:8` · диагностика BSL LS: `ExportVariables` · [карточка](https://docs.checkbsl.org/checks/overall/ExportVariable/)

```bsl
   6 | перем СтруктураДанных Экспорт; // Содержит структуру данных о статусе документа (Начальный, СогласованиеЗаверш
   7 | 										// из регистра сведений торо_МатрицаПереходаСтатусовДокументов.
   8 | Перем БезусловнаяЗапись Экспорт; // Отключает проверки при записи документа  ← замечание
   9 | 
  10 | #Область ОбработчикиСобытий
```

**Что не так:** Экспортная переменная — скрытое глобальное состояние: модули сцепляются через неё и ломаются при параллельных вызовах и в фоновых заданиях; замените методами.

**Как правильно:**

```bsl
// вместо «Перем КэшЦен Экспорт» — повторное использование:
Функция КэшЦен() Экспорт
	Возврат ОбщегоНазначения.ЗначениеИзКэшаСеанса("КэшЦен");
КонецФункции
```

### 4. `UsingLogicalOrInWhere` — Использование логического "ИЛИ" в секции "ГДЕ" запроса (№658)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:278` · диагностика BSL LS: `LogicalOrInTheWhereSectionOfQuery` · [карточка](https://docs.checkbsl.org/checks/query/UsingLogicalOrInWhere/)

```bsl
 276 | 	|	торо_ЗаявкаНаРемонтИсполнителиРемонтныхРабот.Ссылка = &Ссылка
 277 | 	|	И (торо_ЗаявкаНаРемонтИсполнителиРемонтныхРабот.Исполнитель ССЫЛКА Справочник.СтруктураПредприятия
 278 | 	|			ИЛИ торо_ЗаявкаНаРемонтИсполнителиРемонтныхРабот.Исполнитель ССЫЛКА Справочник.Контрагенты)  ← замечание
 279 | 	|
 280 | 	|ИНДЕКСИРОВАТЬ ПО
```

**Что не так:** ИЛИ в секции ГДЕ по разыменованным полям лишает СУБД индексного доступа (№658): переформулируйте через В().

**Как правильно:**

```bsl
ГДЕ Документ.Организация В (&МассивОрганизаций)
// вместо: ГДЕ Документ.Организация = &Организация1
//          ИЛИ Документ.Организация = &Организация2
```

### 5. `DocumentationParameters` — Наличие секции параметров в описании экспортной процедуры (функции)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:354` · диагностика BSL LS: `MissingParameterDescription` · [карточка](https://docs.checkbsl.org/checks/overall/DocumentationParameters/)

```bsl
 352 | //  Заголовок               - строка, заголовок сообщения об ошибке проведения.
 353 | //
 354 | Процедура ПроверитьЗаполнениеТабличнойЧастиРемонтыОборудования(Отказ)  ← замечание
 355 | 
 356 | 	// Укажем, что надо проверить:
```

**Что не так:** Без секции «Параметры:» описание метода не читается средствами EDT и не попадает в контекстную подсказку (№455).

**Как правильно:**

```bsl
// Параметры:
//   ОтборРемонтов - Структура - отбор по периоду и подразделению.
//
Процедура ЗаполнитьНаСервере(Знач ОтборРемонтов) Экспорт
```

### 6. `DocumentationParameters` — Наличие секции параметров в описании экспортной процедуры (функции)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:370` · диагностика BSL LS: `MissingParameterDescription` · [карточка](https://docs.checkbsl.org/checks/overall/DocumentationParameters/)

```bsl
 368 | //  Заголовок               - строка, заголовок сообщения об ошибке проведения.
 369 | //
 370 | Процедура ПроверитьЗаполнениеТабличнойЧастиДокументыОснования(Отказ)  ← замечание
 371 | 
 372 | 	// Укажем, что надо проверить:
```

**Что не так:** Без секции «Параметры:» описание метода не читается средствами EDT и не попадает в контекстную подсказку (№455).

**Как правильно:**

```bsl
// Параметры:
//   ОтборРемонтов - Структура - отбор по периоду и подразделению.
//
Процедура ЗаполнитьНаСервере(Знач ОтборРемонтов) Экспорт
```

### 7. `DocumentationParameters` — Наличие секции параметров в описании экспортной процедуры (функции)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:386` · диагностика BSL LS: `MissingParameterDescription` · [карточка](https://docs.checkbsl.org/checks/overall/DocumentationParameters/)

```bsl
 384 | //  Заголовок               - строка, заголовок сообщения об ошибке проведения.
 385 | //
 386 | Процедура ПроверитьЗаполнениеТабличнойЧастиРемонтныеРаботы(Отказ)  ← замечание
 387 | 
 388 | 	// Укажем, что надо проверить:
```

**Что не так:** Без секции «Параметры:» описание метода не читается средствами EDT и не попадает в контекстную подсказку (№455).

**Как правильно:**

```bsl
// Параметры:
//   ОтборРемонтов - Структура - отбор по периоду и подразделению.
//
Процедура ЗаполнитьНаСервере(Знач ОтборРемонтов) Экспорт
```

### 8. `DocumentationParameters` — Наличие секции параметров в описании экспортной процедуры (функции)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:402` · диагностика BSL LS: `MissingParameterDescription` · [карточка](https://docs.checkbsl.org/checks/overall/DocumentationParameters/)

```bsl
 400 | //  Заголовок               - строка, заголовок сообщения об ошибке проведения.
 401 | //
 402 | Процедура ПроверитьЗаполнениеТабличнойЧастиМатериальныеЗатраты(Отказ)  ← замечание
 403 | 
 404 | 	// Укажем, что надо проверить:
```

**Что не так:** Без секции «Параметры:» описание метода не читается средствами EDT и не попадает в контекстную подсказку (№455).

**Как правильно:**

```bsl
// Параметры:
//   ОтборРемонтов - Структура - отбор по периоду и подразделению.
//
Процедура ЗаполнитьНаСервере(Знач ОтборРемонтов) Экспорт
```

### 9. `DocumentationParameters` — Наличие секции параметров в описании экспортной процедуры (функции)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:418` · диагностика BSL LS: `MissingParameterDescription` · [карточка](https://docs.checkbsl.org/checks/overall/DocumentationParameters/)

```bsl
 416 | //  Заголовок               - строка, заголовок сообщения об ошибке проведения.
 417 | //
 418 | Процедура ПроверитьЗаполнениеТабличнойЧастиИнструментыИТехника(Отказ)  ← замечание
 419 | 
 420 | 	// Укажем, что надо проверить:
```

**Что не так:** Без секции «Параметры:» описание метода не читается средствами EDT и не попадает в контекстную подсказку (№455).

**Как правильно:**

```bsl
// Параметры:
//   ОтборРемонтов - Структура - отбор по периоду и подразделению.
//
Процедура ЗаполнитьНаСервере(Знач ОтборРемонтов) Экспорт
```

### 10. `DocumentationParameters` — Наличие секции параметров в описании экспортной процедуры (функции)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:434` · диагностика BSL LS: `MissingParameterDescription` · [карточка](https://docs.checkbsl.org/checks/overall/DocumentationParameters/)

```bsl
 432 | //  Заголовок               - строка, заголовок сообщения об ошибке проведения.
 433 | //
 434 | Процедура ПроверитьЗаполнениеТабличнойЧастиЗапчасти(Отказ)  ← замечание
 435 | 
 436 | 	// Укажем, что надо проверить:
```

**Что не так:** Без секции «Параметры:» описание метода не читается средствами EDT и не попадает в контекстную подсказку (№455).

**Как правильно:**

```bsl
// Параметры:
//   ОтборРемонтов - Структура - отбор по периоду и подразделению.
//
Процедура ЗаполнитьНаСервере(Знач ОтборРемонтов) Экспорт
```

### 11. `DocumentationParameters` — Наличие секции параметров в описании экспортной процедуры (функции)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:450` · диагностика BSL LS: `MissingParameterDescription` · [карточка](https://docs.checkbsl.org/checks/overall/DocumentationParameters/)

```bsl
 448 | //  Заголовок               - строка, заголовок сообщения об ошибке проведения.
 449 | //
 450 | Процедура ПроверитьЗаполнениеТабличнойЧастиТрудовыеЗатраты(Отказ)  ← замечание
 451 | 	// Укажем, что надо проверить:
 452 | 	СтруктураОбязательныхПолей = Новый Структура("Квалификация");
```

**Что не так:** Без секции «Параметры:» описание метода не читается средствами EDT и не попадает в контекстную подсказку (№455).

**Как правильно:**

```bsl
// Параметры:
//   ОтборРемонтов - Структура - отбор по периоду и подразделению.
//
Процедура ЗаполнитьНаСервере(Знач ОтборРемонтов) Экспорт
```

### 12. `DocumentationParameters` — Наличие секции параметров в описании экспортной процедуры (функции)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:477` · диагностика BSL LS: `MissingParameterDescription` · [карточка](https://docs.checkbsl.org/checks/overall/DocumentationParameters/)

```bsl
 475 | // Процедура выполняет движения документа по регистру "торо_ПлановыеТрудовыеЗатратыРемонтныхРабот".
 476 | //
 477 | Процедура ДвиженияПоРегистру_торо_ПлановыеТрудовыеЗатратыРемонтныхРабот(РежимПроведения, ТаблицаТрудовыхЗатрат  ← замечание
 478 | 
 479 | 	Движения.торо_ПлановыеТрудовыеЗатратыРемонтныхРабот.Записывать = Истина;
```

**Что не так:** Без секции «Параметры:» описание метода не читается средствами EDT и не попадает в контекстную подсказку (№455).

**Как правильно:**

```bsl
// Параметры:
//   ОтборРемонтов - Структура - отбор по периоду и подразделению.
//
Процедура ЗаполнитьНаСервере(Знач ОтборРемонтов) Экспорт
```

### 13. `DocumentationParameters` — Наличие секции параметров в описании экспортной процедуры (функции)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:509` · диагностика BSL LS: `MissingParameterDescription` · [карточка](https://docs.checkbsl.org/checks/overall/DocumentationParameters/)

```bsl
 507 | 
 508 | // Движения по регистру "Внешние основания для работ", простановка Обработано = Истина
 509 | Процедура ДвиженияПоРегистру_торо_ВнешниеОснованияДляРабот(РежимПроведения, ТаблицаВО, Отказ)  ← замечание
 510 | 
 511 | 	Движения.торо_ВнешниеОснованияДляРабот.Очистить();
```

**Что не так:** Без секции «Параметры:» описание метода не читается средствами EDT и не попадает в контекстную подсказку (№455).

**Как правильно:**

```bsl
// Параметры:
//   ОтборРемонтов - Структура - отбор по периоду и подразделению.
//
Процедура ЗаполнитьНаСервере(Знач ОтборРемонтов) Экспорт
```

### 14. `DocumentationReturnValue` — Наличие секции возвращаемого значения в описании экспортной функции (№641)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:538` · диагностика BSL LS: `MissingReturnedValueDescription` · [карточка](https://docs.checkbsl.org/checks/overall/DocumentationReturnValue/)

```bsl
 536 | //  Сформированная таблиица значений.
 537 | //
 538 | Функция ПодготовитьТаблицуРемонтныхРабот(МенеджерВТ)  ← замечание
 539 | 
 540 | 	ТаблицаРемонтныхРабот = РемонтныеРаботы.Выгрузить();
```

**Что не так:** Экспортная функция без секции «Возвращаемое значение:» заставляет читать тело, чтобы понять контракт (№641).

**Как правильно:**

```bsl
// Возвращаемое значение:
//   Массив из СтрокаТаблицыЗначений - незакрытые ремонты.
//
Функция НезакрытыеРемонты(Период) Экспорт
```

### 15. `DocumentationParameters` — Наличие секции параметров в описании экспортной процедуры (функции)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:538` · диагностика BSL LS: `MissingParameterDescription` · [карточка](https://docs.checkbsl.org/checks/overall/DocumentationParameters/)

```bsl
 536 | //  Сформированная таблиица значений.
 537 | //
 538 | Функция ПодготовитьТаблицуРемонтныхРабот(МенеджерВТ)  ← замечание
 539 | 
 540 | 	ТаблицаРемонтныхРабот = РемонтныеРаботы.Выгрузить();
```

**Что не так:** Без секции «Параметры:» описание метода не читается средствами EDT и не попадает в контекстную подсказку (№455).

**Как правильно:**

```bsl
// Параметры:
//   ОтборРемонтов - Структура - отбор по периоду и подразделению.
//
Процедура ЗаполнитьНаСервере(Знач ОтборРемонтов) Экспорт
```

### 16. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:573` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 571 | 	Запрос.МенеджерВременныхТаблиц = МенеджерВТ;
 572 | 	Запрос.Текст = "ВЫБРАТЬ
 573 | 					|	ТаблицаРР.ID,  ← замечание
 574 | 					|   ТаблицаРР.РемонтнаяРабота,
 575 | 					|	ТаблицаРР.Количество,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 17. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:574` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 572 | 	Запрос.Текст = "ВЫБРАТЬ
 573 | 					|	ТаблицаРР.ID,
 574 | 					|   ТаблицаРР.РемонтнаяРабота,  ← замечание
 575 | 					|	ТаблицаРР.Количество,
 576 | 					|	ТаблицаРР.РемонтыОборудования_ID,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 18. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:575` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 573 | 					|	ТаблицаРР.ID,
 574 | 					|   ТаблицаРР.РемонтнаяРабота,
 575 | 					|	ТаблицаРР.Количество,  ← замечание
 576 | 					|	ТаблицаРР.РемонтыОборудования_ID,
 577 | 					|   ТаблицаРР.ПроцентНевыполненныхРабот,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 19. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:576` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 574 | 					|   ТаблицаРР.РемонтнаяРабота,
 575 | 					|	ТаблицаРР.Количество,
 576 | 					|	ТаблицаРР.РемонтыОборудования_ID,  ← замечание
 577 | 					|   ТаблицаРР.ПроцентНевыполненныхРабот,
 578 | 					|   ТаблицаРР.Родитель_ID
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 20. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:577` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 575 | 					|	ТаблицаРР.Количество,
 576 | 					|	ТаблицаРР.РемонтыОборудования_ID,
 577 | 					|   ТаблицаРР.ПроцентНевыполненныхРабот,  ← замечание
 578 | 					|   ТаблицаРР.Родитель_ID
 579 | 					|ПОМЕСТИТЬ ТаблицаРемонтныхРабот
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 21. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:578` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 576 | 					|	ТаблицаРР.РемонтыОборудования_ID,
 577 | 					|   ТаблицаРР.ПроцентНевыполненныхРабот,
 578 | 					|   ТаблицаРР.Родитель_ID  ← замечание
 579 | 					|ПОМЕСТИТЬ ТаблицаРемонтныхРабот
 580 | 					|ИЗ &ТаблицаРР КАК ТаблицаРР
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 22. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:589` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 587 | 					|
 588 | 					|ВЫБРАТЬ
 589 | 					|	ТаблицаРемонтовОборудования.ОбъектРемонта,  ← замечание
 590 | 					|   ТаблицаРемонтовОборудования.ID,
 591 | 					|	ТаблицаРемонтовОборудования.ВидРемонтныхРабот,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 23. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:590` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 588 | 					|ВЫБРАТЬ
 589 | 					|	ТаблицаРемонтовОборудования.ОбъектРемонта,
 590 | 					|   ТаблицаРемонтовОборудования.ID,  ← замечание
 591 | 					|	ТаблицаРемонтовОборудования.ВидРемонтныхРабот,
 592 | 					|	ТаблицаРемонтовОборудования.ДатаНачала,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 24. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:591` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 589 | 					|	ТаблицаРемонтовОборудования.ОбъектРемонта,
 590 | 					|   ТаблицаРемонтовОборудования.ID,
 591 | 					|	ТаблицаРемонтовОборудования.ВидРемонтныхРабот,  ← замечание
 592 | 					|	ТаблицаРемонтовОборудования.ДатаНачала,
 593 | 					|   ТаблицаРемонтовОборудования.ДатаОкончания,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 25. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:592` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 590 | 					|   ТаблицаРемонтовОборудования.ID,
 591 | 					|	ТаблицаРемонтовОборудования.ВидРемонтныхРабот,
 592 | 					|	ТаблицаРемонтовОборудования.ДатаНачала,  ← замечание
 593 | 					|   ТаблицаРемонтовОборудования.ДатаОкончания,
 594 | 					|	ТаблицаРемонтовОборудования.ДокументИсточник
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 26. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:593` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 591 | 					|	ТаблицаРемонтовОборудования.ВидРемонтныхРабот,
 592 | 					|	ТаблицаРемонтовОборудования.ДатаНачала,
 593 | 					|   ТаблицаРемонтовОборудования.ДатаОкончания,  ← замечание
 594 | 					|	ТаблицаРемонтовОборудования.ДокументИсточник
 595 | 					|ПОМЕСТИТЬ ТаблицаРемОборуд
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 27. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:594` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 592 | 					|	ТаблицаРемонтовОборудования.ДатаНачала,
 593 | 					|   ТаблицаРемонтовОборудования.ДатаОкончания,
 594 | 					|	ТаблицаРемонтовОборудования.ДокументИсточник  ← замечание
 595 | 					|ПОМЕСТИТЬ ТаблицаРемОборуд
 596 | 					|ИЗ
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 28. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:604` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 602 | 					|
 603 | 					|ВЫБРАТЬ
 604 | 					|	ТаблицаРемонтныхРабот.ID,  ← замечание
 605 | 					|   ТаблицаРемонтныхРабот.РемонтнаяРабота,
 606 | 					|	ТаблицаРемонтныхРабот.Количество,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 29. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:605` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 603 | 					|ВЫБРАТЬ
 604 | 					|	ТаблицаРемонтныхРабот.ID,
 605 | 					|   ТаблицаРемонтныхРабот.РемонтнаяРабота,  ← замечание
 606 | 					|	ТаблицаРемонтныхРабот.Количество,
 607 | 					|	ТаблицаРемонтныхРабот.РемонтыОборудования_ID,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 30. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:606` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 604 | 					|	ТаблицаРемонтныхРабот.ID,
 605 | 					|   ТаблицаРемонтныхРабот.РемонтнаяРабота,
 606 | 					|	ТаблицаРемонтныхРабот.Количество,  ← замечание
 607 | 					|	ТаблицаРемонтныхРабот.РемонтыОборудования_ID,
 608 | 					|   ТаблицаРемонтныхРабот.ПроцентНевыполненныхРабот,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 31. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:607` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 605 | 					|   ТаблицаРемонтныхРабот.РемонтнаяРабота,
 606 | 					|	ТаблицаРемонтныхРабот.Количество,
 607 | 					|	ТаблицаРемонтныхРабот.РемонтыОборудования_ID,  ← замечание
 608 | 					|   ТаблицаРемонтныхРабот.ПроцентНевыполненныхРабот,
 609 | 					|	ТаблицаРемонтныхРабот.Родитель_ID,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 32. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:608` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 606 | 					|	ТаблицаРемонтныхРабот.Количество,
 607 | 					|	ТаблицаРемонтныхРабот.РемонтыОборудования_ID,
 608 | 					|   ТаблицаРемонтныхРабот.ПроцентНевыполненныхРабот,  ← замечание
 609 | 					|	ТаблицаРемонтныхРабот.Родитель_ID,
 610 | 					|	ТаблицаРемонтовОборудования.ОбъектРемонта,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 33. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:609` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 607 | 					|	ТаблицаРемонтныхРабот.РемонтыОборудования_ID,
 608 | 					|   ТаблицаРемонтныхРабот.ПроцентНевыполненныхРабот,
 609 | 					|	ТаблицаРемонтныхРабот.Родитель_ID,  ← замечание
 610 | 					|	ТаблицаРемонтовОборудования.ОбъектРемонта,
 611 | 					|	ТаблицаРемонтовОборудования.ВидРемонтныхРабот,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 34. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:610` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 608 | 					|   ТаблицаРемонтныхРабот.ПроцентНевыполненныхРабот,
 609 | 					|	ТаблицаРемонтныхРабот.Родитель_ID,
 610 | 					|	ТаблицаРемонтовОборудования.ОбъектРемонта,  ← замечание
 611 | 					|	ТаблицаРемонтовОборудования.ВидРемонтныхРабот,
 612 | 					|	ТаблицаРемонтовОборудования.ДатаНачала,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 35. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:611` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 609 | 					|	ТаблицаРемонтныхРабот.Родитель_ID,
 610 | 					|	ТаблицаРемонтовОборудования.ОбъектРемонта,
 611 | 					|	ТаблицаРемонтовОборудования.ВидРемонтныхРабот,  ← замечание
 612 | 					|	ТаблицаРемонтовОборудования.ДатаНачала,
 613 | 					|   ТаблицаРемонтовОборудования.ДатаОкончания,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 36. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:612` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 610 | 					|	ТаблицаРемонтовОборудования.ОбъектРемонта,
 611 | 					|	ТаблицаРемонтовОборудования.ВидРемонтныхРабот,
 612 | 					|	ТаблицаРемонтовОборудования.ДатаНачала,  ← замечание
 613 | 					|   ТаблицаРемонтовОборудования.ДатаОкончания,
 614 | 					|	ТаблицаРемонтовОборудования.ДокументИсточник КАК ДокументОснование
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 37. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:613` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 611 | 					|	ТаблицаРемонтовОборудования.ВидРемонтныхРабот,
 612 | 					|	ТаблицаРемонтовОборудования.ДатаНачала,
 613 | 					|   ТаблицаРемонтовОборудования.ДатаОкончания,  ← замечание
 614 | 					|	ТаблицаРемонтовОборудования.ДокументИсточник КАК ДокументОснование
 615 | 					|ПОМЕСТИТЬ ТаблицаРемРабот
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 38. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:623` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 621 | 					|
 622 | 					|ВЫБРАТЬ
 623 | 					|	ТаблицаРемРабот.ID,  ← замечание
 624 | 					|   ТаблицаРемРабот.РемонтнаяРабота,
 625 | 					|	ТаблицаРемРабот.Количество,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 39. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:624` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 622 | 					|ВЫБРАТЬ
 623 | 					|	ТаблицаРемРабот.ID,
 624 | 					|   ТаблицаРемРабот.РемонтнаяРабота,  ← замечание
 625 | 					|	ТаблицаРемРабот.Количество,
 626 | 					|	ТаблицаРемРабот.РемонтыОборудования_ID,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 40. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:625` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 623 | 					|	ТаблицаРемРабот.ID,
 624 | 					|   ТаблицаРемРабот.РемонтнаяРабота,
 625 | 					|	ТаблицаРемРабот.Количество,  ← замечание
 626 | 					|	ТаблицаРемРабот.РемонтыОборудования_ID,
 627 | 					|   ТаблицаРемРабот.ПроцентНевыполненныхРабот,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 41. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:626` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 624 | 					|   ТаблицаРемРабот.РемонтнаяРабота,
 625 | 					|	ТаблицаРемРабот.Количество,
 626 | 					|	ТаблицаРемРабот.РемонтыОборудования_ID,  ← замечание
 627 | 					|   ТаблицаРемРабот.ПроцентНевыполненныхРабот,
 628 | 					|	ТаблицаРемРабот.Родитель_ID,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 42. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:627` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 625 | 					|	ТаблицаРемРабот.Количество,
 626 | 					|	ТаблицаРемРабот.РемонтыОборудования_ID,
 627 | 					|   ТаблицаРемРабот.ПроцентНевыполненныхРабот,  ← замечание
 628 | 					|	ТаблицаРемРабот.Родитель_ID,
 629 | 					|	ТаблицаРемРабот.ОбъектРемонта,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 43. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:628` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 626 | 					|	ТаблицаРемРабот.РемонтыОборудования_ID,
 627 | 					|   ТаблицаРемРабот.ПроцентНевыполненныхРабот,
 628 | 					|	ТаблицаРемРабот.Родитель_ID,  ← замечание
 629 | 					|	ТаблицаРемРабот.ОбъектРемонта,
 630 | 					|	ТаблицаРемРабот.ВидРемонтныхРабот,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 44. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:629` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 627 | 					|   ТаблицаРемРабот.ПроцентНевыполненныхРабот,
 628 | 					|	ТаблицаРемРабот.Родитель_ID,
 629 | 					|	ТаблицаРемРабот.ОбъектРемонта,  ← замечание
 630 | 					|	ТаблицаРемРабот.ВидРемонтныхРабот,
 631 | 					|	ТаблицаРемРабот.ДатаНачала,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 45. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:630` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 628 | 					|	ТаблицаРемРабот.Родитель_ID,
 629 | 					|	ТаблицаРемРабот.ОбъектРемонта,
 630 | 					|	ТаблицаРемРабот.ВидРемонтныхРабот,  ← замечание
 631 | 					|	ТаблицаРемРабот.ДатаНачала,
 632 | 					|   ТаблицаРемРабот.ДатаОкончания,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 46. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:631` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 629 | 					|	ТаблицаРемРабот.ОбъектРемонта,
 630 | 					|	ТаблицаРемРабот.ВидРемонтныхРабот,
 631 | 					|	ТаблицаРемРабот.ДатаНачала,  ← замечание
 632 | 					|   ТаблицаРемРабот.ДатаОкончания,
 633 | 					|	ТаблицаРемРабот.ДокументОснование
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 47. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:632` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 630 | 					|	ТаблицаРемРабот.ВидРемонтныхРабот,
 631 | 					|	ТаблицаРемРабот.ДатаНачала,
 632 | 					|   ТаблицаРемРабот.ДатаОкончания,  ← замечание
 633 | 					|	ТаблицаРемРабот.ДокументОснование
 634 | 					|ИЗ
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 48. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:633` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 631 | 					|	ТаблицаРемРабот.ДатаНачала,
 632 | 					|   ТаблицаРемРабот.ДатаОкончания,
 633 | 					|	ТаблицаРемРабот.ДокументОснование  ← замечание
 634 | 					|ИЗ
 635 | 					|	ТаблицаРемРабот КАК ТаблицаРемРабот
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 49. `DocumentationReturnValue` — Наличие секции возвращаемого значения в описании экспортной функции (№641)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:741` · диагностика BSL LS: `MissingReturnedValueDescription` · [карточка](https://docs.checkbsl.org/checks/overall/DocumentationReturnValue/)

```bsl
 739 | // Функция формирует таблицу плановых материальных затрат для движения документа по регистру
 740 | // "торо_ПлановыеМатериальныеЗатратыРемонтныхРабот".
 741 | Функция СформироватьТаблицуМатериальныхЗатрат(МенеджерВТ)  ← замечание
 742 | 
 743 | 	Запрос = Новый Запрос;
```

**Что не так:** Экспортная функция без секции «Возвращаемое значение:» заставляет читать тело, чтобы понять контракт (№641).

**Как правильно:**

```bsl
// Возвращаемое значение:
//   Массив из СтрокаТаблицыЗначений - незакрытые ремонты.
//
Функция НезакрытыеРемонты(Период) Экспорт
```

### 50. `DocumentationParameters` — Наличие секции параметров в описании экспортной процедуры (функции)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:741` · диагностика BSL LS: `MissingParameterDescription` · [карточка](https://docs.checkbsl.org/checks/overall/DocumentationParameters/)

```bsl
 739 | // Функция формирует таблицу плановых материальных затрат для движения документа по регистру
 740 | // "торо_ПлановыеМатериальныеЗатратыРемонтныхРабот".
 741 | Функция СформироватьТаблицуМатериальныхЗатрат(МенеджерВТ)  ← замечание
 742 | 
 743 | 	Запрос = Новый Запрос;
```

**Что не так:** Без секции «Параметры:» описание метода не читается средствами EDT и не попадает в контекстную подсказку (№455).

**Как правильно:**

```bsl
// Параметры:
//   ОтборРемонтов - Структура - отбор по периоду и подразделению.
//
Процедура ЗаполнитьНаСервере(Знач ОтборРемонтов) Экспорт
```

### 51. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:746` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 744 | 	Запрос.МенеджерВременныхТаблиц = МенеджерВТ;
 745 | 	Запрос.Текст = "ВЫБРАТЬ РАЗЛИЧНЫЕ
 746 | 	               |	ТаблицаМатериальныхЗатрат.ID,  ← замечание
 747 | 	               |	ТаблицаМатериальныхЗатрат.РемонтыОборудования_ID,
 748 | 	               |	ТаблицаМатериальныхЗатрат.Номенклатура,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 52. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:747` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 745 | 	Запрос.Текст = "ВЫБРАТЬ РАЗЛИЧНЫЕ
 746 | 	               |	ТаблицаМатериальныхЗатрат.ID,
 747 | 	               |	ТаблицаМатериальныхЗатрат.РемонтыОборудования_ID,  ← замечание
 748 | 	               |	ТаблицаМатериальныхЗатрат.Номенклатура,
 749 | 	               |	ТаблицаМатериальныхЗатрат.Количество,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 53. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:748` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 746 | 	               |	ТаблицаМатериальныхЗатрат.ID,
 747 | 	               |	ТаблицаМатериальныхЗатрат.РемонтыОборудования_ID,
 748 | 	               |	ТаблицаМатериальныхЗатрат.Номенклатура,  ← замечание
 749 | 	               |	ТаблицаМатериальныхЗатрат.Количество,
 750 | 	               |	ТаблицаМатериальныхЗатрат.ХарактеристикаНоменклатуры,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 54. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:749` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 747 | 	               |	ТаблицаМатериальныхЗатрат.РемонтыОборудования_ID,
 748 | 	               |	ТаблицаМатериальныхЗатрат.Номенклатура,
 749 | 	               |	ТаблицаМатериальныхЗатрат.Количество,  ← замечание
 750 | 	               |	ТаблицаМатериальныхЗатрат.ХарактеристикаНоменклатуры,
 751 | 	               |	ВЫРАЗИТЬ(ТаблицаМатериальныхЗатрат.ЕдиницаИзмерения КАК Справочник.УпаковкиНоменклатуры) КАК
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 55. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:750` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 748 | 	               |	ТаблицаМатериальныхЗатрат.Номенклатура,
 749 | 	               |	ТаблицаМатериальныхЗатрат.Количество,
 750 | 	               |	ТаблицаМатериальныхЗатрат.ХарактеристикаНоменклатуры,  ← замечание
 751 | 	               |	ВЫРАЗИТЬ(ТаблицаМатериальныхЗатрат.ЕдиницаИзмерения КАК Справочник.УпаковкиНоменклатуры) КАК
 752 | 	               |ПОМЕСТИТЬ ТаблицаМатЗатрат
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 56. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:765` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 763 | 	               |////////////////////////////////////////////////////////////////////////////////
 764 | 	               |ВЫБРАТЬ
 765 | 	               |	торо_ЗаявкаНаРемонтМатериальныеЗатраты.Номенклатура,  ← замечание
 766 | 	               |	СУММА(ВЫБОР
 767 | 	               |			КОГДА ЕСТЬNULL(торо_ЗаявкаНаРемонтМатериальныеЗатраты.ЕдиницаИзмерения.Коэффициент, 0) = 0
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 57. `ExcessiveDereferenceFields` — Разыменование полей в запросе (№654)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:767` · диагностика BSL LS: `QueryNestedFieldsByDot` · [карточка](https://docs.checkbsl.org/checks/query/ExcessiveDereferenceFields/)

```bsl
 765 | 	               |	торо_ЗаявкаНаРемонтМатериальныеЗатраты.Номенклатура,
 766 | 	               |	СУММА(ВЫБОР
 767 | 	               |			КОГДА ЕСТЬNULL(торо_ЗаявкаНаРемонтМатериальныеЗатраты.ЕдиницаИзмерения.Коэффициент, 0) = 0  ← замечание
 768 | 	               |				ТОГДА 1
 769 | 	               |			ИНАЧЕ торо_ЗаявкаНаРемонтМатериальныеЗатраты.ЕдиницаИзмерения.Коэффициент
```

**Что не так:** Разыменование ссылочных полей через точку (№654): СУБД делает неявное соединение на каждую точку — на больших выборках это главный множитель времени запроса.

**Как правильно:**

```bsl
// вместо Товары.Номенклатура.Код — соединение с явным отбором:
|ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Номенклатура КАК Номенклатура
|	ПО Товары.Номенклатура = Номенклатура.Ссылка
// и выбирать Номенклатура.Код
```

### 58. `ExcessiveDereferenceFields` — Разыменование полей в запросе (№654)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:769` · диагностика BSL LS: `QueryNestedFieldsByDot` · [карточка](https://docs.checkbsl.org/checks/query/ExcessiveDereferenceFields/)

```bsl
 767 | 	               |			КОГДА ЕСТЬNULL(торо_ЗаявкаНаРемонтМатериальныеЗатраты.ЕдиницаИзмерения.Коэффициент, 0) = 0
 768 | 	               |				ТОГДА 1
 769 | 	               |			ИНАЧЕ торо_ЗаявкаНаРемонтМатериальныеЗатраты.ЕдиницаИзмерения.Коэффициент  ← замечание
 770 | 	               |		КОНЕЦ * торо_ЗаявкаНаРемонтМатериальныеЗатраты.Количество) КАК Количество,
 771 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ID,
```

**Что не так:** Разыменование ссылочных полей через точку (№654): СУБД делает неявное соединение на каждую точку — на больших выборках это главный множитель времени запроса.

**Как правильно:**

```bsl
// вместо Товары.Номенклатура.Код — соединение с явным отбором:
|ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Номенклатура КАК Номенклатура
|	ПО Товары.Номенклатура = Номенклатура.Ссылка
// и выбирать Номенклатура.Код
```

### 59. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:771` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 769 | 	               |			ИНАЧЕ торо_ЗаявкаНаРемонтМатериальныеЗатраты.ЕдиницаИзмерения.Коэффициент
 770 | 	               |		КОНЕЦ * торо_ЗаявкаНаРемонтМатериальныеЗатраты.Количество) КАК Количество,
 771 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ID,  ← замечание
 772 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.РемонтнаяРабота,
 773 | 	               |	торо_ЗаявкаНаРемонтМатериальныеЗатраты.ХарактеристикаНоменклатуры,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 60. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:772` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 770 | 	               |		КОНЕЦ * торо_ЗаявкаНаРемонтМатериальныеЗатраты.Количество) КАК Количество,
 771 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ID,
 772 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.РемонтнаяРабота,  ← замечание
 773 | 	               |	торо_ЗаявкаНаРемонтМатериальныеЗатраты.ХарактеристикаНоменклатуры,
 774 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.РемонтыОборудования_ID,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 61. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:773` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 771 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ID,
 772 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.РемонтнаяРабота,
 773 | 	               |	торо_ЗаявкаНаРемонтМатериальныеЗатраты.ХарактеристикаНоменклатуры,  ← замечание
 774 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.РемонтыОборудования_ID,
 775 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ОбъектРемонта,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 62. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:774` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 772 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.РемонтнаяРабота,
 773 | 	               |	торо_ЗаявкаНаРемонтМатериальныеЗатраты.ХарактеристикаНоменклатуры,
 774 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.РемонтыОборудования_ID,  ← замечание
 775 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ОбъектРемонта,
 776 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ВидРемонтныхРабот,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 63. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:775` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 773 | 	               |	торо_ЗаявкаНаРемонтМатериальныеЗатраты.ХарактеристикаНоменклатуры,
 774 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.РемонтыОборудования_ID,
 775 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ОбъектРемонта,  ← замечание
 776 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ВидРемонтныхРабот,
 777 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ДатаНачала,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 64. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:776` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 774 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.РемонтыОборудования_ID,
 775 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ОбъектРемонта,
 776 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ВидРемонтныхРабот,  ← замечание
 777 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ДатаНачала,
 778 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ДатаОкончания,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 65. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:777` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 775 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ОбъектРемонта,
 776 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ВидРемонтныхРабот,
 777 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ДатаНачала,  ← замечание
 778 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ДатаОкончания,
 779 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ДокументОснование,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 66. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:778` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 776 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ВидРемонтныхРабот,
 777 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ДатаНачала,
 778 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ДатаОкончания,  ← замечание
 779 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ДокументОснование,
 780 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.Родитель_ID
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 67. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:779` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 777 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ДатаНачала,
 778 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ДатаОкончания,
 779 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ДокументОснование,  ← замечание
 780 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.Родитель_ID
 781 | 	               |ИЗ
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 68. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:780` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 778 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ДатаОкончания,
 779 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ДокументОснование,
 780 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.Родитель_ID  ← замечание
 781 | 	               |ИЗ
 782 | 	               |	ТаблицаМатЗатрат КАК торо_ЗаявкаНаРемонтМатериальныеЗатраты
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 69. `DocumentationReturnValue` — Наличие секции возвращаемого значения в описании экспортной функции (№641)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:816` · диагностика BSL LS: `MissingReturnedValueDescription` · [карточка](https://docs.checkbsl.org/checks/overall/DocumentationReturnValue/)

```bsl
 814 | //  ТаблицаРемонтныхРабот - ТаблицаЗначений - таблица ремонтных работ.
 815 | //
 816 | Функция СформироватьТаблицуТрудовыхЗатрат(МенеджерВТ)  ← замечание
 817 | 
 818 | 	Запрос = Новый Запрос;
```

**Что не так:** Экспортная функция без секции «Возвращаемое значение:» заставляет читать тело, чтобы понять контракт (№641).

**Как правильно:**

```bsl
// Возвращаемое значение:
//   Массив из СтрокаТаблицыЗначений - незакрытые ремонты.
//
Функция НезакрытыеРемонты(Период) Экспорт
```

### 70. `MethodSize` — Ограничение на количество строк Процедуры/Функции

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:816` · диагностика BSL LS: `MethodSize` · [карточка](https://docs.checkbsl.org/checks/overall/MethodSize/)

```bsl
 814 | //  ТаблицаРемонтныхРабот - ТаблицаЗначений - таблица ремонтных работ.
 815 | //
 816 | Функция СформироватьТаблицуТрудовыхЗатрат(МенеджерВТ)  ← замечание
 817 | 
 818 | 	Запрос = Новый Запрос;
```

**Что не так:** Метод длиннее ~200 строк не удерживается в голове при чтении — декомпозируйте на шаги с говорящими именами.

**Как правильно:**

```bsl
// ЗаполнитьАварийныеРемонтыНаСервере() →
//   ПодготовитьОтборРемонта(), ЗаполнитьТаблицуРемонтов(),
//   ЗаписатьДокументЗакрытия()
```

### 71. `DocumentationParameters` — Наличие секции параметров в описании экспортной процедуры (функции)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:816` · диагностика BSL LS: `MissingParameterDescription` · [карточка](https://docs.checkbsl.org/checks/overall/DocumentationParameters/)

```bsl
 814 | //  ТаблицаРемонтныхРабот - ТаблицаЗначений - таблица ремонтных работ.
 815 | //
 816 | Функция СформироватьТаблицуТрудовыхЗатрат(МенеджерВТ)  ← замечание
 817 | 
 818 | 	Запрос = Новый Запрос;
```

**Что не так:** Без секции «Параметры:» описание метода не читается средствами EDT и не попадает в контекстную подсказку (№455).

**Как правильно:**

```bsl
// Параметры:
//   ОтборРемонтов - Структура - отбор по периоду и подразделению.
//
Процедура ЗаполнитьНаСервере(Знач ОтборРемонтов) Экспорт
```

### 72. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:821` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 819 | 	Запрос.МенеджерВременныхТаблиц = МенеджерВТ;
 820 | 	Запрос.Текст = "ВЫБРАТЬ
 821 | 	               |	ТаблицаТрудовыхЗатрат.ID,  ← замечание
 822 | 	               |	ТаблицаТрудовыхЗатрат.Квалификация,
 823 | 	               |	ТаблицаТрудовыхЗатрат.Количество,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 73. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:822` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 820 | 	Запрос.Текст = "ВЫБРАТЬ
 821 | 	               |	ТаблицаТрудовыхЗатрат.ID,
 822 | 	               |	ТаблицаТрудовыхЗатрат.Квалификация,  ← замечание
 823 | 	               |	ТаблицаТрудовыхЗатрат.Количество,
 824 | 	               |	ТаблицаТрудовыхЗатрат.ВремяРаботы,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 74. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:823` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 821 | 	               |	ТаблицаТрудовыхЗатрат.ID,
 822 | 	               |	ТаблицаТрудовыхЗатрат.Квалификация,
 823 | 	               |	ТаблицаТрудовыхЗатрат.Количество,  ← замечание
 824 | 	               |	ТаблицаТрудовыхЗатрат.ВремяРаботы,
 825 | 	               |	ТаблицаТрудовыхЗатрат.РемонтыОборудования_ID
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 75. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:824` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 822 | 	               |	ТаблицаТрудовыхЗатрат.Квалификация,
 823 | 	               |	ТаблицаТрудовыхЗатрат.Количество,
 824 | 	               |	ТаблицаТрудовыхЗатрат.ВремяРаботы,  ← замечание
 825 | 	               |	ТаблицаТрудовыхЗатрат.РемонтыОборудования_ID
 826 | 	               |ПОМЕСТИТЬ ТаблицаТрудЗатрат
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 76. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:825` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 823 | 	               |	ТаблицаТрудовыхЗатрат.Количество,
 824 | 	               |	ТаблицаТрудовыхЗатрат.ВремяРаботы,
 825 | 	               |	ТаблицаТрудовыхЗатрат.РемонтыОборудования_ID  ← замечание
 826 | 	               |ПОМЕСТИТЬ ТаблицаТрудЗатрат
 827 | 	               |ИЗ
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 77. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:840` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 838 | 	               |ВЫБРАТЬ
 839 | 	               |	ТаблицаРемРабот.ДатаНачала КАК ДатаНач,
 840 | 	               |	КурсыВалют.Период,  ← замечание
 841 | 	               |	КурсыВалют.Кратность,
 842 | 	               |	КурсыВалют.Курс,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 78. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:841` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 839 | 	               |	ТаблицаРемРабот.ДатаНачала КАК ДатаНач,
 840 | 	               |	КурсыВалют.Период,
 841 | 	               |	КурсыВалют.Кратность,  ← замечание
 842 | 	               |	КурсыВалют.Курс,
 843 | 	               |	КурсыВалют.Валюта
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 79. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:842` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 840 | 	               |	КурсыВалют.Период,
 841 | 	               |	КурсыВалют.Кратность,
 842 | 	               |	КурсыВалют.Курс,  ← замечание
 843 | 	               |	КурсыВалют.Валюта
 844 | 	               |ПОМЕСТИТЬ ТабДатВалют
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 80. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:843` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 841 | 	               |	КурсыВалют.Кратность,
 842 | 	               |	КурсыВалют.Курс,
 843 | 	               |	КурсыВалют.Валюта  ← замечание
 844 | 	               |ПОМЕСТИТЬ ТабДатВалют
 845 | 	               |ИЗ
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 81. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:858` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 856 | 	               |////////////////////////////////////////////////////////////////////////////////
 857 | 	               |ВЫБРАТЬ
 858 | 	               |	ТабДатВалют.ДатаНач,  ← замечание
 859 | 	               |	ТабДатВалют.Валюта,
 860 | 	               |	МАКСИМУМ(ТабДатВалют.Период) КАК Период
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 82. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:859` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 857 | 	               |ВЫБРАТЬ
 858 | 	               |	ТабДатВалют.ДатаНач,
 859 | 	               |	ТабДатВалют.Валюта,  ← замечание
 860 | 	               |	МАКСИМУМ(ТабДатВалют.Период) КАК Период
 861 | 	               |ПОМЕСТИТЬ ТабМаксДатВалют
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 83. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:877` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 875 | 	               |////////////////////////////////////////////////////////////////////////////////
 876 | 	               |ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
 877 | 	               |	ТабДатВалют.ДатаНач,  ← замечание
 878 | 	               |	ТабДатВалют.Курс,
 879 | 	               |	ТабДатВалют.Кратность,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 84. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:878` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 876 | 	               |ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
 877 | 	               |	ТабДатВалют.ДатаНач,
 878 | 	               |	ТабДатВалют.Курс,  ← замечание
 879 | 	               |	ТабДатВалют.Кратность,
 880 | 	               |	ТабДатВалют.Валюта,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 85. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:879` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 877 | 	               |	ТабДатВалют.ДатаНач,
 878 | 	               |	ТабДатВалют.Курс,
 879 | 	               |	ТабДатВалют.Кратность,  ← замечание
 880 | 	               |	ТабДатВалют.Валюта,
 881 | 	               |	ТабДатВалют.Период
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 86. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:880` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 878 | 	               |	ТабДатВалют.Курс,
 879 | 	               |	ТабДатВалют.Кратность,
 880 | 	               |	ТабДатВалют.Валюта,  ← замечание
 881 | 	               |	ТабДатВалют.Период
 882 | 	               |ПОМЕСТИТЬ ТабКурсовВалют
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 87. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:881` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 879 | 	               |	ТабДатВалют.Кратность,
 880 | 	               |	ТабДатВалют.Валюта,
 881 | 	               |	ТабДатВалют.Период  ← замечание
 882 | 	               |ПОМЕСТИТЬ ТабКурсовВалют
 883 | 	               |ИЗ
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 88. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:898` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 896 | 	               |ВЫБРАТЬ
 897 | 	               |	ТаблицаРемРабот.ДатаНачала КАК ДатаНач,
 898 | 	               |	Квалификации.Период,  ← замечание
 899 | 	               |	Квалификации.Стоимость,
 900 | 	               |	Квалификации.Валюта,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 89. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:899` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 897 | 	               |	ТаблицаРемРабот.ДатаНачала КАК ДатаНач,
 898 | 	               |	Квалификации.Период,
 899 | 	               |	Квалификации.Стоимость,  ← замечание
 900 | 	               |	Квалификации.Валюта,
 901 | 	               |	Квалификации.Квалификация
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 90. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:900` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 898 | 	               |	Квалификации.Период,
 899 | 	               |	Квалификации.Стоимость,
 900 | 	               |	Квалификации.Валюта,  ← замечание
 901 | 	               |	Квалификации.Квалификация
 902 | 	               |ПОМЕСТИТЬ ТабДатКвалификаций
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 91. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:901` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 899 | 	               |	Квалификации.Стоимость,
 900 | 	               |	Квалификации.Валюта,
 901 | 	               |	Квалификации.Квалификация  ← замечание
 902 | 	               |ПОМЕСТИТЬ ТабДатКвалификаций
 903 | 	               |ИЗ
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 92. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:916` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 914 | 	               |////////////////////////////////////////////////////////////////////////////////
 915 | 	               |ВЫБРАТЬ
 916 | 	               |	ТабДатКвалификаций.ДатаНач,  ← замечание
 917 | 	               |	ТабДатКвалификаций.Квалификация,
 918 | 	               |	МАКСИМУМ(ТабДатКвалификаций.Период) КАК Период
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 93. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:917` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 915 | 	               |ВЫБРАТЬ
 916 | 	               |	ТабДатКвалификаций.ДатаНач,
 917 | 	               |	ТабДатКвалификаций.Квалификация,  ← замечание
 918 | 	               |	МАКСИМУМ(ТабДатКвалификаций.Период) КАК Период
 919 | 	               |ПОМЕСТИТЬ ТабМаксДатКвалификаций
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 94. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:935` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 933 | 	               |////////////////////////////////////////////////////////////////////////////////
 934 | 	               |ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
 935 | 	               |	ТабДатКвалификаций.ДатаНач,  ← замечание
 936 | 	               |	ТабДатКвалификаций.Валюта,
 937 | 	               |	ТабДатКвалификаций.Стоимость,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 95. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:936` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 934 | 	               |ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ
 935 | 	               |	ТабДатКвалификаций.ДатаНач,
 936 | 	               |	ТабДатКвалификаций.Валюта,  ← замечание
 937 | 	               |	ТабДатКвалификаций.Стоимость,
 938 | 	               |	ТабДатКвалификаций.Квалификация
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 96. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:937` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 935 | 	               |	ТабДатКвалификаций.ДатаНач,
 936 | 	               |	ТабДатКвалификаций.Валюта,
 937 | 	               |	ТабДатКвалификаций.Стоимость,  ← замечание
 938 | 	               |	ТабДатКвалификаций.Квалификация
 939 | 	               |ПОМЕСТИТЬ ТабСтоимостейКвалификаций
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 97. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:938` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 936 | 	               |	ТабДатКвалификаций.Валюта,
 937 | 	               |	ТабДатКвалификаций.Стоимость,
 938 | 	               |	ТабДатКвалификаций.Квалификация  ← замечание
 939 | 	               |ПОМЕСТИТЬ ТабСтоимостейКвалификаций
 940 | 	               |ИЗ
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 98. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:954` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 952 | 	               |////////////////////////////////////////////////////////////////////////////////
 953 | 	               |ВЫБРАТЬ
 954 | 	               |	ТабСтоимостейКвалификаций.ДатаНач,  ← замечание
 955 | 	               |	ТабСтоимостейКвалификаций.Валюта,
 956 | 	               |	ТабСтоимостейКвалификаций.Стоимость,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 99. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:955` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 953 | 	               |ВЫБРАТЬ
 954 | 	               |	ТабСтоимостейКвалификаций.ДатаНач,
 955 | 	               |	ТабСтоимостейКвалификаций.Валюта,  ← замечание
 956 | 	               |	ТабСтоимостейКвалификаций.Стоимость,
 957 | 	               |	ТабСтоимостейКвалификаций.Квалификация,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 100. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:956` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 954 | 	               |	ТабСтоимостейКвалификаций.ДатаНач,
 955 | 	               |	ТабСтоимостейКвалификаций.Валюта,
 956 | 	               |	ТабСтоимостейКвалификаций.Стоимость,  ← замечание
 957 | 	               |	ТабСтоимостейКвалификаций.Квалификация,
 958 | 	               |	ЕСТЬNULL(ТабКурсовВалют.Курс, 0) КАК Курс,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 101. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:957` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 955 | 	               |	ТабСтоимостейКвалификаций.Валюта,
 956 | 	               |	ТабСтоимостейКвалификаций.Стоимость,
 957 | 	               |	ТабСтоимостейКвалификаций.Квалификация,  ← замечание
 958 | 	               |	ЕСТЬNULL(ТабКурсовВалют.Курс, 0) КАК Курс,
 959 | 	               |	ЕСТЬNULL(ТабКурсовВалют.Кратность, 1) КАК Кратность
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 102. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:974` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 972 | 	               |////////////////////////////////////////////////////////////////////////////////
 973 | 	               |ВЫБРАТЬ
 974 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ID,  ← замечание
 975 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.РемонтнаяРабота,
 976 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.РемонтыОборудования_ID,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 103. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:975` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 973 | 	               |ВЫБРАТЬ
 974 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ID,
 975 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.РемонтнаяРабота,  ← замечание
 976 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.РемонтыОборудования_ID,
 977 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ОбъектРемонта,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 104. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:976` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 974 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ID,
 975 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.РемонтнаяРабота,
 976 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.РемонтыОборудования_ID,  ← замечание
 977 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ОбъектРемонта,
 978 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ВидРемонтныхРабот,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 105. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:977` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 975 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.РемонтнаяРабота,
 976 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.РемонтыОборудования_ID,
 977 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ОбъектРемонта,  ← замечание
 978 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ВидРемонтныхРабот,
 979 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ДатаНачала,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 106. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:978` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 976 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.РемонтыОборудования_ID,
 977 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ОбъектРемонта,
 978 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ВидРемонтныхРабот,  ← замечание
 979 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ДатаНачала,
 980 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ДатаОкончания,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 107. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:979` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 977 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ОбъектРемонта,
 978 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ВидРемонтныхРабот,
 979 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ДатаНачала,  ← замечание
 980 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ДатаОкончания,
 981 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ДокументОснование,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 108. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:980` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 978 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ВидРемонтныхРабот,
 979 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ДатаНачала,
 980 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ДатаОкончания,  ← замечание
 981 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ДокументОснование,
 982 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.Родитель_ID,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 109. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:981` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 979 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ДатаНачала,
 980 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ДатаОкончания,
 981 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ДокументОснование,  ← замечание
 982 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.Родитель_ID,
 983 | 	               |	торо_ЗаявкаНаРемонтТрудовыеЗатраты.Количество,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 110. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:982` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 980 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ДатаОкончания,
 981 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ДокументОснование,
 982 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.Родитель_ID,  ← замечание
 983 | 	               |	торо_ЗаявкаНаРемонтТрудовыеЗатраты.Количество,
 984 | 	               |	торо_ЗаявкаНаРемонтТрудовыеЗатраты.Квалификация,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 111. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:983` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 981 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.ДокументОснование,
 982 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.Родитель_ID,
 983 | 	               |	торо_ЗаявкаНаРемонтТрудовыеЗатраты.Количество,  ← замечание
 984 | 	               |	торо_ЗаявкаНаРемонтТрудовыеЗатраты.Квалификация,
 985 | 	               |	торо_ЗаявкаНаРемонтТрудовыеЗатраты.ВремяРаботы
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 112. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:984` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 982 | 	               |	торо_ЗаявкаНаРемонтРемонтныеРаботы.Родитель_ID,
 983 | 	               |	торо_ЗаявкаНаРемонтТрудовыеЗатраты.Количество,
 984 | 	               |	торо_ЗаявкаНаРемонтТрудовыеЗатраты.Квалификация,  ← замечание
 985 | 	               |	торо_ЗаявкаНаРемонтТрудовыеЗатраты.ВремяРаботы
 986 | 	               |ПОМЕСТИТЬ торо_ЗаявкаНаРемонт
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 113. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:985` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 983 | 	               |	торо_ЗаявкаНаРемонтТрудовыеЗатраты.Количество,
 984 | 	               |	торо_ЗаявкаНаРемонтТрудовыеЗатраты.Квалификация,
 985 | 	               |	торо_ЗаявкаНаРемонтТрудовыеЗатраты.ВремяРаботы  ← замечание
 986 | 	               |ПОМЕСТИТЬ торо_ЗаявкаНаРемонт
 987 | 	               |ИЗ
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 114. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:1000` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 998 | 	               |////////////////////////////////////////////////////////////////////////////////
 999 | 	               |ВЫБРАТЬ
1000 | 	               |	торо_ЗаявкаНаРемонт.Квалификация,  ← замечание
1001 | 	               |	торо_ЗаявкаНаРемонт.ID,
1002 | 	               |	торо_ЗаявкаНаРемонт.РемонтнаяРабота,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 115. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:1001` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
 999 | 	               |ВЫБРАТЬ
1000 | 	               |	торо_ЗаявкаНаРемонт.Квалификация,
1001 | 	               |	торо_ЗаявкаНаРемонт.ID,  ← замечание
1002 | 	               |	торо_ЗаявкаНаРемонт.РемонтнаяРабота,
1003 | 	               |	торо_ЗаявкаНаРемонт.РемонтыОборудования_ID,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 116. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:1002` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
1000 | 	               |	торо_ЗаявкаНаРемонт.Квалификация,
1001 | 	               |	торо_ЗаявкаНаРемонт.ID,
1002 | 	               |	торо_ЗаявкаНаРемонт.РемонтнаяРабота,  ← замечание
1003 | 	               |	торо_ЗаявкаНаРемонт.РемонтыОборудования_ID,
1004 | 	               |	торо_ЗаявкаНаРемонт.ОбъектРемонта,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 117. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:1003` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
1001 | 	               |	торо_ЗаявкаНаРемонт.ID,
1002 | 	               |	торо_ЗаявкаНаРемонт.РемонтнаяРабота,
1003 | 	               |	торо_ЗаявкаНаРемонт.РемонтыОборудования_ID,  ← замечание
1004 | 	               |	торо_ЗаявкаНаРемонт.ОбъектРемонта,
1005 | 	               |	торо_ЗаявкаНаРемонт.ВидРемонтныхРабот,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 118. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:1004` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
1002 | 	               |	торо_ЗаявкаНаРемонт.РемонтнаяРабота,
1003 | 	               |	торо_ЗаявкаНаРемонт.РемонтыОборудования_ID,
1004 | 	               |	торо_ЗаявкаНаРемонт.ОбъектРемонта,  ← замечание
1005 | 	               |	торо_ЗаявкаНаРемонт.ВидРемонтныхРабот,
1006 | 	               |	торо_ЗаявкаНаРемонт.ДатаНачала,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 119. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:1005` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
1003 | 	               |	торо_ЗаявкаНаРемонт.РемонтыОборудования_ID,
1004 | 	               |	торо_ЗаявкаНаРемонт.ОбъектРемонта,
1005 | 	               |	торо_ЗаявкаНаРемонт.ВидРемонтныхРабот,  ← замечание
1006 | 	               |	торо_ЗаявкаНаРемонт.ДатаНачала,
1007 | 	               |	торо_ЗаявкаНаРемонт.ДатаОкончания,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 120. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:1006` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
1004 | 	               |	торо_ЗаявкаНаРемонт.ОбъектРемонта,
1005 | 	               |	торо_ЗаявкаНаРемонт.ВидРемонтныхРабот,
1006 | 	               |	торо_ЗаявкаНаРемонт.ДатаНачала,  ← замечание
1007 | 	               |	торо_ЗаявкаНаРемонт.ДатаОкончания,
1008 | 	               |	торо_ЗаявкаНаРемонт.ДокументОснование,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 121. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:1007` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
1005 | 	               |	торо_ЗаявкаНаРемонт.ВидРемонтныхРабот,
1006 | 	               |	торо_ЗаявкаНаРемонт.ДатаНачала,
1007 | 	               |	торо_ЗаявкаНаРемонт.ДатаОкончания,  ← замечание
1008 | 	               |	торо_ЗаявкаНаРемонт.ДокументОснование,
1009 | 	               |	торо_ЗаявкаНаРемонт.Родитель_ID,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 122. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:1008` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
1006 | 	               |	торо_ЗаявкаНаРемонт.ДатаНачала,
1007 | 	               |	торо_ЗаявкаНаРемонт.ДатаОкончания,
1008 | 	               |	торо_ЗаявкаНаРемонт.ДокументОснование,  ← замечание
1009 | 	               |	торо_ЗаявкаНаРемонт.Родитель_ID,
1010 | 	               |	торо_ЗаявкаНаРемонт.ВремяРаботы,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 123. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:1009` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
1007 | 	               |	торо_ЗаявкаНаРемонт.ДатаОкончания,
1008 | 	               |	торо_ЗаявкаНаРемонт.ДокументОснование,
1009 | 	               |	торо_ЗаявкаНаРемонт.Родитель_ID,  ← замечание
1010 | 	               |	торо_ЗаявкаНаРемонт.ВремяРаботы,
1011 | 	               |	СУММА(ЕСТЬNULL(ТабСтоимостейКвалификацийИКурсов.Стоимость, 0) * ЕСТЬNULL(ТабСтоимостейКвалиф
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 124. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:1010` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
1008 | 	               |	торо_ЗаявкаНаРемонт.ДокументОснование,
1009 | 	               |	торо_ЗаявкаНаРемонт.Родитель_ID,
1010 | 	               |	торо_ЗаявкаНаРемонт.ВремяРаботы,  ← замечание
1011 | 	               |	СУММА(ЕСТЬNULL(ТабСтоимостейКвалификацийИКурсов.Стоимость, 0) * ЕСТЬNULL(ТабСтоимостейКвалиф
1012 | 	               |	СУММА(торо_ЗаявкаНаРемонт.Количество) КАК Количество
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 125. `DocumentationReturnValue` — Наличие секции возвращаемого значения в описании экспортной функции (№641)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:1072` · диагностика BSL LS: `MissingReturnedValueDescription` · [карточка](https://docs.checkbsl.org/checks/overall/DocumentationReturnValue/)

```bsl
1070 | //  ТаблицаРемонтныхРабот - ТаблицаЗначений - таблица ремонтных работ.
1071 | //
1072 | Функция СформироватьТаблицуЗапчастей(МенеджерВТ)  ← замечание
1073 | 
1074 | 	Запрос = Новый Запрос;
```

**Что не так:** Экспортная функция без секции «Возвращаемое значение:» заставляет читать тело, чтобы понять контракт (№641).

**Как правильно:**

```bsl
// Возвращаемое значение:
//   Массив из СтрокаТаблицыЗначений - незакрытые ремонты.
//
Функция НезакрытыеРемонты(Период) Экспорт
```

### 126. `DocumentationParameters` — Наличие секции параметров в описании экспортной процедуры (функции)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:1072` · диагностика BSL LS: `MissingParameterDescription` · [карточка](https://docs.checkbsl.org/checks/overall/DocumentationParameters/)

```bsl
1070 | //  ТаблицаРемонтныхРабот - ТаблицаЗначений - таблица ремонтных работ.
1071 | //
1072 | Функция СформироватьТаблицуЗапчастей(МенеджерВТ)  ← замечание
1073 | 
1074 | 	Запрос = Новый Запрос;
```

**Что не так:** Без секции «Параметры:» описание метода не читается средствами EDT и не попадает в контекстную подсказку (№455).

**Как правильно:**

```bsl
// Параметры:
//   ОтборРемонтов - Структура - отбор по периоду и подразделению.
//
Процедура ЗаполнитьНаСервере(Знач ОтборРемонтов) Экспорт
```

### 127. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:1077` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
1075 | 	Запрос.МенеджерВременныхТаблиц = МенеджерВТ;
1076 | 	Запрос.Текст = "ВЫБРАТЬ РАЗЛИЧНЫЕ
1077 | 					|	ТаблицаЗапчастей.РемонтыОборудования_ID,  ← замечание
1078 | 					|   ТаблицаЗапчастей.Номенклатура,
1079 | 					|	ТаблицаЗапчастей.Количество,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 128. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:1078` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
1076 | 	Запрос.Текст = "ВЫБРАТЬ РАЗЛИЧНЫЕ
1077 | 					|	ТаблицаЗапчастей.РемонтыОборудования_ID,
1078 | 					|   ТаблицаЗапчастей.Номенклатура,  ← замечание
1079 | 					|	ТаблицаЗапчастей.Количество,
1080 | 					|	ТаблицаЗапчастей.ХарактеристикаНоменклатуры,
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 129. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:1079` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
1077 | 					|	ТаблицаЗапчастей.РемонтыОборудования_ID,
1078 | 					|   ТаблицаЗапчастей.Номенклатура,
1079 | 					|	ТаблицаЗапчастей.Количество,  ← замечание
1080 | 					|	ТаблицаЗапчастей.ХарактеристикаНоменклатуры,
1081 | 					|	ТаблицаЗапчастей.ЕдиницаИзмерения
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 130. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:1080` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
1078 | 					|   ТаблицаЗапчастей.Номенклатура,
1079 | 					|	ТаблицаЗапчастей.Количество,
1080 | 					|	ТаблицаЗапчастей.ХарактеристикаНоменклатуры,  ← замечание
1081 | 					|	ТаблицаЗапчастей.ЕдиницаИзмерения
1082 | 					|ПОМЕСТИТЬ ТаблицаЗатратЗапчастей
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 131. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:1081` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
1079 | 					|	ТаблицаЗапчастей.Количество,
1080 | 					|	ТаблицаЗапчастей.ХарактеристикаНоменклатуры,
1081 | 					|	ТаблицаЗапчастей.ЕдиницаИзмерения  ← замечание
1082 | 					|ПОМЕСТИТЬ ТаблицаЗатратЗапчастей
1083 | 					|ИЗ
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 132. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:1091` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
1089 | 					|
1090 | 					|ВЫБРАТЬ Различные
1091 | 					|	ТаблицаРемРабот.РемонтыОборудования_ID,  ← замечание
1092 | 					|	ТаблицаРемРабот.ДатаНачала
1093 | 					|ПОМЕСТИТЬ ТаблицаРемонтовОборудования
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 133. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:1092` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
1090 | 					|ВЫБРАТЬ Различные
1091 | 					|	ТаблицаРемРабот.РемонтыОборудования_ID,
1092 | 					|	ТаблицаРемРабот.ДатаНачала  ← замечание
1093 | 					|ПОМЕСТИТЬ ТаблицаРемонтовОборудования
1094 | 					|ИЗ
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 134. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:1100` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
1098 | 					|
1099 | 					|ВЫБРАТЬ
1100 | 					|	ТаблицаЗатратЗапчастей.Номенклатура,  ← замечание
1101 | 					|	СУММА(ВЫБОР
1102 | 					|			КОГДА естьnull(ТаблицаЗатратЗапчастей.ЕдиницаИзмерения.Коэффициент, 0) = 0
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 135. `ExcessiveDereferenceFields` — Разыменование полей в запросе (№654)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:1102` · диагностика BSL LS: `QueryNestedFieldsByDot` · [карточка](https://docs.checkbsl.org/checks/query/ExcessiveDereferenceFields/)

```bsl
1100 | 					|	ТаблицаЗатратЗапчастей.Номенклатура,
1101 | 					|	СУММА(ВЫБОР
1102 | 					|			КОГДА естьnull(ТаблицаЗатратЗапчастей.ЕдиницаИзмерения.Коэффициент, 0) = 0  ← замечание
1103 | 					|				ТОГДА 1
1104 | 					|			ИНАЧЕ ТаблицаЗатратЗапчастей.ЕдиницаИзмерения.Коэффициент
```

**Что не так:** Разыменование ссылочных полей через точку (№654): СУБД делает неявное соединение на каждую точку — на больших выборках это главный множитель времени запроса.

**Как правильно:**

```bsl
// вместо Товары.Номенклатура.Код — соединение с явным отбором:
|ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Номенклатура КАК Номенклатура
|	ПО Товары.Номенклатура = Номенклатура.Ссылка
// и выбирать Номенклатура.Код
```

### 136. `ExcessiveDereferenceFields` — Разыменование полей в запросе (№654)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:1104` · диагностика BSL LS: `QueryNestedFieldsByDot` · [карточка](https://docs.checkbsl.org/checks/query/ExcessiveDereferenceFields/)

```bsl
1102 | 					|			КОГДА естьnull(ТаблицаЗатратЗапчастей.ЕдиницаИзмерения.Коэффициент, 0) = 0
1103 | 					|				ТОГДА 1
1104 | 					|			ИНАЧЕ ТаблицаЗатратЗапчастей.ЕдиницаИзмерения.Коэффициент  ← замечание
1105 | 					|		КОНЕЦ * ТаблицаЗатратЗапчастей.Количество) КАК Количество,
1106 | 					|	ТаблицаЗатратЗапчастей.ХарактеристикаНоменклатуры,
```

**Что не так:** Разыменование ссылочных полей через точку (№654): СУБД делает неявное соединение на каждую точку — на больших выборках это главный множитель времени запроса.

**Как правильно:**

```bsl
// вместо Товары.Номенклатура.Код — соединение с явным отбором:
|ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Номенклатура КАК Номенклатура
|	ПО Товары.Номенклатура = Номенклатура.Ссылка
// и выбирать Номенклатура.Код
```

### 137. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:1106` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
1104 | 					|			ИНАЧЕ ТаблицаЗатратЗапчастей.ЕдиницаИзмерения.Коэффициент
1105 | 					|		КОНЕЦ * ТаблицаЗатратЗапчастей.Количество) КАК Количество,
1106 | 					|	ТаблицаЗатратЗапчастей.ХарактеристикаНоменклатуры,  ← замечание
1107 | 					|	ТаблицаЗатратЗапчастей.РемонтыОборудования_ID,
1108 | 					|	торо_ЗаявкаНаРемонтРемонтныеРаботы.ДатаНачала КАК Период
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```

### 138. `FieldMustHaveAlias` — Назначение псевдонимов полям запроса

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:1107` · диагностика BSL LS: `AssignAliasFieldsInQuery` · [карточка](https://docs.checkbsl.org/checks/query/FieldMustHaveAlias/)

```bsl
1105 | 					|		КОНЕЦ * ТаблицаЗатратЗапчастей.Количество) КАК Количество,
1106 | 					|	ТаблицаЗатратЗапчастей.ХарактеристикаНоменклатуры,
1107 | 					|	ТаблицаЗатратЗапчастей.РемонтыОборудования_ID,  ← замечание
1108 | 					|	торо_ЗаявкаНаРемонтРемонтныеРаботы.ДатаНачала КАК Период
1109 | 					|ИЗ
```

**Что не так:** Поле запроса без псевдонима получает служебное имя: выборка читается по позиции и ломается при любой правке текста запроса.

**Как правильно:**

```bsl
ВЫБРАТЬ Т.Номенклатура КАК Номенклатура,
	СУММА(Т.Количество) КАК Количество
ИЗ Документ.Реализация.Товары КАК Т
СГРУППИРОВАТЬ ПО Т.Номенклатура
```


## 🟢 Рекомендации (на усмотрение автора)

### 139. `StyleLowercaseПерем` — Ключевое слово «перем» со строчной буквы (стиль AGENTS.md)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:6` · диагностика BSL LS: `слой 1 (checkbsl_scan)` · [карточка](https://docs.checkbsl.org/checks/overall/StyleLowercaseПерем/)

```bsl
   4 | // СЛУЖЕБНЫЕ ПЕРЕМЕННЫЕ
   5 | 
   6 | перем СтруктураДанных Экспорт; // Содержит структуру данных о статусе документа (Начальный, СогласованиеЗаверш  ← замечание
   7 | 										// из регистра сведений торо_МатрицаПереходаСтатусовДокументов.
   8 | Перем БезусловнаяЗапись Экспорт; // Отключает проверки при записи документа
```

**Что не так:** Ключевое слово «перем» строчной буквой ломает единый стиль модуля (соглашение проекта: ключевые слова с заглавной).

**Как правильно:**

```bsl
// писать с заглавной: Перем Результат;
```

### 140. `KeyWordNotCanonical` — Каноническое написание ключевых слов (№441)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:6` · диагностика BSL LS: `CanonicalSpellingKeywords` · [карточка](https://docs.checkbsl.org/checks/overall/KeyWordNotCanonical/)

```bsl
   4 | // СЛУЖЕБНЫЕ ПЕРЕМЕННЫЕ
   5 | 
   6 | перем СтруктураДанных Экспорт; // Содержит структуру данных о статусе документа (Начальный, СогласованиеЗаверш  ← замечание
   7 | 										// из регистра сведений торо_МатрицаПереходаСтатусовДокументов.
   8 | Перем БезусловнаяЗапись Экспорт; // Отключает проверки при записи документа
```

**Что не так:** Неканоничное написание ключевых слов (или/и/тогда внутри строки) затрудняет чтение и диффы (№441): И/ИЛИ/НЕ/Тогда — канонические формы.

**Как правильно:**

```bsl
Для Каждого ТекСтрока Из Объект.ЗакрываемыеРемонты Цикл
	Если ТекСтрока.Обрабатывать И (ТекСтрока.Отменен ИЛИ ТекСтрока.Перенесен) Тогда
```

### 141. `LineLength` — Ограничение на длину строки (№444)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:6` · диагностика BSL LS: `LineLength` · [карточка](https://docs.checkbsl.org/checks/overall/LineLength/)

```bsl
   4 | // СЛУЖЕБНЫЕ ПЕРЕМЕННЫЕ
   5 | 
   6 | перем СтруктураДанных Экспорт; // Содержит структуру данных о статусе документа (Начальный, СогласованиеЗаверш  ← замечание
   7 | 										// из регистра сведений торо_МатрицаПереходаСтатусовДокументов.
   8 | Перем БезусловнаяЗапись Экспорт; // Отключает проверки при записи документа
```

**Что не так:** Строка длиннее 120 символов (№444): горизонтальная прокрутка прячет конец выражения — самое важное.

**Как правильно:**

```bsl
ТекстСообщения = НСтр("ru = 'Ни для одной из выбранных строк'
	+ ' не указан новый период отправки'");
```

### 142. `CodeOutsideRegion` — Код должен быть расположен внутри области (№455,680)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:6` · диагностика BSL LS: `CodeOutOfRegion` · [карточка](https://docs.checkbsl.org/checks/overall/CodeOutsideRegion/)

```bsl
   4 | // СЛУЖЕБНЫЕ ПЕРЕМЕННЫЕ
   5 | 
   6 | перем СтруктураДанных Экспорт; // Содержит структуру данных о статусе документа (Начальный, СогласованиеЗаверш  ← замечание
   7 | 										// из регистра сведений торо_МатрицаПереходаСтатусовДокументов.
   8 | Перем БезусловнаяЗапись Экспорт; // Отключает проверки при записи документа
```

**Что не так:** Код вне областей ломает структуру модуля и навигацию по нему; размещайте по стандартным областям (№455/№680).

**Как правильно:**

```bsl
#Область СлужебныеПроцедурыИФункции

Процедура ЗаполнитьСтрокуЗакрытия(Строка)
	// ...
КонецПроцедуры

#КонецОбласти
```

### 143. `CodeOutsideRegion` — Код должен быть расположен внутри области (№455,680)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:8` · диагностика BSL LS: `CodeOutOfRegion` · [карточка](https://docs.checkbsl.org/checks/overall/CodeOutsideRegion/)

```bsl
   6 | перем СтруктураДанных Экспорт; // Содержит структуру данных о статусе документа (Начальный, СогласованиеЗаверш
   7 | 										// из регистра сведений торо_МатрицаПереходаСтатусовДокументов.
   8 | Перем БезусловнаяЗапись Экспорт; // Отключает проверки при записи документа  ← замечание
   9 | 
  10 | #Область ОбработчикиСобытий
```

**Что не так:** Код вне областей ломает структуру модуля и навигацию по нему; размещайте по стандартным областям (№455/№680).

**Как правильно:**

```bsl
#Область СлужебныеПроцедурыИФункции

Процедура ЗаполнитьСтрокуЗакрытия(Строка)
	// ...
КонецПроцедуры

#КонецОбласти
```

### 144. `KeyWordNotCanonical` — Каноническое написание ключевых слов (№441)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:38` · диагностика BSL LS: `CanonicalSpellingKeywords` · [карточка](https://docs.checkbsl.org/checks/overall/KeyWordNotCanonical/)

```bsl
  36 | 	ШаблонСообщения = НСтр("ru = 'В табличной части ""%1"" нет строк.'");
  37 | 
  38 | 	Для каждого текТЧ из СписокТЧ Цикл  ← замечание
  39 | 		Если ЭтотОбъект[ТекТЧ.Ключ].Количество() = 0 Тогда
  40 | 			ТекстСообщения = СтрШаблон(ШаблонСообщения, ТекТЧ.Значение);
```

**Что не так:** Неканоничное написание ключевых слов (или/и/тогда внутри строки) затрудняет чтение и диффы (№441): И/ИЛИ/НЕ/Тогда — канонические формы.

**Как правильно:**

```bsl
Для Каждого ТекСтрока Из Объект.ЗакрываемыеРемонты Цикл
	Если ТекСтрока.Обрабатывать И (ТекСтрока.Отменен ИЛИ ТекСтрока.Перенесен) Тогда
```

### 145. `KeyWordNotCanonical` — Каноническое написание ключевых слов (№441)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:45` · диагностика BSL LS: `CanonicalSpellingKeywords` · [карточка](https://docs.checkbsl.org/checks/overall/KeyWordNotCanonical/)

```bsl
  43 | 	КонецЦикла;
  44 | 
  45 | 	Если Отказ тогда  ← замечание
  46 | 		Возврат;
  47 | 	КонецЕсли;
```

**Что не так:** Неканоничное написание ключевых слов (или/и/тогда внутри строки) затрудняет чтение и диффы (№441): И/ИЛИ/НЕ/Тогда — канонические формы.

**Как правильно:**

```bsl
Для Каждого ТекСтрока Из Объект.ЗакрываемыеРемонты Цикл
	Если ТекСтрока.Обрабатывать И (ТекСтрока.Отменен ИЛИ ТекСтрока.Перенесен) Тогда
```

### 146. `LineLength` — Ограничение на длину строки (№444)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:63` · диагностика BSL LS: `LineLength` · [карточка](https://docs.checkbsl.org/checks/overall/LineLength/)

```bsl
  61 | 	Если ФОИспользоватьХарактеристикиНоменклатуры = Истина Тогда
  62 | 		ПараметрыПроверки = Новый Структура("СуффиксХарактеристики, ИмяТЧ", "Номенклатуры", "МатериальныеЗатраты");
  63 | 		НоменклатураСервер.ПроверитьЗаполнениеХарактеристик(ЭтотОбъект,МассивНепроверяемыхРеквизитов,Отказ, Параметр  ← замечание
  64 | 	КонецЕсли;
  65 | 
```

**Что не так:** Строка длиннее 120 символов (№444): горизонтальная прокрутка прячет конец выражения — самое важное.

**Как правильно:**

```bsl
ТекстСообщения = НСтр("ru = 'Ни для одной из выбранных строк'
	+ ' не указан новый период отправки'");
```

### 147. `Spelling` — Код должен быть написан без опечаток

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:111` · диагностика BSL LS: `Typo` · [карточка](https://docs.checkbsl.org/checks/overall/Spelling/)

```bsl
 109 | 	Пока Выборка.Следующий() Цикл
 110 | 		Если Не Выборка.Проведен Тогда
 111 | 			ТекстСообщения = НСтр("ru = 'Есть непроведенный документ основание: " + Строка(Выборка.Ссылка) + "'");  ← замечание
 112 | 			ОбщегоНазначения.СообщитьПользователю(ТекстСообщения,,,, Отказ);
 113 | 			Возврат;
```

**Что не так:** Опечатки в идентификаторах и текстах: поиск по коду промахивается, пользователи видят ошибки в интерфейсе, доверие к продукту падает.

**Как правильно:**

```bsl
// включить проверку орфографии в EDT и исправить найденное;
// в интерфейсных текстах — сверка с предметным словарём проекта
```

### 148. `LineLength` — Ограничение на длину строки (№444)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:117` · диагностика BSL LS: `LineLength` · [карточка](https://docs.checkbsl.org/checks/overall/LineLength/)

```bsl
 115 | 	КонецЦикла;
 116 | 
 117 | 	ШаблонСообщения = НСтр("ru = 'Для объекта ремонта ""%1"" с видом ремонта ""%2"" отсутствуют строки в дереве р  ← замечание
 118 | 
 119 | 	Для Каждого СтрокаСРемонтом Из РемонтыОборудования Цикл
```

**Что не так:** Строка длиннее 120 символов (№444): горизонтальная прокрутка прячет конец выражения — самое важное.

**Как правильно:**

```bsl
ТекстСообщения = НСтр("ru = 'Ни для одной из выбранных строк'
	+ ' не указан новый период отправки'");
```

### 149. `LineLength` — Ограничение на длину строки (№444)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:140` · диагностика BSL LS: `LineLength` · [карточка](https://docs.checkbsl.org/checks/overall/LineLength/)

```bsl
 138 | 
 139 | 	торо_РаботаСоСтатусамиДокументовСервер.УстановитьСтатусРемонтовПриПроведенииЗаявки(ЭтотОбъект);
 140 | 	торо_РаботаСоСтатусамиДокументовСервер.УстановитьСтатусДокумента(Ссылка, Ссылка, Движения, Перечисления.торо_  ← замечание
 141 | 	торо_РаботаСоСтатусамиДокументовСервер.ИзменитьСтатусыДокументовОснований(Ссылка);
 142 | 
```

**Что не так:** Строка длиннее 120 символов (№444): горизонтальная прокрутка прячет конец выражения — самое важное.

**Как правильно:**

```bsl
ТекстСообщения = НСтр("ru = 'Ни для одной из выбранных строк'
	+ ' не указан новый период отправки'");
```

### 150. `LatinC` — Термин "1C:Предприятие" ошибочно задан с латинской "C"

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:147` · диагностика BSL LS: `LatinAndCyrillicSymbolInWord` · [карточка](https://docs.checkbsl.org/checks/overall/LatinC/)

```bsl
 145 | Процедура ОбработкаУдаленияПроведения(Отказ)
 146 | 
 147 | 	МассивIDДляБлокировки = торо_Ремонты.МассивIDДляБлокировкиРемонтовОборудования(Ссылка, РемонтыОборудования.Вы  ← замечание
 148 | 	Если МассивIDДляБлокировки <> Неопределено И МассивIDДляБлокировки.Количество() > 0 Тогда
 149 | 		ТекстСообщения = НСтр("es='¡Es imposible anular la realización del documento, puesto que hay unos documentos
```

**Что не так:** Термин «1C:Предприятие» с латинской «C»: в текстах и именах смешение раскладок ломает поиск и порождает опечатки-двойники.

**Как правильно:**

```bsl
// писать кириллической «С»: 1С:Предприятие
```

### 151. `LineLength` — Ограничение на длину строки (№444)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:147` · диагностика BSL LS: `LineLength` · [карточка](https://docs.checkbsl.org/checks/overall/LineLength/)

```bsl
 145 | Процедура ОбработкаУдаленияПроведения(Отказ)
 146 | 
 147 | 	МассивIDДляБлокировки = торо_Ремонты.МассивIDДляБлокировкиРемонтовОборудования(Ссылка, РемонтыОборудования.Вы  ← замечание
 148 | 	Если МассивIDДляБлокировки <> Неопределено И МассивIDДляБлокировки.Количество() > 0 Тогда
 149 | 		ТекстСообщения = НСтр("es='¡Es imposible anular la realización del documento, puesto que hay unos documentos
```

**Что не так:** Строка длиннее 120 символов (№444): горизонтальная прокрутка прячет конец выражения — самое важное.

**Как правильно:**

```bsl
ТекстСообщения = НСтр("ru = 'Ни для одной из выбранных строк'
	+ ' не указан новый период отправки'");
```

### 152. `LineLength` — Ограничение на длину строки (№444)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:149` · диагностика BSL LS: `LineLength` · [карточка](https://docs.checkbsl.org/checks/overall/LineLength/)

```bsl
 147 | 	МассивIDДляБлокировки = торо_Ремонты.МассивIDДляБлокировкиРемонтовОборудования(Ссылка, РемонтыОборудования.Вы
 148 | 	Если МассивIDДляБлокировки <> Неопределено И МассивIDДляБлокировки.Количество() > 0 Тогда
 149 | 		ТекстСообщения = НСтр("es='¡Es imposible anular la realización del documento, puesto que hay unos documentos  ← замечание
 150 | 		ОбщегоНазначения.СообщитьПользователю(ТекстСообщения,,,, Отказ);
 151 | 	КонецЕсли;
```

**Что не так:** Строка длиннее 120 символов (№444): горизонтальная прокрутка прячет конец выражения — самое важное.

**Как правильно:**

```bsl
ТекстСообщения = НСтр("ru = 'Ни для одной из выбранных строк'
	+ ' не указан новый период отправки'");
```

### 153. `LineLength` — Ограничение на длину строки (№444)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:342` · диагностика BSL LS: `LineLength` · [карточка](https://docs.checkbsl.org/checks/overall/LineLength/)

```bsl
 340 | 	Если Выборка.Следующий() Тогда
 341 | 		Если Дата <= Выборка.Дата Тогда
 342 | 			ТекстСообщения = НСтр("es='¡La fecha del documento debe ser más de fechas del documento-razón!';ru='Дата до  ← замечание
 343 | 			ОбщегоНазначения.СообщитьПользователю(ТекстСообщения,,,, Отказ);
 344 | 		КонецЕсли;
```

**Что не так:** Строка длиннее 120 символов (№444): горизонтальная прокрутка прячет конец выражения — самое важное.

**Как правильно:**

```bsl
ТекстСообщения = НСтр("ru = 'Ни для одной из выбранных строк'
	+ ' не указан новый период отправки'");
```

### 154. `LineLength` — Ограничение на длину строки (№444)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:360` · диагностика BSL LS: `LineLength` · [карточка](https://docs.checkbsl.org/checks/overall/LineLength/)

```bsl
 358 | 
 359 | 	// Вызовем общую процедуру проверки.
 360 | 	торо_ЗаполнениеДокументов.ПроверитьЗаполнениеТабличнойЧасти(ЭтотОбъект, "РемонтыОборудования", СтруктураОбяза  ← замечание
 361 | 
 362 | КонецПроцедуры
```

**Что не так:** Строка длиннее 120 символов (№444): горизонтальная прокрутка прячет конец выражения — самое важное.

**Как правильно:**

```bsl
ТекстСообщения = НСтр("ru = 'Ни для одной из выбранных строк'
	+ ' не указан новый период отправки'");
```

### 155. `LineLength` — Ограничение на длину строки (№444)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:376` · диагностика BSL LS: `LineLength` · [карточка](https://docs.checkbsl.org/checks/overall/LineLength/)

```bsl
 374 | 
 375 | 	// Вызовем общую процедуру проверки.
 376 | 	торо_ЗаполнениеДокументов.ПроверитьЗаполнениеТабличнойЧасти(ЭтотОбъект, "ДокументыОснования", СтруктураОбязат  ← замечание
 377 | 
 378 | КонецПроцедуры
```

**Что не так:** Строка длиннее 120 символов (№444): горизонтальная прокрутка прячет конец выражения — самое важное.

**Как правильно:**

```bsl
ТекстСообщения = НСтр("ru = 'Ни для одной из выбранных строк'
	+ ' не указан новый период отправки'");
```

### 156. `LineLength` — Ограничение на длину строки (№444)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:392` · диагностика BSL LS: `LineLength` · [карточка](https://docs.checkbsl.org/checks/overall/LineLength/)

```bsl
 390 | 
 391 | 	// Вызовем общую процедуру проверки.
 392 | 	торо_ЗаполнениеДокументов.ПроверитьЗаполнениеТабличнойЧасти(ЭтотОбъект, "РемонтныеРаботы", СтруктураОбязатель  ← замечание
 393 | 
 394 | КонецПроцедуры
```

**Что не так:** Строка длиннее 120 символов (№444): горизонтальная прокрутка прячет конец выражения — самое важное.

**Как правильно:**

```bsl
ТекстСообщения = НСтр("ru = 'Ни для одной из выбранных строк'
	+ ' не указан новый период отправки'");
```

### 157. `LineLength` — Ограничение на длину строки (№444)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:408` · диагностика BSL LS: `LineLength` · [карточка](https://docs.checkbsl.org/checks/overall/LineLength/)

```bsl
 406 | 
 407 | 	// Вызовем общую процедуру проверки.
 408 | 	торо_ЗаполнениеДокументов.ПроверитьЗаполнениеТабличнойЧасти(ЭтотОбъект, "МатериальныеЗатраты", СтруктураОбяза  ← замечание
 409 | 
 410 | КонецПроцедуры
```

**Что не так:** Строка длиннее 120 символов (№444): горизонтальная прокрутка прячет конец выражения — самое важное.

**Как правильно:**

```bsl
ТекстСообщения = НСтр("ru = 'Ни для одной из выбранных строк'
	+ ' не указан новый период отправки'");
```

### 158. `LineLength` — Ограничение на длину строки (№444)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:424` · диагностика BSL LS: `LineLength` · [карточка](https://docs.checkbsl.org/checks/overall/LineLength/)

```bsl
 422 | 
 423 | 	// Вызовем общую процедуру проверки.
 424 | 	торо_ЗаполнениеДокументов.ПроверитьЗаполнениеТабличнойЧасти(ЭтотОбъект, "Инструменты", СтруктураОбязательныхП  ← замечание
 425 | 
 426 | КонецПроцедуры
```

**Что не так:** Строка длиннее 120 символов (№444): горизонтальная прокрутка прячет конец выражения — самое важное.

**Как правильно:**

```bsl
ТекстСообщения = НСтр("ru = 'Ни для одной из выбранных строк'
	+ ' не указан новый период отправки'");
```

### 159. `LineLength` — Ограничение на длину строки (№444)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:440` · диагностика BSL LS: `LineLength` · [карточка](https://docs.checkbsl.org/checks/overall/LineLength/)

```bsl
 438 | 
 439 | 	// Вызовем общую процедуру проверки.
 440 | 	торо_ЗаполнениеДокументов.ПроверитьЗаполнениеТабличнойЧасти(ЭтотОбъект, "ЗапчастиРемонта", СтруктураОбязатель  ← замечание
 441 | 
 442 | КонецПроцедуры
```

**Что не так:** Строка длиннее 120 символов (№444): горизонтальная прокрутка прячет конец выражения — самое важное.

**Как правильно:**

```bsl
ТекстСообщения = НСтр("ru = 'Ни для одной из выбранных строк'
	+ ' не указан новый период отправки'");
```

### 160. `LineLength` — Ограничение на длину строки (№444)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:455` · диагностика BSL LS: `LineLength` · [карточка](https://docs.checkbsl.org/checks/overall/LineLength/)

```bsl
 453 | 
 454 | 	// Вызовем общую процедуру проверки.
 455 | 	торо_ЗаполнениеДокументов.ПроверитьЗаполнениеТабличнойЧасти(ЭтотОбъект, "ТрудовыеЗатраты", СтруктураОбязатель  ← замечание
 456 | 
 457 | КонецПроцедуры
```

**Что не так:** Строка длиннее 120 символов (№444): горизонтальная прокрутка прячет конец выражения — самое важное.

**Как правильно:**

```bsl
ТекстСообщения = НСтр("ru = 'Ни для одной из выбранных строк'
	+ ' не указан новый период отправки'");
```

### 161. `LineLength` — Ограничение на длину строки (№444)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:459` · диагностика BSL LS: `LineLength` · [карточка](https://docs.checkbsl.org/checks/overall/LineLength/)

```bsl
 457 | КонецПроцедуры
 458 | 
 459 | Процедура ДвиженияПоРегистру_торо_ПлановыеМатериальныеЗатратыРемонтныхРабот(РежимПроведения, ТаблицаМатериальн  ← замечание
 460 | 
 461 | 	Движения.торо_ПлановыеМатериальныеЗатратыРемонтныхРабот.Записывать = Истина;
```

**Что не так:** Строка длиннее 120 символов (№444): горизонтальная прокрутка прячет конец выражения — самое важное.

**Как правильно:**

```bsl
ТекстСообщения = НСтр("ru = 'Ни для одной из выбранных строк'
	+ ' не указан новый период отправки'");
```

### 162. `KeyWordNotCanonical` — Каноническое написание ключевых слов (№441)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:462` · диагностика BSL LS: `CanonicalSpellingKeywords` · [карточка](https://docs.checkbsl.org/checks/overall/KeyWordNotCanonical/)

```bsl
 460 | 
 461 | 	Движения.торо_ПлановыеМатериальныеЗатратыРемонтныхРабот.Записывать = Истина;
 462 | 	Для Каждого ЗаписьМатериальныеЗатраты ИЗ ТаблицаМатериальныхЗатрат Цикл  ← замечание
 463 | 		Движение = Движения.торо_ПлановыеМатериальныеЗатратыРемонтныхРабот.Добавить();
 464 | 		Движение.ID = ЗаписьМатериальныеЗатраты.ID;
```

**Что не так:** Неканоничное написание ключевых слов (или/и/тогда внутри строки) затрудняет чтение и диффы (№441): И/ИЛИ/НЕ/Тогда — канонические формы.

**Как правильно:**

```bsl
Для Каждого ТекСтрока Из Объект.ЗакрываемыеРемонты Цикл
	Если ТекСтрока.Обрабатывать И (ТекСтрока.Отменен ИЛИ ТекСтрока.Перенесен) Тогда
```

### 163. `KeyWordNotCanonical` — Каноническое написание ключевых слов (№441)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:480` · диагностика BSL LS: `CanonicalSpellingKeywords` · [карточка](https://docs.checkbsl.org/checks/overall/KeyWordNotCanonical/)

```bsl
 478 | 
 479 | 	Движения.торо_ПлановыеТрудовыеЗатратыРемонтныхРабот.Записывать = Истина;
 480 | 	Для Каждого ЗаписьТрудовыеЗатраты ИЗ ТаблицаТрудовыхЗатрат Цикл  ← замечание
 481 | 		Движение = Движения.торо_ПлановыеТрудовыеЗатратыРемонтныхРабот.Добавить();
 482 | 		Движение.РемонтыОборудования_ID = ЗаписьТрудовыеЗатраты.РемонтыОборудования_ID;
```

**Что не так:** Неканоничное написание ключевых слов (или/и/тогда внутри строки) затрудняет чтение и диффы (№441): И/ИЛИ/НЕ/Тогда — канонические формы.

**Как правильно:**

```bsl
Для Каждого ТекСтрока Из Объект.ЗакрываемыеРемонты Цикл
	Если ТекСтрока.Обрабатывать И (ТекСтрока.Отменен ИЛИ ТекСтрока.Перенесен) Тогда
```

### 164. `ConsecutiveBlankLines` — Последовательные пустые строки

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:528` · диагностика BSL LS: `ConsecutiveEmptyLines` · [карточка](https://docs.checkbsl.org/checks/overall/ConsecutiveBlankLines/)

```bsl
 526 | 	//Движения.торо_ВнешниеОснованияДляРабот.Записать(Истина);
 527 | КонецПроцедуры
 528 |   ← замечание
 529 | 
 530 | // Выгружает данные в табличной части в таблицу значений, добавляет ей необходимые колонки для проведения.
```

**Что не так:** Несколько пустых строк подряд сбивают ритм чтения — по стандарту один пустой разделитель между блоками.

**Как правильно:**

```bsl
// оставить одну пустую строку между блоками кода
```

### 165. `ConsecutiveBlankLines` — Последовательные пустые строки

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:547` · диагностика BSL LS: `ConsecutiveEmptyLines` · [карточка](https://docs.checkbsl.org/checks/overall/ConsecutiveBlankLines/)

```bsl
 545 | 	ОписаниеТиповЧ = Новый ОписаниеТипов(МассивТипов, , ,КЧ);
 546 | 	ТаблицаРемонтныхРабот.Колонки.Добавить("ПроцентНевыполненныхРабот", ОписаниеТиповЧ);
 547 |   ← замечание
 548 | 
 549 | 	КД = Новый КвалификаторыДаты(ЧастиДаты.Дата);
```

**Что не так:** Несколько пустых строк подряд сбивают ритм чтения — по стандарту один пустой разделитель между блоками.

**Как правильно:**

```bsl
// оставить одну пустую строку между блоками кода
```

### 166. `ConsecutiveBlankLines` — Последовательные пустые строки

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:644` · диагностика BSL LS: `ConsecutiveEmptyLines` · [карточка](https://docs.checkbsl.org/checks/overall/ConsecutiveBlankLines/)

```bsl
 642 | 					|;
 643 | 					|";
 644 |   ← замечание
 645 | 
 646 | 	Запрос.УстановитьПараметр("ТаблицаРР",ТаблицаРемонтныхРабот);
```

**Что не так:** Несколько пустых строк подряд сбивают ритм чтения — по стандарту один пустой разделитель между блоками.

**Как правильно:**

```bsl
// оставить одну пустую строку между блоками кода
```

### 167. `ConsecutiveBlankLines` — Последовательные пустые строки

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:737` · диагностика BSL LS: `ConsecutiveEmptyLines` · [карточка](https://docs.checkbsl.org/checks/overall/ConsecutiveBlankLines/)

```bsl
 735 | 
 736 | КонецФункции // ()
 737 |   ← замечание
 738 | 
 739 | // Функция формирует таблицу плановых материальных затрат для движения документа по регистру
```

**Что не так:** Несколько пустых строк подряд сбивают ритм чтения — по стандарту один пустой разделитель между блоками.

**Как правильно:**

```bsl
// оставить одну пустую строку между блоками кода
```

### 168. `LineLength` — Ограничение на длину строки (№444)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:1222` · диагностика BSL LS: `LineLength` · [карточка](https://docs.checkbsl.org/checks/overall/LineLength/)

```bsl
1220 | 	Запрос.УстановитьПараметр("РемонтыОборудования", РемонтыОборудования.Выгрузить());
1221 | 
1222 | 	ШаблонСообщения = НСтр("es='Para la reparación ""%1"" es duplicado el ejecutor ""%2"" (""%3"").';ru='Для ремо  ← замечание
1223 | 
1224 | 	Выборка = Запрос.Выполнить().Выбрать();
```

**Что не так:** Строка длиннее 120 символов (№444): горизонтальная прокрутка прячет конец выражения — самое важное.

**Как правильно:**

```bsl
ТекстСообщения = НСтр("ru = 'Ни для одной из выбранных строк'
	+ ' не указан новый период отправки'");
```


## Подавленные (решение Ревьюера «не баг»)

| Ключ | Место | Причина | Автор |
|---|---|---|---|
| `MagicNumber` | /Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:197 | Умножение на 100 — перевод доли в проценты, осмысленная операция, не магическое число | Roman |
| `MagicNumber` | /Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:217 | Умножение на 100 — перевод доли в проценты, осмысленная операция, не магическое число | Roman |
| `MagicNumber` | /Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ЗаявкаНаРемонт/ObjectModule.bsl:542 | КвалификаторыЧисла(5,2) — спецификация точности типа, допустимая по практике | Roman |
---

Карточки: docs.checkbsl.org/checks/<section>/<Ключ>/; ключи без каталога — 1c-syntax.github.io/bsl-language-server/diagnostics/<Код>/.
Срабатывания — кандидаты в findings 05a, решение за Ревьюером.
