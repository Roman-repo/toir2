# Отчёт код-ревью — BSL LS (`bsl_ls_analyze.py`)

- Дата: 2026-08-30 20:12
- Слой: bsl-language-server (AST), файлов разобрано: 2627
- Вход: /Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыявленныеДефекты/ObjectModule.bsl
- Замечания: **22** — 🔴 2, 🟡 5, 🟢 15
- **Вердикт: 🔴 есть — возврат на этап «Разработка», правка, повторный прогон (следующий отчёт r2).**

## 🔴 Блокирующие (исправить до повторного прогона)

### 1. `MethodCognitiveComplexity` — Когнитивная сложность методов не должна быть слишком высокой

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыявленныеДефекты/ObjectModule.bsl:13` · диагностика BSL LS: `CognitiveComplexity` · [карточка](https://docs.checkbsl.org/checks/overall/MethodCognitiveComplexity/)

```bsl
  11 | #Область ОбработчикиСобытий
  12 | 
  13 | Процедура ОбработкаЗаполнения(Основание)  ← замечание
  14 | 
  15 | 	Если ТипЗнч(Основание) = Тип("ДокументСсылка.торо_ВнешнееОснованиеДляРабот") Тогда
```

**Что не так:** Когнитивная сложность растёт от вложенности и смены контекста: метод читается, но не удерживается в голове — вынесите вложенные ветки в методы.

**Как правильно:**

```bsl
// ранний выход вместо вложенности:
Если Отказ Тогда
	Возврат;
КонецЕсли;
// основной сценарий на нулевом уровне
```

### 2. `DataExchangeLoad` — Наличие обязательной конструкции "Если ОбменДанными.Загрузка Тогда ..."

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыявленныеДефекты/ObjectModule.bsl:85` · диагностика BSL LS: `DataExchangeLoading` · [карточка](https://docs.checkbsl.org/checks/overall/DataExchangeLoad/)

```bsl
  83 | КонецПроцедуры
  84 | 
  85 | Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)  ← замечание
  86 | 
  87 | 	Для Каждого Дефект Из СписокДефектов Цикл
```

**Что не так:** Без проверки ОбменДанными.Загрузка обработчики записи при обмене/миграции повторно запускают бизнес-логику: проверки, побочные записи, пересчёты итогов (№459).

**Как правильно:**

```bsl
Процедура ПередЗаписью(Отказ)
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	// ... бизнес-логика
КонецПроцедуры
```


## 🟡 Важные (править или обосновать в 05a)

### 3. `ExportVariable` — Использование экспортной переменной

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыявленныеДефекты/ObjectModule.bsl:7` · диагностика BSL LS: `ExportVariables` · [карточка](https://docs.checkbsl.org/checks/overall/ExportVariable/)

```bsl
   5 | // СЛУЖЕБНЫЕ ПЕРЕМЕННЫЕ
   6 | 
   7 | перем СтруктураДанных Экспорт; // Содержит структуру данных о статусе документа (Начальный, СогласованиеЗаверш  ← замечание
   8 | 										 // из регистра сведений торо_МатрицаПереходаСтатусовДокументов.
   9 | Перем БезусловнаяЗапись Экспорт; // Отключает проверки при записи документа
```

**Что не так:** Экспортная переменная — скрытое глобальное состояние: модули сцепляются через неё и ломаются при параллельных вызовах и в фоновых заданиях; замените методами.

**Как правильно:**

```bsl
// вместо «Перем КэшЦен Экспорт» — повторное использование:
Функция КэшЦен() Экспорт
	Возврат ОбщегоНазначения.ЗначениеИзКэшаСеанса("КэшЦен");
КонецФункции
```

### 4. `ExportVariable` — Использование экспортной переменной

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыявленныеДефекты/ObjectModule.bsl:9` · диагностика BSL LS: `ExportVariables` · [карточка](https://docs.checkbsl.org/checks/overall/ExportVariable/)

```bsl
   7 | перем СтруктураДанных Экспорт; // Содержит структуру данных о статусе документа (Начальный, СогласованиеЗаверш
   8 | 										 // из регистра сведений торо_МатрицаПереходаСтатусовДокументов.
   9 | Перем БезусловнаяЗапись Экспорт; // Отключает проверки при записи документа  ← замечание
  10 | 
  11 | #Область ОбработчикиСобытий
```

**Что не так:** Экспортная переменная — скрытое глобальное состояние: модули сцепляются через неё и ломаются при параллельных вызовах и в фоновых заданиях; замените методами.

**Как правильно:**

```bsl
// вместо «Перем КэшЦен Экспорт» — повторное использование:
Функция КэшЦен() Экспорт
	Возврат ОбщегоНазначения.ЗначениеИзКэшаСеанса("КэшЦен");
КонецФункции
```

### 5. `DocumentationParameters` — Наличие секции параметров в описании экспортной процедуры (функции)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыявленныеДефекты/ObjectModule.bsl:270` · диагностика BSL LS: `MissingParameterDescription` · [карточка](https://docs.checkbsl.org/checks/overall/DocumentationParameters/)

```bsl
 268 | 
 269 | // Движения по регистру "Внешние основания для работ", простановка Обработано = Истина
 270 | Процедура ДвиженияПоРегистру_торо_ВнешниеОснованияДляРабот(РежимПроведения, ТаблицаВО, Отказ)  ← замечание
 271 | 
 272 | 	Движения.торо_ВнешниеОснованияДляРабот.Очистить();
```

**Что не так:** Без секции «Параметры:» описание метода не читается средствами EDT и не попадает в контекстную подсказку (№455).

**Как правильно:**

```bsl
// Параметры:
//   ОтборРемонтов - Структура - отбор по периоду и подразделению.
//
Процедура ЗаполнитьНаСервере(Знач ОтборРемонтов) Экспорт
```

### 6. `ForbiddenMethodCurrentDate` — Использование метода "ТекущаяДата()

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыявленныеДефекты/ObjectModule.bsl:299` · диагностика BSL LS: `DeprecatedCurrentDate` · [карточка](https://docs.checkbsl.org/checks/overall/ForbiddenMethodCurrentDate/)

```bsl
 297 | 	Если НЕ ЗначениеЗаполнено(ДатаОбнаружения) И ОбщегоНазначения.ХранилищеОбщихНастроекЗагрузить(
 298 | 		"НастройкиТОиР", "ПодставлятьТекущуюДатуВоВнешнееОснованиеИВыявленныеДефекты", Ложь) Тогда
 299 | 		ДатаОбнаружения = ТекущаяДата();  ← замечание
 300 | 	КонецЕсли;
 301 | 
```

**Что не так:** ТекущаяДата() возвращает время сервера без сеансной таймзоны: на границе суток даты «прыгают»; вместо неё ТекущаяДатаСеанса().

**Как правильно:**

```bsl
Объект.ДатаАктуальности = ТекущаяДатаСеанса();
```

### 7. `ForbiddenMethodCurrentDate` — Использование метода "ТекущаяДата()

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыявленныеДефекты/ObjectModule.bsl:308` · диагностика BSL LS: `DeprecatedCurrentDate` · [карточка](https://docs.checkbsl.org/checks/overall/ForbiddenMethodCurrentDate/)

```bsl
 306 | 	ПараметрыСобытия = СтруктураПараметров.СтруктураДокументаОснования;
 307 | 
 308 | 	ЭтотОбъект.Дата = ТекущаяДата();  ← замечание
 309 | 	ЭтотОбъект.ДатаОбнаружения = ПараметрыСобытия.ДатаДокументаИсточник;
 310 | 
```

**Что не так:** ТекущаяДата() возвращает время сервера без сеансной таймзоны: на границе суток даты «прыгают»; вместо неё ТекущаяДатаСеанса().

**Как правильно:**

```bsl
Объект.ДатаАктуальности = ТекущаяДатаСеанса();
```


## 🟢 Рекомендации (на усмотрение автора)

### 8. `VariableWithoutDescription` — Переменные должны содержать описание

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыявленныеДефекты/ObjectModule.bsl:2` · диагностика BSL LS: `MissingVariablesDescription` · [карточка](https://docs.checkbsl.org/checks/overall/VariableWithoutDescription/)

```bsl
   1 | #Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда
   2 | Перем СписокОтбора;  ← замечание
   3 | 
   4 | ////////////////////////////////////////////////////////////////////////////////
```

**Что не так:** Переменная (особенно экспортная) без описания — контракт модуля без документации: тип и назначение приходится угадывать по присвоениям (№455).

**Как правильно:**

```bsl
Перем КэшЦен Экспорт; // Соответствие - кэш цен номенклатуры сеанса
```

### 9. `CodeOutsideRegion` — Код должен быть расположен внутри области (№455,680)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыявленныеДефекты/ObjectModule.bsl:2` · диагностика BSL LS: `CodeOutOfRegion` · [карточка](https://docs.checkbsl.org/checks/overall/CodeOutsideRegion/)

```bsl
   1 | #Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда
   2 | Перем СписокОтбора;  ← замечание
   3 | 
   4 | ////////////////////////////////////////////////////////////////////////////////
```

**Что не так:** Код вне областей ломает структуру модуля и навигацию по нему; размещайте по стандартным областям (№455/№680).

**Как правильно:**

```bsl
#Область СлужебныеПроцедурыИФункции

Процедура ЗаполнитьСтрокуЗакрытия(Строка)
	// ...
КонецПроцедуры

#КонецОбласти
```

### 10. `KeyWordNotCanonical` — Каноническое написание ключевых слов (№441)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыявленныеДефекты/ObjectModule.bsl:7` · диагностика BSL LS: `CanonicalSpellingKeywords` · [карточка](https://docs.checkbsl.org/checks/overall/KeyWordNotCanonical/)

```bsl
   5 | // СЛУЖЕБНЫЕ ПЕРЕМЕННЫЕ
   6 | 
   7 | перем СтруктураДанных Экспорт; // Содержит структуру данных о статусе документа (Начальный, СогласованиеЗаверш  ← замечание
   8 | 										 // из регистра сведений торо_МатрицаПереходаСтатусовДокументов.
   9 | Перем БезусловнаяЗапись Экспорт; // Отключает проверки при записи документа
```

**Что не так:** Неканоничное написание ключевых слов (или/и/тогда внутри строки) затрудняет чтение и диффы (№441): И/ИЛИ/НЕ/Тогда — канонические формы.

**Как правильно:**

```bsl
Для Каждого ТекСтрока Из Объект.ЗакрываемыеРемонты Цикл
	Если ТекСтрока.Обрабатывать И (ТекСтрока.Отменен ИЛИ ТекСтрока.Перенесен) Тогда
```

### 11. `LineLength` — Ограничение на длину строки (№444)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыявленныеДефекты/ObjectModule.bsl:7` · диагностика BSL LS: `LineLength` · [карточка](https://docs.checkbsl.org/checks/overall/LineLength/)

```bsl
   5 | // СЛУЖЕБНЫЕ ПЕРЕМЕННЫЕ
   6 | 
   7 | перем СтруктураДанных Экспорт; // Содержит структуру данных о статусе документа (Начальный, СогласованиеЗаверш  ← замечание
   8 | 										 // из регистра сведений торо_МатрицаПереходаСтатусовДокументов.
   9 | Перем БезусловнаяЗапись Экспорт; // Отключает проверки при записи документа
```

**Что не так:** Строка длиннее 120 символов (№444): горизонтальная прокрутка прячет конец выражения — самое важное.

**Как правильно:**

```bsl
ТекстСообщения = НСтр("ru = 'Ни для одной из выбранных строк'
	+ ' не указан новый период отправки'");
```

### 12. `CodeOutsideRegion` — Код должен быть расположен внутри области (№455,680)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыявленныеДефекты/ObjectModule.bsl:7` · диагностика BSL LS: `CodeOutOfRegion` · [карточка](https://docs.checkbsl.org/checks/overall/CodeOutsideRegion/)

```bsl
   5 | // СЛУЖЕБНЫЕ ПЕРЕМЕННЫЕ
   6 | 
   7 | перем СтруктураДанных Экспорт; // Содержит структуру данных о статусе документа (Начальный, СогласованиеЗаверш  ← замечание
   8 | 										 // из регистра сведений торо_МатрицаПереходаСтатусовДокументов.
   9 | Перем БезусловнаяЗапись Экспорт; // Отключает проверки при записи документа
```

**Что не так:** Код вне областей ломает структуру модуля и навигацию по нему; размещайте по стандартным областям (№455/№680).

**Как правильно:**

```bsl
#Область СлужебныеПроцедурыИФункции

Процедура ЗаполнитьСтрокуЗакрытия(Строка)
	// ...
КонецПроцедуры

#КонецОбласти
```

### 13. `CodeOutsideRegion` — Код должен быть расположен внутри области (№455,680)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыявленныеДефекты/ObjectModule.bsl:9` · диагностика BSL LS: `CodeOutOfRegion` · [карточка](https://docs.checkbsl.org/checks/overall/CodeOutsideRegion/)

```bsl
   7 | перем СтруктураДанных Экспорт; // Содержит структуру данных о статусе документа (Начальный, СогласованиеЗаверш
   8 | 										 // из регистра сведений торо_МатрицаПереходаСтатусовДокументов.
   9 | Перем БезусловнаяЗапись Экспорт; // Отключает проверки при записи документа  ← замечание
  10 | 
  11 | #Область ОбработчикиСобытий
```

**Что не так:** Код вне областей ломает структуру модуля и навигацию по нему; размещайте по стандартным областям (№455/№680).

**Как правильно:**

```bsl
#Область СлужебныеПроцедурыИФункции

Процедура ЗаполнитьСтрокуЗакрытия(Строка)
	// ...
КонецПроцедуры

#КонецОбласти
```

### 14. `KeyWordNotCanonical` — Каноническое написание ключевых слов (№441)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыявленныеДефекты/ObjectModule.bsl:73` · диагностика BSL LS: `CanonicalSpellingKeywords` · [карточка](https://docs.checkbsl.org/checks/overall/KeyWordNotCanonical/)

```bsl
  71 | 
  72 | 		Если Основание.Свойство("МассивСтрокТЧ") Тогда
  73 | 			Для каждого Строка из Основание.МассивСтрокТЧ Цикл  ← замечание
  74 | 				НоваяСтрока = СписокДефектов.Добавить();
  75 | 				ЗаполнитьЗначенияСвойств(НоваяСтрока, Строка);
```

**Что не так:** Неканоничное написание ключевых слов (или/и/тогда внутри строки) затрудняет чтение и диффы (№441): И/ИЛИ/НЕ/Тогда — канонические формы.

**Как правильно:**

```bsl
Для Каждого ТекСтрока Из Объект.ЗакрываемыеРемонты Цикл
	Если ТекСтрока.Обрабатывать И (ТекСтрока.Отменен ИЛИ ТекСтрока.Перенесен) Тогда
```

### 15. `LineLength` — Ограничение на длину строки (№444)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыявленныеДефекты/ObjectModule.bsl:98` · диагностика BSL LS: `LineLength` · [карточка](https://docs.checkbsl.org/checks/overall/LineLength/)

```bsl
  96 | 
  97 | 	Если СписокДефектов.Количество() = 0 Тогда
  98 | 		ТекстСообщения = НСтр("es='En la parte tabular ""la Lista de los defectos"" no hay líneas.';ru='В табличной   ← замечание
  99 | 		ОбщегоНазначения.СообщитьПользователю(ТекстСообщения,,,,Отказ);
 100 | 	КонецЕсли;
```

**Что не так:** Строка длиннее 120 символов (№444): горизонтальная прокрутка прячет конец выражения — самое важное.

**Как правильно:**

```bsl
ТекстСообщения = НСтр("ru = 'Ни для одной из выбранных строк'
	+ ' не указан новый период отправки'");
```

### 16. `LineLength` — Ограничение на длину строки (№444)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыявленныеДефекты/ObjectModule.bsl:111` · диагностика BSL LS: `LineLength` · [карточка](https://docs.checkbsl.org/checks/overall/LineLength/)

```bsl
 109 | 		Если ТекСтрокаСписокДефектов.ВидДефекта.Пустая() Тогда
 110 | 			ШаблонСообщения = НСтр("es='2.';ru='Не указан вид дефекта %1/%2.';en='The type of defect%1 /%2 is not speci
 111 | 			ТекстСообщения = СтрШаблон(ШаблонСообщения, ТекСтрокаСписокДефектов.ОбъектРемонта, ТекСтрокаСписокДефектов.  ← замечание
 112 | 			ОбщегоНазначения.СообщитьПользователю(ТекстСообщения,,,,Отказ);
 113 | 		КонецЕсли;
```

**Что не так:** Строка длиннее 120 символов (№444): горизонтальная прокрутка прячет конец выражения — самое важное.

**Как правильно:**

```bsl
ТекстСообщения = НСтр("ru = 'Ни для одной из выбранных строк'
	+ ' не указан новый период отправки'");
```

### 17. `LineLength` — Ограничение на длину строки (№444)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыявленныеДефекты/ObjectModule.bsl:145` · диагностика BSL LS: `LineLength` · [карточка](https://docs.checkbsl.org/checks/overall/LineLength/)

```bsl
 143 |         Движение.ДатаНачалаРемонта                      = ТекСтрокаСписокДефектов.ДатаНачалаРемонта;
 144 |         Движение.ДатаОкончанияРемонта                   = ТекСтрокаСписокДефектов.ДатаОкончанияРемонта;
 145 |         Движение.КрайняяДопустимаяДатаЗавершенияРемонта = ТекСтрокаСписокДефектов.КрайняяДопустимаяДатаЗаверше  ← замечание
 146 | 
 147 | 		// регистр сведений торо_Ремонты
```

**Что не так:** Строка длиннее 120 символов (№444): горизонтальная прокрутка прячет конец выражения — самое важное.

**Как правильно:**

```bsl
ТекстСообщения = НСтр("ru = 'Ни для одной из выбранных строк'
	+ ' не указан новый период отправки'");
```

### 18. `LineLength` — Ограничение на длину строки (№444)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыявленныеДефекты/ObjectModule.bsl:157` · диагностика BSL LS: `LineLength` · [карточка](https://docs.checkbsl.org/checks/overall/LineLength/)

```bsl
 155 |         Движение.ДатаНачалаПлан                         = ТекСтрокаСписокДефектов.ДатаНачалаРемонта;
 156 |         Движение.ДатаОкончанияПлан                      = ТекСтрокаСписокДефектов.ДатаОкончанияРемонта;
 157 |         Движение.КрайняяДопустимаяДатаЗавершенияРемонта = ТекСтрокаСписокДефектов.КрайняяДопустимаяДатаЗаверше  ← замечание
 158 | 
 159 | 
```

**Что не так:** Строка длиннее 120 символов (№444): горизонтальная прокрутка прячет конец выражения — самое важное.

**Как правильно:**

```bsl
ТекстСообщения = НСтр("ru = 'Ни для одной из выбранных строк'
	+ ' не указан новый период отправки'");
```

### 19. `ConsecutiveBlankLines` — Последовательные пустые строки

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыявленныеДефекты/ObjectModule.bsl:158` · диагностика BSL LS: `ConsecutiveEmptyLines` · [карточка](https://docs.checkbsl.org/checks/overall/ConsecutiveBlankLines/)

```bsl
 156 |         Движение.ДатаОкончанияПлан                      = ТекСтрокаСписокДефектов.ДатаОкончанияРемонта;
 157 |         Движение.КрайняяДопустимаяДатаЗавершенияРемонта = ТекСтрокаСписокДефектов.КрайняяДопустимаяДатаЗаверше
 158 |   ← замечание
 159 | 
 160 | 	КонецЦикла;
```

**Что не так:** Несколько пустых строк подряд сбивают ритм чтения — по стандарту один пустой разделитель между блоками.

**Как правильно:**

```bsl
// оставить одну пустую строку между блоками кода
```

### 20. `LineLength` — Ограничение на длину строки (№444)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыявленныеДефекты/ObjectModule.bsl:170` · диагностика BSL LS: `LineLength` · [карточка](https://docs.checkbsl.org/checks/overall/LineLength/)

```bsl
 168 | 
 169 | 	Движения.торо_СтатусыДокументовВыявленныхДефектов.Записывать = Истина;
 170 | 	торо_РаботаСоСтатусамиДокументовСервер.УстановитьСтатусДокумента(Ссылка, Ссылка, Движения, Перечисления.торо_  ← замечание
 171 | 	торо_РаботаСоСтатусамиДокументовСервер.ИзменитьСтатусыДокументовОснований(Ссылка);
 172 | 
```

**Что не так:** Строка длиннее 120 символов (№444): горизонтальная прокрутка прячет конец выражения — самое важное.

**Как правильно:**

```bsl
ТекстСообщения = НСтр("ru = 'Ни для одной из выбранных строк'
	+ ' не указан новый период отправки'");
```

### 21. `DuplicatedStringConstants` — Строковые литералы не должны дублироваться

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыявленныеДефекты/ObjectModule.bsl:188` · диагностика BSL LS: `DuplicateStringLiteral` · [карточка](https://docs.checkbsl.org/checks/overall/DuplicatedStringConstants/)

```bsl
 186 | 
 187 | 	Для каждого Строка Из СписокДефектов Цикл
 188 |         Строка.ДатаНачалаРемонта                      = Дата("00010101");  ← замечание
 189 |         Строка.ДатаОкончанияРемонта                   = Дата("00010101");
 190 |         Строка.КрайняяДопустимаяДатаЗавершенияРемонта = Дата("00010101");
```

**Что не так:** Строковый литерал, повторённый 3+ раза, при переименовании правят не везде — получают тихую рассинхронизацию; вынесите в константу модуля или функцию.

**Как правильно:**

```bsl
Перем ИмяСобытияЖР; // "ЗакрытиеРемонтов.Запись"
// ... в методах: ЗаписьЖурналаРегистрации(ИмяСобытияЖР, ...)
```

### 22. `LineLength` — Ограничение на длину строки (№444)

**Файл:** `/Users/lgovskijroman/Projects/toir2/Project/Toir/src/Documents/торо_ВыявленныеДефекты/ObjectModule.bsl:320` · диагностика BSL LS: `LineLength` · [карточка](https://docs.checkbsl.org/checks/overall/LineLength/)

```bsl
 318 | 	СтрокаДефекта = ЭтотОбъект.СписокДефектов.Добавить();
 319 | 	СтрокаДефекта.ID = Новый УникальныйИдентификатор;
 320 | 	СтрокаДефекта.ГарантийныйРемонт = торо_ГарантийноеОбслуживание.ПолучитьЗначениеФлагаГарантийныйРемонтДляОбъек  ← замечание
 321 | 	СтрокаДефекта.ДокументИсточник 	= ПараметрыСобытия.ДокументИсточник;
 322 | 	СтрокаДефекта.ОбъектРемонта 	= ПараметрыСобытия.ОбъектРемонта;
```

**Что не так:** Строка длиннее 120 символов (№444): горизонтальная прокрутка прячет конец выражения — самое важное.

**Как правильно:**

```bsl
ТекстСообщения = НСтр("ru = 'Ни для одной из выбранных строк'
	+ ' не указан новый период отправки'");
```

---

Карточки: docs.checkbsl.org/checks/<section>/<Ключ>/; ключи без каталога — 1c-syntax.github.io/bsl-language-server/diagnostics/<Код>/.
Срабатывания — кандидаты в findings 05a, решение за Ревьюером.
